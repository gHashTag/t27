# Wave Loop 838 Close-out Report

**Date:** 2026-08-04  
**Wave:** 838  
**Issue:** #1616  
**PR:** #1617  
**Branch:** `wave-loop-838`  
**Parent branch:** `wave-loop-837` HEAD (earlier wave PRs remain open)

---

## Variant implemented

**Variant A (recommended):** module-scope `[495][2]^6 Pt` non-power-of-two outer-dimension array-of-struct variable from call with indexed signed writes.

- Outer dimension: `495` (odd, non-power-of-two)
- Inner dimensions: `[2]^6` (fixed)
- Struct: `Pt { x : i16, y : i16 }` (32 bits)
- Elements: `495 × 64 = 31,680`
- Packed vector: `31,680 × 32 = 1,013,760` bits (~0.967 MiBit)

---

## Artifacts changed

- `scripts/gen_w838.py`
  - Copied from `scripts/gen_w837.py` and updated `OUTER = 495`, `MID_IDX = 247`.
  - Copy hazard fixed: destination path, module header f-string, and `MID_IDX` comment.
- `specs/scratch/w838_bench_module_495x2p6_aos_var_call_write.t27`
  - 2,174,932 bytes, 94,111 lines.
  - Generated by `python3 scripts/gen_w838.py`.
- `bootstrap/tests/icarus_lowerable.rs`
  - Added integration test `accepts_w838_bench_module_495x2p6_aos_var_call_write`.
- `.trinity/seals/scratch_w838_bench_module_495x2p6_aos_var_call_write.json`
  - Saved by `t27c seal --save`.

## Not changed

- `bootstrap/src/compiler.rs` — zero compiler changes for the witness.
- `scripts/cocotb_ref_model.py` — unchanged.
- `bootstrap/stage0/FROZEN_HASH` — unchanged `68a0b933c00ba5efd7facb5997f00880c3eecae55e6ac5e8cea2aee399b92adc`.

---

## Verification matrix

| Gate | Command | Result |
|------|---------|--------|
| Build | `cargo build --release -p t27c` | green (627 warnings, 0 errors) |
| Parse | `t27c parse specs/scratch/w838_bench_module_495x2p6_aos_var_call_write.t27` | PASS |
| Lowerable | `t27c icarus-lowerable ...` | `lowerable` |
| Simulate | `t27c icarus-simulate ...` | `PASSED` (17 cycles) |
| Cocotb | `t27c icarus-cocotb ...` | `reference-model OK` |
| Seal | `t27c seal --save ...` | saved |
| Unit tests | `cargo test -p t27c --bin t27c` | 1494/0/2 |
| `tri` | `cargo test -p tri` | 78/0 |
| `flash-spi` | `cargo test -p flash-spi` | 2/0 |
| `bitnet_pipeline` | `cargo test -p t27c --test bitnet_pipeline` | 20/0 |
| `bitnet_top` | `cargo test -p t27c --test bitnet_top` | 17/0 |
| `icarus_lowerable` | `cargo test -p t27c --test icarus_lowerable` | **298/0** |
| `verilog_const_array` | `cargo test -p t27c --test verilog_const_array` | 2/0 |

---

## Weak-point audit

No new actionable weak points surfaced during W838. Pre-existing items remain:

- `bootstrap/tests/verilog_array_literal_expr.rs` regression (deeper compiler lowering issue, tracked for a separate ring).
- FPGA E2E CI red (`sby` missing + Yosys static-cast error in generated `uart.v`).
- ~627 release warnings need a dedicated cleanup sprint.
- Vivado-in-Docker CI gap (private image not yet published).
- 30-day commit traceability by subject remains low; keep closing references in commit subjects.
- Generator copy hazard persists; parameterizing `WAVE`/`OUTER` in the template is still the top tooling backlog item.

---

## Cooperation variants for Wave Loop 839

- **A (recommended):** `[497][2]^6 Pt` — continue the outer-dimension ladder, `OUTER = 497`, `MID_IDX = 248`.
- **B:** `[495][3]^6 Pt` — grow the second inner dimension to stress stride scaling (94,656 elements, ~2.888 MiBit).
- **C:** `[495][2]^6 Pt` with negative-index writes to exercise wrap-around / signed-index lowering.

---

*φ² + φ⁻² = 3 | TRINITY*
