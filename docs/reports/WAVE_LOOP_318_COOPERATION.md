# Wave Loop 318 — Three Cooperation Variants for W319

**Date:** 2026-06-23
**Prepared for:** Next loop (W319) and external collaboration
**Competitive baseline:** CktFormalizer v3, Sparkle HDL (~230 theorems, 0 generic ∀), Ternary Fabric, BitNet-RISC-V

---

## Variant A: Steady-State to 41 (RECOMMENDED)

### Thesis
Continue +2 generic ∀ per wave to reach **41 by W319**. The 2-variable lattice is complete; now prove **associativity** and **commutativity** to close the algebraic foundation.

### W319 Targets

| Objective | Current | W319 Target |
|-----------|---------|-------------|
| Generic ∀ theorems | 39 | **41** |
| Total Lean 4 theorems | 68 | **70** |
| Pool A floor | 60 | **61** (uniform) |
| CODER floor | 51 | **52** |
| Pool B | 76 | **77** |
| Integration | 61 | **62** |

### Tactics
1. **Associativity Base Case:** `ternaryMacAssociativityBaseGeneric` — proves `mac(mac(0,a,.plus),b,.plus) = mac(0,a+b,.plus)` for generic `a`, `b`.
2. **Commutativity of Accumulation:** `ternaryMacAccumulateCommutativeGeneric` — proves `mac(mac(0,a,.plus),b,.plus) = mac(mac(0,b,.plus),a,.plus)` for generic `a`, `b`.

### Risk: VERY LOW | Reward: HIGH

---

## Variant B: Full Associativity Sprint

### Thesis
Prove **full MAC associativity** for all weight combinations. This closes the algebraic foundation for arbitrarily deep systolic arrays.

### W319 Targets

| Objective | Deliverable |
|-----------|-------------|
| Associativity (plus-plus) | `ternaryMacAssociativityPlusPlusGeneric` |
| Associativity (minus-minus) | `ternaryMacAssociativityMinusMinusGeneric` |
| Associativity (mixed) | `ternaryMacAssociativityMixedGeneric` |
| Total generic ∀ | **41** |

### Tactics
1. Prove `mac(mac(0, a, w1), b, w2) = mac(mac(0, b, w2), a, w1)` for generic `a`, `b`, `w1`, `w2`.
2. Extend to 2×2 GEMM tile associativity.
3. Document in `docs/TERNARY_MAC_ALGEBRA.md`.

### Risk: MEDIUM | Reward: VERY HIGH
Full associativity is the gateway to proving correctness of arbitrarily deep systolic arrays and tiled GEMM decompositions. It would be the **first complete associativity proof** for ternary MAC in any verified HDL.

---

## Variant C: Structural Induction for N-Scaling

### Thesis
Generalize the N-scaling pattern from instance proofs (Double through Penta) to a **structural induction theorem**: `∀ N, mac^N(0, a, .plus) = N*a`. This would be the first truly parametric depth proof in t27's Lean 4 suite.

### W319 Targets

| Objective | Deliverable |
|-----------|-------------|
| N-Scaling inductive hypothesis | `ternaryMacNPlusScalingInductive` |
| Base case (N=1) | `ternaryMacOnePlusScalingGeneric` |
| Inductive step | `ternaryMacPlusScalingSucc` |
| Total generic ∀ | **41** |

### Tactics
1. Define a Lean 4 recursive function `ternaryMacN` parametrized by depth `N`.
2. Prove by induction on `N` that `ternaryMacN 0 a .plus = N*a`.
3. Show that this subsumes all instance theorems (Double through Penta) as corollaries.

### Resource Needs
- 1 proof engineer (50% time)
- Mathematical consultation on dependent types in Lean 4 (15% time)

### Risk: MEDIUM-HIGH | Reward: VERY HIGH
This would be the **first formally verified parametric-depth proof** for ternary systolic arrays. It would elevate t27 from "instance proofs" to "general theory" — a qualitative leap that competitors cannot match. The PQC universal masking paper (arXiv:2604.18717) proves that universal hardware proofs in Lean 4 are feasible; structural induction for N-scaling would be t27's universal proof.

---

## Recommendation

Execute **Variant A** for W319. Initiate **Variant C** as background research (1 theorem/week). **Variant B** is a stretch goal for W320–W321.

*Cooperation variants generated by Trinity Agent (Queen) following AEL v2.0.*
