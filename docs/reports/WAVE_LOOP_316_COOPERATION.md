# Wave Loop 316 — Three Cooperation Variants for W317

**Date:** 2026-06-23
**Prepared for:** Next loop (W317) and external collaboration
**Competitive baseline:** CktFormalizer v3, Sparkle HDL (~230 theorems, 0 generic ∀), KU Leuven LUT DSE, Sparse-BitNet

---

## Variant A: Steady-State to 38 (RECOMMENDED)

### Thesis
Continue +2 generic ∀ per wave to reach **38 by W317**. The N-scaling pattern family now covers depths 2–5 for plus weights and 2–4 for minus weights. Completing the depth-5 minus pair and adding a commutativity theorem is low-risk, high-reward.

### W317 Targets

| Objective | Current | W317 Target |
|-----------|---------|-------------|
| Generic ∀ theorems | 36 | **38** |
| Total Lean 4 theorems | 65 | **67** |
| Pool A floor | 58 | **59** (uniform — adder_tree catches up) |
| CODER floor | 49 | **50** |
| Pool B | 74 | **75** |
| Integration | 59 | **60** |

### Tactics
1. **Penta Minus Scaling:** `ternaryMacPentaMinusGeneric` — proves `mac⁵(0,a,.minus) = -5*a`. Completes the depth-5 pair.
2. **MAC Commutativity (zero-weight):** `ternaryMacCommutativityZeroWeightGeneric` — proves `mac(0,a,w) = mac(a,0,w)` for generic `a` and zero weight.

### Risk: VERY LOW | Reward: HIGH

---

## Variant B: Associativity Sprint

### Thesis
Prove **MAC associativity** for zero-psum cases. This is the last major algebraic property missing from the ternary MAC theory. Now that the N-scaling family reaches depth 5, associativity becomes the critical missing link for arbitrarily deep systolic arrays.

### W317 Targets

| Objective | Deliverable |
|-----------|-------------|
| Associativity base case | `ternaryMacAssociativityZeroPsumGeneric` |
| Tiled GEMM correctness | `ternaryGemm2x2AssociativeGeneric` |
| Total generic ∀ | **38** |

### Tactics
1. Prove `mac(mac(0, a, w1), b, w2) = mac(0, a, w1) + ternaryMul(b, w2)` for generic `a`, `b`, `w1`, `w2`.
2. Extend to 2×2 GEMM tile associativity.
3. Document in `docs/TERNARY_MAC_ALGEBRA.md`.

### Risk: MEDIUM | Reward: VERY HIGH
Associativity is the gateway to proving correctness of arbitrarily deep systolic arrays and tiled GEMM decompositions. It would be the **first associativity proof** for ternary MAC in any verified HDL.

---

## Variant C: Structural Induction for N-Scaling

### Thesis
Generalize the N-scaling pattern from instance proofs (Double through Penta) to a **structural induction theorem**: `∀ N, mac^N(0, a, .plus) = N*a`. This would be the first truly parametric depth proof in t27's Lean 4 suite.

### W317 Targets

| Objective | Deliverable |
|-----------|-------------|
| N-Scaling inductive hypothesis | `ternaryMacNPlusScalingInductive` |
| Base case (N=1) | `ternaryMacOnePlusScalingGeneric` |
| Inductive step | `ternaryMacPlusScalingSucc` |
| Total generic ∀ | **38** |

### Tactics
1. Define a Lean 4 recursive function `ternaryMacN` parametrized by depth `N`.
2. Prove by induction on `N` that `ternaryMacN 0 a .plus = N*a`.
3. Show that this subsumes all instance theorems (Double through Penta) as corollaries.

### Resource Needs
- 1 proof engineer (50% time)
- Mathematical consultation on dependent types in Lean 4 (15% time)

### Risk: MEDIUM-HIGH | Reward: VERY HIGH
This would be the **first formally verified parametric-depth proof** for ternary systolic arrays. It would elevate t27 from "instance proofs" to "general theory" — a qualitative leap that competitors cannot match. The KU Leuven LUT DSE paper explicitly parametrizes chain depth (`L`, `μ`, `K`) — a structural induction theorem would provide formal guarantees for **any** parameter choice.

---

## Recommendation

Execute **Variant A** for W317. Initiate **Variant C** as background research (1 theorem/week). **Variant B** is a stretch goal for W318–W319.

*Cooperation variants generated by Trinity Agent (Queen) following AEL v2.0.*
