# Wave Loop 332 — Cooperation Variants for W333

**Date:** 2026-06-23  
**Prepared for:** IGLA CODER + IGLA RACE coordination  
**Branch:** `trinity-rust-rings`

---

## Strategic Context

Wave Loop 332 achieved **83 generic ∀ theorems** — a capstone milestone with
the semiring action theorem unifying the entire proven lattice. The conformance
suite reports **0 failures** for the first time in recent history, confirming
pipeline robustness.

The next wave (W333) targets:
- Pool A floor ≥75, CODER ≥65, Pool B ≥93, Integration ≥75
- Lean 4 generic ∀ ≥86 (3 new theorems)
- 10-variable accumulation boundary test (omega saturation)
- TorchLean collaboration research spike

---

## Variant A — Aggressive Depth (Race Prioritized)

**Allocation:**
- Pool A: +2 invariants/spec (double depth)
- Pool B: +1 invariant (standard)
- CODER: +1 invariant/spec (standard)
- Integration: +1 invariant (standard)
- Lean 4: **3 generic ∀ theorems** — 10-variable accumulation (plus/minus) + ring inverse

**Rationale:**
Pool A (RTL specs) is the competitive differentiator. Doubling invariants for
one wave accelerates hardware coverage depth, signaling commitment to
high-assurance RTL verification. 10-variable accumulation tests the `omega`
saturation boundary — if it succeeds, t27 proves `simp+omega` scales to 10
variables; if it fails, we learn the automation limit.

**Risk:**
10-variable expressions may cause `omega` timeouts. Need fallback to `ring_nf`
or `native_decide` if `omega` fails.

**Commit message pattern:** `feat(w333): W333 IGLA CODER+RACE — Pool A double-depth, 10-variable accumulation, 86 generic ∀`

---

## Variant B — Balanced Expansion (Recommended)

**Allocation:**
- Pool A: +1 invariant/spec (standard)
- Pool B: +1 invariant (standard)
- CODER: +1 invariant/spec (standard)
- Integration: +1 invariant (standard)
- Lean 4: **3 generic ∀ theorems** —
  - `ternaryMacAccumulateTenPlusGeneric` (10-variable plus accumulation)
  - `ternaryMacAccumulateTenMinusGeneric` (10-variable minus accumulation)
  - `ternaryMacRingInverseGeneric` (additive inverse: `mac(psum, -a, w) = -mac(0, a, w) + psum`)

**Rationale:**
Maintains uniform depth progression across all pools. 10-variable accumulation
pushes the N-variable family to the predicted `omega` boundary. Ring inverse
theorem completes the ring-like algebraic structure (additive inverses) for
ternary MAC, building on the semiring action (W332).

**Commit message pattern:** `feat(w333): W333 IGLA CODER+RACE — Pool A 74→75, CODER 64→65, Pool B 91→92, Lean 4 83→86 generic ∀`

---

## Variant C — TorchLean Collaboration (Research Priority)

**Allocation:**
- Pool A: +1 invariant/spec (standard)
- Pool B: +1 invariant (standard)
- CODER: +1 invariant/spec (standard)
- Integration: +1 invariant (standard)
- Lean 4: **2 generic ∀ theorems** (conservative)
- **NEW:** Research task — evaluate TorchLean SSA/DAG IR for ternary MAC primitive

**Rationale:**
TorchLean (arXiv:2602.22631, lean-dojo group) formalizes neural networks in
Lean 4 with a shared SSA/DAG IR. Extending this IR with ternary MAC primitives
would bridge TorchLean's NN verification with t27's hardware-level ternary MAC
algebra, enabling end-to-end proofs: "this quantized model runs correctly on this
verified ternary accelerator."

**Task breakdown:**
1. Clone TorchLean repository and build it.
2. Identify IR extension points for custom operators.
3. Draft `TernaryMacOp` primitive with semantics matching t27's `ternaryMac`.
4. Prototype 1 equivalence theorem: TorchLean's ternary MAC equals t27's
   `ternaryMac` for concrete inputs.
5. Write collaboration proposal for TorchLean authors.

**Risk:**
TorchLean may have breaking changes; integration timeline uncertain. Recommend
starting with a GitHub issue to gauge author interest before significant effort.

**Commit message pattern:** `feat(w333): W333 IGLA CODER+RACE + research(w333): TorchLean ternary MAC IR extension`

---

## Recommendation

**Execute Variant B (Balanced Expansion)** as the primary W333 plan. Variant B
maintains the proven cadence while pushing the N-variable accumulation boundary
to depth 10 and completing the ring structure with additive inverses.

**Parallel track:** Initiate Variant C (TorchLean) as a lightweight research
spike (~15% effort). Open a GitHub issue on lean-dojo/TorchLean proposing the
ternary MAC primitive extension. If authors respond positively by W334, escalate
to full integration.

**Avoid Variant A** unless Pool A depth is explicitly requested by downstream
stakeholders. Double-depth creates non-uniformity and risks `omega` timeouts
across more specs.

---

*Cooperation variants generated by Trinity Agent (Queen) — AEL v2.0*
