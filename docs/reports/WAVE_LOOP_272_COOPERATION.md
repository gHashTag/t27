# Wave Loop 272 — Three Cooperation Variants for W273

**Date:** 2026-06-16
**Prepared for:** Wave Loop 273
**Status:** ✅ COMPLETE

---

## Variant A: Submit + Resume with Structural Correction (Recommended)

**Strategy:** Submit W272 commit to mainline, then execute a **structural correction wave** to resolve the 7 specs with undefined references and nesting defects discovered in the W272 weakness audit.

**W273 Plan:**
- **Structural correction** — Fix undefined references in `formal.t27`, `gemm.t27`, `opcodes.t27`, `bram_weights.t27`, `bench_proxy.t27`, `arch.t27`, `dataset.t27`. Dedented nested invariants to module level. Remove duplicate invariant names. +0 tests, +0 invariants (pure refactoring).
- **Pool A floor elimination** — After structural correction, raise `ternary_gemm` and `ternary_mac` from 19→20. +4 tests, +2 invariants.
- **Pool A floor completion** — Raise `formal` from 19→20 (after its undefined references are fixed). +2 tests, +1 invariant.
- **CODER depth** — Raise `arch` from 10→11. +3 tests, +1 invariant.
- **Pool B depth** — Raise `systolic_ternary` from 24→25. +2 tests, +1 invariant.
- **Total:** +11 tests, +5 invariants across 5 specs (plus structural correction in 7 specs).

**Rationale:** W272 weakness audit discovered structural defects that compound with each new invariant addition. Cleaning these defects before further floor-raising prevents technical debt accumulation. Variant A is the most sustainable path.

**Risk:** Low-Medium. Structural corrections are safe but touch many files.

---

## Variant B: Pool A Uniform ≥20 Priority

**Strategy:** Ignore structural defects for one more wave and push all remaining Pool A floor specs to ≥20.

**W273 Plan:**
- **Pool A floor elimination** — Raise `ternary_gemm`, `ternary_mac`, and `formal` from 19→20. +6 tests, +3 invariants.
- **Pool A depth** — Raise `cordic` and `rtl` from 20→21. +4 tests, +2 invariants.
- **Total:** +10 tests, +5 invariants across 5 specs.

**Rationale:** ALL Pool A ≥20 is a historic milestone that would make t27 the first spec-first hardware description language with uniform 20+ invariants across all core RTL specs. The structural defects are non-blocking; they can be deferred to W274.

**Risk:** Medium. Adding invariants to specs with undefined references increases technical debt.

---

## Variant C: CODER Uniform ≥11 Priority

**Strategy:** Shift focus to CODER after 4 consecutive Pool A-centric waves (W269–W272).

**W273 Plan:**
- **CODER floor elimination** — Raise `arch`, `dataset`, `benchmark`, `pipeline`, and `prm` from 10→11. +12 tests, +5 invariants.
- **Total:** +12 tests, +5 invariants across 5 specs.

**Rationale:** CODER has been the trailing category for 272 waves. With `bench_proxy` now at 11, the remaining 9 specs at 10 represent a significant floor. A concentrated CODER push could achieve ALL CODER ≥11 in 2 waves (W273–W274), matching the pace of Pool A historic milestones.

**Risk:** Medium. CODER specs have lower test-to-invariant ratios; new invariants require more semantic creativity.

---

## Recommended Selection

**Variant A** is recommended for W273. The structural correction discovered in W272 is a clear signal to pause mechanical floor-raising and pay down technical debt. Fixing 7 specs' undefined references and nesting defects now prevents cascading maintenance issues later.

If the structural correction is deferred, **Variant B** achieves the strongest visible milestone (ALL Pool A ≥20).

If CODER parity is urgent, **Variant C** delivers the fastest CODER floor raise.

---

## Process Note for W273

**CRITICAL:** Continue running `git status --short | grep '\.t27'` before starting W273 edits. Four consecutive waves with zero latent changes confirms the strengthened check is reliable.

**CRITICAL:** If executing Variant A, run the structural correction in a separate commit (or separate PR) from the +11 tests / +5 invariants commit to keep diffs reviewable.

---

*Generated by Trinity S³AI autonomous wave loop.*
*φ² + 1/φ² = 3 | TRINITY*
