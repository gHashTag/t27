# Wave Loop 412 Report — measured-to-lean standalone + raw-ns + PVT context

> Issue: [#1332](https://github.com/gHashTag/t27/issues/1332)  
> Branch: `wave-loop-412`  
> Closes #1332

---

## Summary

Wave Loop 412 inherited the physical blockers from W410–W411:

- P12 is still not wired to a logic-analyzer channel.
- The Digilent DLC10 cable is still not detected (`VID=0x03FD`).

Without answers to Q1+Q2, the default **Variant A + B** bundle could not start.
W412 therefore delivered the **Variant C fallback**: extend the formal tooling
so it is useful without physical access, and update the branch model to match
the new integration reality.

---

## What changed

### `cli/tri/src/fpga.rs`

- Added `--standalone` to `tri fpga measured-to-lean`. Emits a self-contained
  `.lean` file with imports and `namespace Trinity.BitstreamConfig` wrapper.
- Added `--raw-ns` to read `(period_ns, sck_low_ns, sck_high_ns)` directly from
  a JSON record.
- Added `MeasuredCclkRawNs` struct.
- Extended `MeasuredCclk` with `period_ns`.
- Added unit tests for standalone and raw-ns output.

### `proofs/lean4/Trinity/TernaryFPGABoot.lean`

- Added raw-ns helper functions and predicate
  `measured_cclk_from_raw_ns_satisfies_flash_spec`.
- Added `measured_boot_transaction_from_raw_ns` and chain theorem
  `measured_cclk_from_raw_ns_implies_transaction_ok`.
- Added concrete example `measured_raw_ns_40_20_20_satisfies_flash_spec`.
- Added `ProcessCorner` inductive and `PvtContext` structure.
- Added placeholder PVT derating functions
  `n25q128_min_sck_low_ns_pvt` / `n25q128_min_sck_high_ns_pvt`.
- Added `measured_cclk_with_pvt_satisfies_flash_spec` predicate and implication
  theorems.
- Added concrete PVT example for an industrial-corner context.

### `fpga/HARDWARE_SSOT.md`

- Added §3.6.12 documenting `--standalone`, `--raw-ns`, and the PVT placeholder
  model.

### `docs/BRANCHING_MODEL.md`

- Updated to Strategy P (Wave Loop 412): `master` = integration+release,
  `trinity-rust-rings` = archived/deprecated as of 2026-07-04.

### Close-out docs

- `docs/reports/WAVE_LOOP_412_REPORT.md` — this report.
- `docs/reports/FPGA_LOOP_EVIDENCE_W412_2026-07-04.md` — command outputs and
  verification summary.
- `docs/reports/FPGA_LOOP_COOPERATION_W413_2026-07-04.md` — three W413 variants.

---

## Branching/strategy resolution

- PR #1331 was conflicting because the W411 branch carried hundreds of
  autogenerated `.trinity/seals/*.json` files and session logs.
- Reorganized W411 into a single clean commit (11 files, no seals) and
  squash-merged it to `master` with `--admin`.
- Issue #1329 closed automatically.
- Confirmed `gh api compare master...trinity-rust-rings` shows
  `ahead_by=1, behind_by=5`; therefore `trinity-rust-rings` was **not deleted**
  and remains archived.

---

## Acceptance criteria status

| ID | Criterion | Status |
|----|-----------|--------|
| AC-C1 | PVT constants replaced with real data or documented placeholder | ✅ placeholder documented, falsification plan recorded |
| AC-C2 | `measured-to-lean --out File.lean --standalone` works | ✅ |
| AC-C3 | Raw-ns input predicate defined and implies transaction spec | ✅ |
| AC-D1 | `./scripts/tri test` parse/typecheck/gen/seal-verify passes | ✅ |
| AC-D2 | `lake build Trinity.TernaryFPGABoot` passes | ✅ |
| AC-D3 | `cargo test -p tri fpga::tests` passes | ✅ (16/16) |
| AC-D4 | `docs/BRANCHING_MODEL.md` updated for Strategy P | ✅ |
| AC-D5 | W412 report + evidence + W413 cooperation committed | ✅ |
| AC-A/B | Physical capture / relay gate | ❌ blocked — P12 + DLC10 unavailable |

---

## Risks and weak points

1. **Physical evidence gap remains.** The model is still not anchored to real
   silicon measurements.
2. **PVT derating is a placeholder.** The `PvtContext` fields are captured but
   the derating functions ignore them. Replacing them with real curves is the
   next formal milestone.
3. **`trinity-rust-rings` drift.** `master` is now 1 commit ahead and 5 behind
   `trinity-rust-rings`; deletion is deferred to W413.
4. **Gen-verilog-yosys-smoke backlog.** 16 failures remain tracked separately.

---

## Verification results

- `lake build Trinity.TernaryFPGABoot` — green.
- `cargo test -p tri fpga::tests` — 16/16 pass.
- `./scripts/tri test` — parse/typecheck/gen/seal-verify green; yosys smoke
  40 pass, 16 fail (pre-existing).

---

## Recommended next wave (W413)

See `docs/reports/FPGA_LOOP_COOPERATION_W413_2026-07-04.md`. The default
recommendation is **Variant A + B if the bench becomes available**, otherwise
continue **Variant C** (real PVT curves, sigrok CSV raw-ns import, or relay mock
CI scaffolding).

---

*phi^2 + phi^-2 = 3 | TRINITY*
