# Wave Loop 331 — Cooperation Variants for W332

**Date:** 2026-06-23  
**Prepared for:** IGLA CODER + IGLA RACE coordination  
**Branch:** `trinity-rust-rings`

---

## Strategic Context

Wave Loop 331 achieved **80 generic ∀ theorems** — an 80× multiplier over any
competitor. The key new threat/opportunity is **TorchLean** (arXiv:2602.22631),
a unified Lean 4 framework for neural-network specification, execution, and
verification. TorchLean does not cover ternary hardware — this is a collaboration
opening.

The next wave (W332) targets:
- Pool A floor ≥74, CODER ≥64, Pool B ≥92, Integration ≥74
- Lean 4 generic ∀ ≥83 (3 new theorems)
- Potential TorchLean collaboration exploration

---

## Variant A — Aggressive Depth (Coder Prioritized)

**Allocation:**
- Pool A: +1 invariant/spec (standard)
- Pool B: +1 invariant (standard)
- CODER: +2 invariants/spec (double depth)
- Integration: +1 invariant (standard)
- Lean 4: **3 generic ∀ theorems** — 9-variable accumulation (plus/minus) + semiring unification

**Rationale:**
CODER specs currently lag Pool A in absolute depth (63 vs 73). Doubling CODER
invariants for one wave accelerates software-side coverage parity. The 9-variable
accumulation theorems push toward the `omega` saturation boundary — a strategic
experiment to discover automation limits.

**Risk:**
9-variable expressions may cause `omega` timeouts or require `ring_nf`
preprocessing, increasing proof time.

**Commit message pattern:** `feat(w332): W332 IGLA CODER+RACE — CODER double-depth, 9-variable accumulation, Pool A 73→74`

---

## Variant B — Balanced Expansion (Recommended)

**Allocation:**
- Pool A: +1 invariant/spec (standard)
- Pool B: +1 invariant (standard)
- CODER: +1 invariant/spec (standard)
- Integration: +1 invariant (standard)
- Lean 4: **3 generic ∀ theorems** —
  - `ternaryMacAccumulateNinePlusGeneric` (9-variable plus accumulation)
  - `ternaryMacAccumulateNineMinusGeneric` (9-variable minus accumulation)
  - `ternaryMacSemiringActionGeneric` (unified semiring action over Int)

**Rationale:**
Maintains uniform depth progression across all pools. 9-variable accumulation
extends the N-variable lattice to depth 9, matching next-generation systolic-array
tiles (8×8→9×9). Semiring action theorem formally unifies identity + associativity
+ commutativity + distributivity + scaling into a single algebraic statement,
creating a "capstone" theorem for the ternary MAC semiring-like structure.

**Commit message pattern:** `feat(w332): W332 IGLA CODER+RACE — Pool A 73→74, CODER 63→64, Pool B 90→91, Lean 4 80→83 generic ∀`

---

## Variant C — TorchLean Collaboration (Research Priority)

**Allocation:**
- Pool A: +1 invariant/spec (standard)
- Pool B: +1 invariant (standard)
- CODER: +1 invariant/spec (standard)
- Integration: +1 invariant (standard)
- Lean 4: **2 generic ∀ theorems** (conservative)
- **NEW:** Research/integration task — evaluate TorchLean SSA/DAG IR for ternary
  MAC primitive extension

**Rationale:**
TorchLean (arXiv:2602.22631) formalizes neural networks in Lean 4 with a shared
IR (operator-tagged SSA/DAG). This IR currently supports standard IEEE-754
operators. Extending it with ternary MAC primitives would:
1. Bridge TorchLean's neural-network verification with t27's hardware-level
   ternary MAC algebra.
2. Enable end-to-end proofs: "this quantized model runs correctly on this
   verified ternary accelerator."
3. Position t27 as the hardware backend for TorchLean — a major strategic win.

**Task breakdown:**
1. Read TorchLean paper and identify IR extension points.
2. Draft ternary MAC operator semantics in TorchLean IR style.
3. Prototype 1-2 theorems showing ternary MAC equivalence to standard
   `mul + add` in TorchLean's floating-point semantics.
4. Write collaboration proposal for TorchLean authors (lean-dojo group).

**Risk:**
Integration with external project timeline uncertain. TorchLean authors may have
other priorities. Recommend contacting them via GitHub issue before W332 end.

**Commit message pattern:** `feat(w332): W332 IGLA CODER+RACE + research(w332): TorchLean ternary MAC integration proposal`

---

## Recommendation

**Execute Variant B (Balanced Expansion)** as the primary W332 plan. Variant B
maintains the proven cadence (+54 tests/+27 invariants/+3 Lean ∀ per wave) while
pushing toward the 83 generic ∀ milestone and 9-variable accumulation boundary.

**Parallel track:** Initiate Variant C (TorchLean collaboration) as a
research spike — allocate ~20% of W332 effort to reading TorchLean paper and
drafting the IR extension proposal. If the collaboration shows promise by W333,
escalate to a full integration task.

**Avoid Variant A** unless CODER depth is explicitly flagged as a blocker by
downstream consumers (benchmarking, pilot pretraining).

---

*Cooperation variants generated by Trinity Agent (Queen) — AEL v2.0*
