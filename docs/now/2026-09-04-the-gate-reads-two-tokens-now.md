# NOW -- The gate reads two tokens now (2026-09-04)

## The gate reads two tokens now (Closes #3114)

- A command with subcommands prints its own Commands: block and one that takes arguments does not -- that structural rule is what lets the second token be checked without reading t27c gen specs/x.t27 as a subcommand specs
- Dead tri mentions 99 -> 136, distinct names 24 -> 35: tri skill commit 11, tri skill seal 8, tri math compete 4, tri notebook query 3, tri experience record 3, every one confirmed by running it
- notebook and math are t27c groups reached through the forward-anything fallthrough; dropping it loses both, and the mutation proves it
- Four mutations: a planted dead second token fails, an always-empty group list fails, dropping the fallthrough fails, and making every leaf look like a group takes the reading to 192 and trips the self-check
