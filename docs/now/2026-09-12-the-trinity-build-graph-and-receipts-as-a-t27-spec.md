# NOW -- The Trinity build graph and receipts as a .t27 spec (2026-09-12)

## S03 of gHashTag/trinity#988: the build graph, pinned dependencies and generation provenance (Closes #3565)

- `specs/trinity/build_graph.t27` (module `trinity_build_graph`, `KIND = "build-graph"`) declares what the consumer's build is made of at `976df517`: Zig `0.15.x` required and `0.15.2` measured with on `ubuntu-latest`; `zig build -Dci=true` and `zig build test -Dci=true`, the latter with an exit code that does not reach the job (gHashTag/trinity#616, fix proposed in #987); the two `build.zig` options; the four `build.zig.zon` pins with how `build.zig` uses each (`emsdk` pinned but not referenced directly; `raylib` only in the `!ci_mode` GUI targets; `zig_hdc` and `zig_golden_float` as the modules the library imports); the submodule `external/zig-golden-float` as a second, unpinned reference the build does not read; the seven profiles of the manifest with what is measured for each; the untracked build outputs; fourteen tracked files a generator writes, each with its generator and its inputs; the seven generator output locations; and the receipt policy of the bootstrap/fixture profile: two independent runs, byte-identical, no normalization.
- `tools/trinity_build_receipt.py graph --trinity-root` derives `conformance/trinity/build_graph.json` from the pinned tree: 44 named modules with their roots, 252 import edges resolved to 84 named modules, 152 package modules (149 to `zig-hdc-vsa` of zig_hdc, 3 to `golden-float` of zig_golden_float) and 16 inline modules, 0 unresolved; the generated files and their inputs hashed at the pin (blob hashes for files, tree hashes for directories). `receipt --runs 2` writes `conformance/trinity/bootstrap_receipt.json`: the compiler-source revision (`bff21b85`, the last commit that changed `bootstrap/src`, `Cargo.toml` or `Cargo.lock`), the `t27c` binary hash, the host, and the hash of what each of the four backends generated from each of the 18 matrix fixtures in each run: 72 outputs, 0 differing -- deterministic. `check` recomputes offline that the compiler sources, `Cargo.lock`, the fixtures and a fresh generation still match the receipt, and with `--trinity-root` judges drift in the consumer: a generated file changed without its inputs is a suspected hand edit, inputs changed without the file is stale, JSON in a generated location that no entry registers is unregistered; at the pin there is none. `--self-check` plants each of those and an unresolved import.
- A fresh clone builds the bootstrap profile: `spec-guards` builds `t27c` from a clean checkout on every pull request (https://github.com/gHashTag/t27/actions/runs/34686608378 on #3577); the consumer's own CI resolves the pinned packages and builds the headless profile (https://github.com/gHashTag/trinity/actions/runs/34678824282). Full application-profile reproduction is S12's.
- `tools/trinity_compiler_matrix.py` now names the compiler-source revision by `bootstrap/src` rather than `bootstrap/` (the matrix fixtures live under `bootstrap/tests/`), the same revision `bff21b85`; its record and the seal of `compiler_matrix.t27` were refreshed. `tools/published_figures.py`: the `test blocks` pin follows the corpus by the four test blocks of the spec, 12659 -> 12663.
- Not claimed: that the consumer's test graph passes (its exit code is not measured), that the native, fpga, training or network profiles build (unbuilt or external, as `PROFILE_STATUS` says), or that the receipt covers more than the compiler and the fixture generation of this repository.
