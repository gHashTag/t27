# NOW -- When you cannot separate two populations, show them (2026-08-24)

## When you cannot separate two populations, show them (Closes #2161)

- The boundary column's denominator holds comparisons that decide a verdict and loop bounds that cannot, and no line-local filter separates them: a kill is the only proof of verdict-reachability, and the filter tried yesterday removed proven kills.
- So the column is unreadable rather than wrong. It now prints the source beside each survivor's line number, for this operator only: if len(out) > 6 and while len(v) < N classify themselves at a glance, and one of six is worth reading.
- And the summary says what the denominator is when this operator ran, so killed/total is not read as a rate. When a metric mixes populations you cannot separate, report the members rather than a ratio [measured].
