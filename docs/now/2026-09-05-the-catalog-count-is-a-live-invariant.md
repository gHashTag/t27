# NOW -- The catalog count is a live invariant, and the pages that still fixed it (2026-09-05)

## The catalog count is a live invariant, and the pages that still fixed it (Refs #2466)

- the SSOT `specs/numeric/formats_catalog.t27` carries **109** formats; the live arXiv:2606.09686v2 declares 83, and v3 ("Golden Ruler", submitted 4 Sep 2026 as submit/8037924, announces Mon 7 Sep 2026) declares 109 -- `python3 tools/check_catalog_count.py` exits 0 with SSOT == fresh regen == 109 and WARNs on the v2 divergence by design until v3 is live (Refs #2466)
- living pages that named the catalog by its count -- `README.md`, `conformance/vectors/README.md`, `docs/POSITIONING_CONFORMANCE_LAYER.md`, `conformance/witness/gf256/{README,SEPARATION_BOUND}.md`, `.claude/skills/tnf-gfternary.md` -- now carry 83 as a dated measurement next to the live number and cite the paper under its v3 title
- no die exists: TTSKY26a and TTSKY26b were withdrawn before fabrication and refunded; the gf16 oracle is the Artix-7 XC7A200T FPGA prototype, so "silicon-oracle anchor" became "FPGA-oracle anchor (XC7A200T)"; the TNF manuscript is under review at Microprocessors and Microsystems (Elsevier), MICPRO-D-26-00839, submitted 3 Sep 2026, with no arXiv preprint
- **this fix-up:** `conformance/vectors/crosswalk_sw_hw.py` read its SW total from `INDEX_all_formats.json` and then printed a hard-coded 83 into its own header; it now prints the count it read (dry run in a temp dir: `(=109)`, the July HW snapshot kept and dated); the W778 comment in `specs/numeric/golden_sieve.t27` and the W602 entry in `docs/theory/IGLA-FORMAL-RESULTS.md` now say which catalogue they measured, and the ledger's header says that *silicon* there means an FPGA die
- `specs/numeric/golden_sieve.t27` was resealed with a freshly built `t27c` (`seal --save`, then `--verify`: all five hashes MATCH); only `spec_hash` and `sealed_at` moved and the four `gen_hash_*` are byte-identical -- a comment does not change what the spec generates
- `docs/theory/TNF_ARTICLE_RU.md` reference 11 pointed at hackaday.io/project/183153 (404, no Wayback snapshot); the repo's corrected English bibliography (`docs/reports/upstream/tnf_paper.FIXED.tex`) already carries hackaday.io/project/164907 (Ternary Computing Menagerie, HTTP 200, hosts "Ternary27 Standard.pdf") and the translation now matches it; reference 28's DOI 10.1145/3649329.3656502 resolves at Crossref (FQP, DAC '24) and only dl.acm.org refuses non-browser fetches
- the three required gates were red because the PR added no `docs/now/` entry and referenced no t27 issue; this entry and `Refs #2466` in the PR body are the repair
