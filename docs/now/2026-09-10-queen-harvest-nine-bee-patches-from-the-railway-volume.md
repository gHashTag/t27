# NOW -- Queen harvest: nine bee patches from the Railway volume (2026-09-10)

## Queen harvest: nine bee patches from the Railway volume (Closes #3508, #3515, #3524, #3525, #3530, #3531, #3534, #3536, #3538; PR #3560)

- The swarm on `trios-agent-server` (Railway project 999) worked #3507-#3542 on 2026-09-09/10 and committed to `queen-<issue>` branches inside the container volume; the bee contract says "do not push", so nothing reached GitHub and the swarm sat idle with no boundary issues left. Harvested over `railway ssh` as `git format-patch` per branch (34 branches, 3 more issues had only uncommitted worktree diffs) and applied to `master` `4dfb1ab6` with `git am --3way`; every patch applied clean, bee authorship (`Trinity Bee <bee@trinity.local>`) kept.
- Re-running each issue's own acceptance criteria with `t27c` built from `master`: 9 patches meet them and pass every repository gate; 20 introduce a hard parse error in the file they touch (all 20 files parse clean on `master`); 4 parse with a DISCARD of a test-block assertion (`std.mem.eql`, `@FieldType`, `std.meta.fields`); 1 (#3518, `mha_block.t27`) meets its numbers but fails `typecheck` (`forward` expects 2 args, got 3) and adds four `TODO: Implement dropout` markers. Only the 9 are merged. The swarm's own reviewer had marked 26 as accepted -- INFERENCE: the review lane has no `t27c` in the image and accepted on the body scan alone.
- Merged: `random.t27` (#3508), `ring_buffer.t27` (#3515), `relu_activation.t27` (#3524), `bezier.t27` (#3525), `gelu_activation.t27` (#3530), `array.t27` (#3531), `linked_list.t27` (#3534), `sha256.t27` (#3536), `constants.t27` (#3538). `t27c parse-complete` over `specs/`: 838 / 693 consume-all / 0 TRUNCATE / 76 DISCARD / 69 no-parse, identical to `master`. `t27c suite --ratchet --corpus-only`: CLEAN. Seals for the nine specs re-saved with `t27c seal --save` (18 seal files; the four `gen_hash_*` fields moved because the generated code moved, which is what a seal is for). Published-figures pins moved with the reason in the pin: test blocks 12465 -> 12542 (+77), `x.len` field reads 680 -> 710 (+30), `[]T{}` literals 478 -> 480 (+2). No `--bless`, no ratchet raised.
- Not claimed: the 20 + 4 + 1 rejected patches are not fixed here and their issues stay open; the 3 uncommitted worktree diffs (#3507 `bitvector.t27`, #3514 `status.t27`, #3521 `text.t27`) are not applied. Follow-up: give the in-container reviewer a `t27c` binary, or move the acceptance check to a GitHub Action on pushed `queen/*` branches, so a green review means the criteria actually ran.
