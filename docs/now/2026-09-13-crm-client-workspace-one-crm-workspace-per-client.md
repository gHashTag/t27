# NOW -- CRM client workspace: one CRM workspace per client (2026-09-13)

## CRM client workspace: a dashboard and a thread per client (Closes #3604)

- `specs/automation/crm-client-workspace.t27` (module `automation::crm_client_workspace`, `KIND = "automation"`) answers the owner's 2026-09-13 remark that every client's chat lands in one place. Measured before the spec (code and render Postgres read the same day): `agent_messages` was keyed by `telegram_id` only -- one thread per person across bot, app and phone; the mini app kept one localStorage atom and "new conversation" DELETEd the whole server history; `/crm` was three flat panels with no client page; duet runs lived in a process `Map` and the report went to Saved Messages -- the redeploy of #2377 at 19:41-19:44 +07 cut run `duet-mtzsz1la` after its first pair.
- The thread: `THREAD_COLUMN = "thread"` on `agent_messages`, `THREAD_SELF = "self"` as DEFAULT so every existing row and caller is unchanged, `THREAD_CLIENT_PREFIX = "client:"` for the seller's thread about one client; `THREAD_PARAM = "client"` on all `ROUTES_WITH_THREAD = 4` agent routes; `DELETE_CLEARS_ONLY_ITS_THREAD = true`. `fn thread_for(digits, is_caller)` falls back to `self` for a malformed id or the caller's own id. Context (`CONTEXT_INJECTED = "crm_client_profile+lead_context"`) enters the system prompt as context, not as instructions.
- The gate: `GATE_BEFORE_DB = true` -- `fn refused_before_db(digits, is_caller, is_seller)` refuses a stranger, an id outside `CLIENT_ID_MIN_DIGITS..CLIENT_ID_MAX_DIGITS = 5..15`, and the caller's own id before the first read; `CALLER = "seller"` as in crm-sellers.t27.
- The dashboard: `ROUTE_CLIENT = "/crm/:clientId"`, `ROUTE_CLIENT_CHAT = "/crm/:clientId/chat"`, `PANELS = 6` (profile+SOUL+skills, plan, duets, touches+stage, media, messages), `READS_THROUGH = "/mcp"` so visibility stays in the tool gate, `NEW_READ_TOOLS = 3` (`crm_clients`, `crm_client_plan`, `crm_duet_runs`), `UNREACHABLE_SAYS = "unavailable"` -- never zeros, `DASHBOARD_SENDS_NOTHING = true`.
- Persisted runs: `TABLE_RUNS = "crm_duet_runs"`, `RUN_PERSISTED_AT = "start,turn,finish"`, `fn run_reads_as_lost(running, minutes)` after `RUN_LOST_AFTER_MIN = 30`, `RUN_LOST_IS_STORED = false` (derived on read).
- Checked: `public/t27/t27_compiler.wasm` through trinity `agents-from-specs.mjs` reports `typecheckOk: true, errors: 0, discarded: 0, hirOk: true`; ASCII only. Host PRs in gHashTag/999-multibots-telegraf are listed in the PR description of this spec.
- Not claimed: `DASHBOARD_SEEN_LIVE = false`, `RUN_SURVIVED_REDEPLOY = false` -- both are shown by the host after deploy, not by this file.
- `tools/published_figures.py`: the test-blocks pin follows the corpus, 12739 -> 12744 (five in this spec); no `pub const OP_*` added.
