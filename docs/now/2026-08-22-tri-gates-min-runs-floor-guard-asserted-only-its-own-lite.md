# NOW -- tri gates --min-runs floor guard asserted only its own literals (2026-08-22)

## tri gates --min-runs floor guard asserted only its own literals (Closes #2369)

- `gates.rs` shipped a test named `the_floor_is_what_makes_a_zero_meaningful` whose entire body was `let below = 2u64; let at = 50u64; assert!(below < 50, ...); assert!(at >= 50, ...)`. It restated two literals to each other. `min_runs` occurs six times in the crate — gates.rs 31, 37, 46, 76, 94, 105 — and the test named none of them. The guard was **absent, not weak**: copied into a file with no production logic and no `use` of the crate at all, it compiled with `rustc --test` and passed.
- Measured on master `6dcefd044c276c0453f7ce2c7206718d480052e5`: setting `default_value_t = 50` to `0` at line 30 left **173 passed; 0 failed**, and `cargo test -p tri gates` reported the floor test itself `ok`. With the floor at 0, the skip at line 94 (`total < min_runs`) never fires, so a day-old workflow with 2 runs and no success is printed as a gate that "has never succeeded" — this command becoming the thing its own module docs say it exists to find.
- Repair follows the one that fixed `cibase.rs` in #2361: the predicate is lifted out of `dead`'s network loop into `too_few_runs_to_judge(total, min_runs)`, and the test now reads the floor `tri gates dead` actually ships with by parsing it back out of clap, instead of repeating `50` in its own body. Both assertions run the shipped value through the shipped predicate.
- Same mutant against the repaired guard now fails: `--min-runs defaults to 0, so a workflow with 2 lifetime runs and no success would be reported as a dead gate`. The decisive extraction no longer compiles — 2 × `error[E0425]: cannot find function too_few_runs_to_judge in this scope`.
- Severity **latent, not live**: the shipped default is 50 and no caller overrides it, so no run of `tri gates dead` has been wrong. What was broken is the guard, which would not have reported it if it had been.
