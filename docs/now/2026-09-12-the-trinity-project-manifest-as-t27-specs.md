# NOW -- The Trinity project manifest as .t27 specs (2026-09-12)

## S01 of gHashTag/trinity#988: the canonical project manifest, ownership and coverage baseline (Closes #3563)

- `specs/trinity/` registers the missing namespace in `specs/OWNERS.md` and holds the manifest: `project.t27` (module `trinity_project`, the consumer `gHashTag/trinity` pinned at `976df517`, the seven profiles with `headless` as the initial one, the eight dispositions, the five evidence tags of `docs/system/project.md`, the dialect and build counts, the four dependency pins, the twelve work packages S01..S12 with their issues) and fifty capability cards `capabilities/<id>.t27` (module `trinity_capability_<id>`, `KIND = "capability"`), each with owner, disposition, profile, canonical spec and dialect, implementation and generated paths, backend, the `build.zig` targets it owns, its acceptance command, its evidence status and source, and its work package.
- `tools/trinity_manifest.py` derives the inventory from a clean pinned checkout (`inventory --trinity-root`; a modified tracked file is refused) and holds the cards to it (`check`): 51 executables, 6 libraries, 73 tests and 68 steps of `build.zig` are each owned by exactly one card; 46 targets are installed by `zig build -Dci=true` and belong to headless cards, 5 are guarded by `!ci_mode` and do not; every `trinity:` path is tracked; the mirror `apps/website/public/t27/files/` (1044 `.t27`) is never canonical; `DIALECT` follows the extension of `CANONICAL_SPEC`; a backend is claimed only by executable, adapter and research cards; no spec has two owners, no two cards one ID; `measured` names its command and its source; every work package has a card. `--self-check` plants thirteen defects (duplicate ownership, absent path, ambiguous dialect, dirty source, unsupported backend, unassigned target, unknown target, pin mismatch, count mismatch, unsupported evidence, mirrored canonical, profile mismatch, uncovered package) and requires each to be reported.
- The inventory (`conformance/trinity/inventory.json`) and the derived report (`conformance/trinity/report.json`) are committed with the revision they were taken from. Measured at that revision: 31 `.t27` outside the mirror, 764 `.tri`, 1981 `.vibee`, 2830 `.zig` of which 749 are reachable from the 173 files `build.zig` names and 2081 are not; 29 registry commands; a catalog snapshot of 856 specs from 8 repositories, recorded as a snapshot and not as coverage.
- Evidence is what a public CI log measured, nothing more: `Build & Test` (ubuntu-latest, zig 0.15.2) succeeded for `zig build -Dci=true` at `79ffb034`, one data commit before the pin; the test step is piped through `tee` there and its exit code is not measured (gHashTag/trinity#616), so no card claims a passing test. Guarded GUI targets are `not-claimed`; the HSLM CLI is `declared` as the epic records it; `trinity:t27/` is `deprecated` as a second copy of this repository's compiler and numeric specs; the `.tri` and `.vibee` corpora and the ten `.t27` programs under `src/tri27` are `declared` until S02 states which compiler proves what.
- `specs/catalog/discovery.t27` (module `catalog_discovery`) is the canonical copy of the world-scan contract that gHashTag/trinity#990 vendored under the site's mirror path, registered under `catalog/` in `specs/OWNERS.md`; the card `catalog.world-scan` cites it.
- Seals for the 52 files were produced by `t27c seal --save` at `03f0faf6` (this repository's `master`); `t27c typecheck` reports 0 errors on each. `tools/published_figures.py`: the pinned `test blocks` population follows the corpus by the 60 test blocks of these files, with the movement written next to the pin.
- Not claimed: that any test of the consumer passes, that the headless profile reproduces from a clean clone (S03, S12), or that a card's contract exists where `CANONICAL_SPEC` is empty (S04..S11 author them). A local macOS build was attempted and is not recorded, because the local zig could not link a hello-world against the host SDK.
