# NOW -- A workflow for the moment an aggregate moves (2026-08-29)

## A workflow for the moment an aggregate moves (Refs #2754)

- column-forensics: two per-item dumps in, named rows out, each cause quoted from the tool the gates run rather than a reimplementation
- verdict phase defaults to regression:false -- a number that falls when a silent drop is fixed was measuring the drop
- refuses to derive the tables itself: recomputing with a different invocation is how a second disagreeing number is born
