# NOW -- "silicon-proven" read as a die that was never made (2026-09-24)

## GF-T results on the AX7203 now say FPGA-proven (Refs #2180)

- Thirteen lines in six files called GF-T results "silicon-proven", "ON SILICON" or "proven on live silicon": the gft_dot2 MAC (3/3), the GF-T16 format, and the 14 bitstreams of `docs/GFT_WHITEPAPER.md` section 3. A fourteenth line, in a seventh file, is the `on silicon` row of the GF-T / GA-T table in `docs/NUMERIC_FORMATS_SSOT.md`. All of it was measured on Artix-7 boards (AX7203, XC7A200T). No die was ever fabricated (`README.md:36-37` and `README.md:165-166`: TTSKY26a / TTSKY26b withdrawn and refunded). `STATUS.md:33` reserves SILICON for a received die, and `STATUS.md:112` forbids a SILICON claim anywhere in t27.
- Reworded to "FPGA-proven (AX7203)" in `bootstrap/tests/gft_dot2.rs`, `gft_dot4.rs`, `gft_dot8.rs`, `gft_layer2.rs` (comments, and one assert failure message that no test compares), the section 3 heading of `docs/GFT_WHITEPAPER.md`, and T86 / T88 of `docs/theory/IGLA-FORMAL-RESULTS.md`. The SSOT table row now reads `on FPGA`. What was measured, and on which board, is unchanged.
- `docs/theory/IGLA-FORMAL-RESULTS.md` already defines silicon as the die of the FPGA on the board an entry names (reading note, lines 6-10). T86 / T88 change only because "silicon-proven" is the phrase that gets quoted outside the ledger; the rest of the ledger stays under that note.
- Left alone on purpose: the reference RTL the four tests embed verbatim. Its `gft_dot2` module is byte-identical to `gHashTag/tri-net` `fpga/gft/gft_dot2.v` (the tests label it `trinity-fpga/build/gft_dot2`), so its "silicon-proven gft_mul" comment is fixed in tri-net. Also left: the six sealed specs `gft_dot2`, `gft_dot4`, `gft_dot8`, `gft_layer2`, `gft_mul_rne`, `tnf17`, whose seals hash the spec text, so a comment edit there is a reseal, not a wording fix; and the wider "on silicon" = FPGA usage (`README.md:43`, `docs/SILICON_TRAINING_METHODOLOGY.md`, the rest of the IGLA ledger), for a later pass.
- Test function names such as `spec_first_gft_mac_matches_silicon_proven_rtl` are unchanged, to keep this change comment-only; renaming them is a separate change.
