# NOW -- The quiet mark was zero for five, and three of them were writers (2026-08-29)

## The quiet mark was zero for five, and three of them were writers (Refs #2804)

- opened all five quiet? sites: two are save_* functions whose job is to WRITE the file the tool called missing, three report the absence with println! and return
- the mark's report vocabulary was a GATE's -- bail, FAIL, exit(1 -- while these are CLI commands that talk to a person
- write sites filtered and println counted: 21 -> 18 findings, quiet? 5 -> 2, and the two that remain are loaders returning an empty default, which is correct
- kept the mark with its 0-for-5 rate stated in the output, rather than deleting it or quietly widening it
