# NOW -- The column that could not see the repair (2026-08-31)

## The column that could not see the repair (Closes #2966)

- t27c corpus measured Zig with build-obj, which never Sema-analyses a body nothing references, so yesterday's +32 moved it by 0 and no ratchet could ever redden for that class
- second row measured with zig test --test-no-exec, carried into --json and into --per-spec, whose Zig field is three digits now
- the gap is 282 against 165: 117 files count as Zig-accepted while carrying something the compiler would reject the moment it looked
- invariant bodies <= build <= gen is pinned and measured before asserting; -femit-bin gets a real path because /dev/null fails on every input and would read as a catastrophic regression
