# NOW -- The notification ids went backwards (2026-08-31)

## The notification ids went backwards

- three republish notifications in one session named versions in DECREASING order; the third named one older than the second, both older than the live version
- so the stream replays superseded versions rather than announcing new ones, and the check needs an ordering comparison rather than judgement
- the version prefix is a unix timestamp: 1788114931 < 1788115237 settles it without opening the file
- section 410 paid for itself on its first use -- two commands instead of the re-read the first of the three cost
