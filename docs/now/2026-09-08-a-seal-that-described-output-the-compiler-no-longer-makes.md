# NOW -- A seal that described output the compiler no longer makes (2026-09-08)

## A seal that described output the compiler no longer makes (Closes #3415)

- A seal records four `gen_hash_*` fields -- the hashes of what each backend emitted -- and **nothing checked them**. Measured on master: **109 stale `gen_hash_rust`, 7 stale `gen_hash_zig`, 0 C, 0 Verilog, and 0 stale `spec_hash`**. All 116 were left by five merged backend repairs (#3401, #3403, #3405, #3407, #3411), **every one of them mine**. None touched a spec, so `spec_hash` stayed correct and every coverage and staleness check stayed green while a sixth of the Rust seals described output the compiler no longer produces.
- What the existing checks cover, and do not: `check_seal_coverage.py` asks whether a seal describes a spec that exists unchanged **at source**; `Seal Staleness Warning` is about the NMSE manifest and `FROZEN_HASH`, unrelated to `.trinity/seals`, and exits 0 by design. `t27c seal --verify` answers the missing question exactly -- exit 1 with a precise MISMATCH line, exit 0 on a current seal, both verified as controls -- and **nothing called it across the corpus**.
- The refresh took three attempts and each failure was informative. `--save` fixed 116 -> 58, not 0, because it writes to `.trinity/seals/<module>.json`, one name, and **1313 seals cover 728 distinct specs -- 547 specs carry more than one seal file**. The remaining 58 duplicates were rewritten in place; `sealed_at` was left alone, since rewriting a hash the tool itself computed is not a fresh certification event. Now 0.
- Some duplicate seals are not named after a module at all: `specs/tri/utils/logger.t27` has `"[]const u8".json`, `utils_"[]const u8".json` and `utils_TriLogger.json`. **A seal named after a type string.** Which name is canonical is a decision and is filed, not guessed.
- `gen_hash=none` is a different debt and is counted apart: **169 seals** record it for at least one backend, and `--save` refuses to overwrite them ("4 of 4 backends rejected it"). Conflating the two is how 116 stayed invisible. That 169 independently reproduces the figure an earlier audit reported for `none == none`.
- `tools/check_seal_currency.py` now asks the question, with a self-check that plants a wrong hash on a scratch tree and requires exactly it to be reported -- because a zero from a check that cannot see is indistinguishable from a healthy zero. Exit 2 when `t27c` is absent: a check that could not run has not passed.
