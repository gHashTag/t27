# NOW -- Nothing had ever tried to refute an equivalence claim (2026-08-24)

## Nothing had ever tried to refute an equivalence claim (Refs #2161)

- Refs #2161. `# mutant-equivalent: <why>` marks a survivor as unkillable by construction, and `tri gates mutate` printed it as "claims equivalent: ...". The word `claims` was carrying real weight -- NOTHING had ever checked one, and six sit in tools/
- The run best placed to notice a stale claim is the mutation run itself: it already built the mutant and already knows the verdict. The missing step was one comparison. A claimed line whose mutant DIES is now reported as contradicted. Measured against all six that already existed: none contradicted -- worth exactly as much as the run that could have refuted them and did not
- Counts, not membership: a line can hold two mutable sites (`if a < 1 or b < 1:`), and a claimed line with one of two dead has been contradicted once. Keying on "did the line leave the survivor list" would call that claim intact, and a half-true claim is the hardest to catch by eye
- WRITING ABOUT THE MARKER CREATED A CLAIM. The extractor matched the marker anywhere inside any comment, so prose describing the mechanism registered as a claim bound to whatever code line followed. Caught because the count printed 2 where I had written 1. A false positive in the extractor becomes a FALSE REFUTATION in the checker -- the new check gave the old bug a way to lie. The marker must now OPEN the comment; every real claim already did
- A scanner that matches a marker anywhere cannot tell a use from a mention, and documentation is where mentions live. Any tool that greps the tree for its own vocabulary will eventually read its own documentation as data -- and the count is the cheapest place to notice, which argues for printing counts nobody asked for
- Two commits destroyed with git in one session, both recovered: `git checkout master -- <file>` (wanted to branch, got master restored over my edit) and `git checkout -b tmp` + commit + `branch -D tmp` (checkout -b carried unrelated uncommitted work onto the throwaway branch). Both are a command borrowed from a neighbouring recipe where the verb matches and the object does not
