# NOW — the mutation line closed (2026-08-23)

All twelve boundary survivors are gone. **111 mutants across 13 gates — 36 silent, 21 loud, 33 invert, 21 boundary — two survivors, both proven equivalences that say so in the row.**

Six closed in one pass, and they rhymed. **Every one needed an input no case had a reason to build**, and in each the missing input was the degenerate one:

- a file with **zero** cases — the prose census counts `> 0`, and nothing planted an empty file
- a **new** file with zero cases — announced as prose-only under `>=`, on a tree with nothing wrong
- a file **already empty when the ledger was written** — an emptying is a *transition*, and under `>=` a file that never made one is announced as EMPTIED on every run forever
- a ledger line with **no separator** — under `>=` the index raises and the gate dies on a healthy tree
- **exactly five** repaired seals, and **exactly `MIN_ROWS`** in the catalog floor

Nothing exotic. A control author plants the fault they are testing for, which is by construction a *non-degenerate* example: a file with the wrong contents, not a file with no contents. The boundary operator's whole yield is the empty case, the equal case, the one-off-the-edge case — **the inputs that are nobody's example.**

One planted tree closed two boundaries where they were the same question on either side of a ledger: the prose count appears in the record header and again in the verify census.

And "cosmetic" was closed rather than declared, twice. Both `"(+N more)"` guards change a message and nothing else, and both got a case — a declared exception costs every future reader more than a case costs to write once.
