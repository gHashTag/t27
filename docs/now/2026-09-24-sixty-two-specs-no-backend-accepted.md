# NOW -- 62 specs that no backend accepted now compile (2026-09-24)

## Parse-level repairs for the Queen board's Broken list (Closes #4793)

- The Queen board (t27.ai/queen, Specs) showed Broken 218, from a manifest built at `6e13d65c` on 2026-09-21. Re-measured on master `a103752` with the explorer wasm and `t27c` built from that commit: 7 already generate, about 81 are not modules (Markdown, TRI-27 assembly, the `spec X {}` dialect; #4541 gives them their own axis), 24 are fixtures broken on purpose, 54 live in other repositories, and 62 are real sources in this repository. This entry is about the 62.
- All 62 failed in the parser, before any backend: Zig idioms (`std.ArrayList`, `for (xs) |x|`, `try std.testing.expect`, `[_]T{...}`), Rust idioms (`let mut`, `Vec::new`, `format!`, slices), the older t27 style (`import x;`, `module X { ... }` blocks, `given ... when ...` tests, `};` after a struct) and typos. Five of them are the regressions of #4488.
- The syntax is translated to t27 and the meaning kept: declarations, tests, invariants, benches, assertions and constants stay. Where t27 has no direct form, the spec says so in a comment. Each batch of files was reviewed a second time against the original's declaration and assertion counts.
- All 62 now pass the seven explorer targets (c, js, rust, ts, verilog, verilog_hir, zig) and `t27c gen` / `gen-c` / `gen-rust` / `gen-verilog`. `tools/ci/check_specs_still_parse.py` reports them as repaired and no spec that parsed stopped parsing.
- Baselines moved with them, in this change: 61 entries left `tools/specs_generate_baseline.txt` (54 of the 62 were listed there; the other 7 were fixed earlier on master and still listed); `tools/duplicate_declarations_baseline.txt` drops benchmark (13), adamw (11) and file/operations (1) to zero. Seals were refreshed with `t27c seal --save` for every changed spec (117 files updated, 6 new): seal currency goes from 542 current / 170 gen_hash=none on master to 661 / 66.
- Not in this change, and still red on master: 11 specs under `specs/port/tools/` that do not generate and are not baselined (bee ports), and `specs/igla/race/cordic.t27`, whose assertionless count fell 144 -> 0 without a re-bless. `specs/igla/coder/benchmark.t27` still carries 18 quantified invariants over unbounded domains (open decision #2774), so it shows as a warning, not an error.
