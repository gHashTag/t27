# NOW -- the prototype gave the right number because one of its alternatives was dead (2026-09-05)

## the prototype gave the right number because one of its alternatives was dead (Refs #3195)

- tri skill lost now flags an entry whose body names none of its own identifiers and does name one another entry owns -- the shape that had SW-conformance gf48 carrying 39 lines of Wave Loop 434. Measured: 1 on the damaged base, 0 after the repair, 0 in SKILL.md across 524 sections
- the naive question is useless: 58 of 63 NOW.md entries with a wave number mention some other wave, because an entry points at the next one. Naming none of its OWN is the discriminator
- the python sketch reported 1 and the Rust port reported 49; the difference was one dead alternative. \\b# requires a word character before the hash, so the issue-number branch never matched anything, and the sketch compared waves and formats only -- the right population, by accident. Issue numbers are now excluded deliberately with the measurement beside the pattern
- both test fixtures were wrong and both tests were right to fail: XADC_LIVE_W434 does not match because the underscore is a word character, and an entry naming its neighbours but not itself is exactly the shape that should flag
