# NOW -- The swarm ran out of fuel, and 1427 functions have no test (2026-09-20)

## A second feeder, from the compiler's own coverage answer (Closes #4305)

- Measured on the live deployment at 10:16Z with ten workers running: one tick skipped **697** candidates -- 554 issues with no `## Boundary`, 51 claimed, 43 completed, 25 incompleteSpec, 18 notFirst, 6 fileConflict. Of **673 open issues only 119 carry a boundary**, and an issue without one can never be dispatched. The swarm was not slow; it had nothing it could take.
- The one feeder that made dispatchable issues had drained its pool the same morning: `28 files hold 146 empty bodies; 100 files covered by open issues; 0 uncovered`.
- The pool nobody was reading is `t27c coverage`, which is the compiler's own answer rather than a grep. Over the corpus: **887 specs answer, 278 have at least one untested function, 1427 functions have none**. 236 of those specs have between one and eight -- one issue each, 692 functions of work, and an exact criterion a grep can check (`Untested:  0`). Dry run against master: **221 uncovered specs**, which at ten workers is a long runway.
- Eight is the ceiling on purpose. "`Untested:` went down" is not a string a grep can match; `Untested:  0` is. The 42 specs above the line are printed in the log rather than silently dropped, and they wait for a change that can chunk them without writing a criterion nobody can check.
- `Untested: 0` is also what an empty file prints, so the function count is a criterion too: `grep -cE '^[[:space:]]*(pub[[:space:]]+)?fn[[:space:]]'` must still print what it prints today. Deleting a function is the cheapest way to have no untested functions.
- One implementation, not two: the new feeder imports the bee-shell, the claims check, the open-boundary scan and the idle gate from `feed_empty_bodies.py`. A second copy of that machinery is the duplication this repository gates against in its own specs.
- **`t27c lint` was found wrong on the way past.** It warns `has no test or invariant` about `bit_to_trit_pair` in `specs/base/ternary_encoding.t27`, which `test bit_to_trit_pair_zero` calls on line 249. Over 60 specs: 677 lint warnings against 190 untested functions from `coverage`, disagreeing on 59 of the 60. `docs/BEE_TOOLBELT.md` now says so in place of recommending it, and the defect is filed as #4306. No criterion is written on `lint`.
- The census moved because this adds a workflow: `quiet` 57 -> 58 workflow files and 135 -> 137 "named a path but not quiet"; `shell` 57 -> 58 files, 78 -> 79 jobs, 267 -> 269 run-steps, 246 -> 248 steps run with `bash -eo pipefail`.
- What this does NOT establish: that a bee can actually write a good test for these functions. It establishes that 692 of them have none, that the number came from the compiler, and that the criterion is exact.
