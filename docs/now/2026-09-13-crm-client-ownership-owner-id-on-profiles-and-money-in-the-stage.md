# NOW -- CRM client ownership: owner_id on profiles and money in the stage (2026-09-13)

## CRM client ownership: who owns a profile, and money before touches (Closes #3608)

- `specs/automation/crm-client-ownership.t27` (module `automation::crm_client_ownership`, `KIND = "automation"`, `EXTENDS = "crm-client-workspace"`) closes the two gaps #3604 left open and that were seen live the same day: `crm_client_profiles` had `PRIMARY KEY (telegram_id)` and no owner column -- one profile per person on the whole installation; `crm_clients` and `crm_history` called `stageOf` with `paid = false` hard-coded, so a person with a COMPLETED MONEY_INCOME row in `payments_v2` showed `written`/`new` instead of `client`/`winback` as `crm_leads` and `crm_summary` already showed.
- Ownership: `OWNER_COLUMN = "owner_id"` (text, nullable), `OWNER_IS = "seller-telegram-id"`, `UNIQUE_KEY = "owner_id,telegram_id"` with the legacy primary key dropped, `MIGRATION_IDEMPOTENT = true` (ADD COLUMN IF NOT EXISTS, DROP pkey only if present), `BACKFILL_FROM = "crm_people"` (the ingest already knows which owner met whom). `fn profile_visible(row)`: a seller sees their own rows and rows nobody has claimed (`UNOWNED_READABLE_BY_ANY_SELLER = true`); `SETUP_ALWAYS_WRITES_OWNER = true`, so the next `crm_client_setup` claims a legacy row. `READS_SCOPED_BY_OWNER = 4`: `crm_client_profile`, `crm_clients`, `clientContextBlock`, the duet `sellerBrief`. `fn conflicts(same_owner, same_client)`: two sellers may hold two profiles of one person.
- Money: `PAYMENTS_TABLE = "payments_v2"`, `PAYMENT_COUNTS_WHEN = "COMPLETED+MONEY_INCOME"`, `PAYMENTS_SCOPED_TO = "owner-bots"` (the same `visibleScope -> whoPaid` the other tools use; CRM audit 2026-09-12 P1 #2), `STAGE_TOOLS_READING_MONEY = 2` (`crm_clients`, `crm_history`), `MONEY_BEFORE_TOUCHES = true`, `fn stage_code(paid, quiet_days, touch_code)` with `WINBACK_AFTER_QUIET_DAYS = 60`; rows carry `paid: bool` (`PAID_FLAG_IN_ROWS = true`). `MONEY_UNREACHABLE_FALLS_BACK_TO_TOUCHES = true` with `paid_known: false` -- a dead Supabase costs the money column, not the answer.
- `specs/automation/crm-client-workspace.t27`: `DASHBOARD_SEEN_LIVE` flips to `true` on the evidence of 2026-09-13 ~20:40 +07 -- the owner opened `/crm`, `/crm/435572800` and its client thread on app.t27.ai after the #2384 deploy, all six panels with data, and a question in the client thread left the `self` thread at the same length. `RUN_SURVIVED_REDEPLOY` stays `false`.
- Checked: `public/t27/t27_compiler.wasm` through trinity `agents-from-specs.mjs` reports `typecheckOk: true, errors: 0, discarded: 0, hirOk: true` for both specs; ASCII only. Host PR in gHashTag/999-multibots-telegraf is listed in the PR description of this spec.
- Not claimed: `MIGRATION_RAN_ON_PROD = false`, `PAYING_CLIENT_SEEN_LIVE = false` -- both are shown by the host after deploy, not by this file.
- `tools/published_figures.py`: the test-blocks pin follows the corpus, 12744 -> 12749 (five in the new spec; the workspace spec keeps its five); no `pub const OP_*` added.
