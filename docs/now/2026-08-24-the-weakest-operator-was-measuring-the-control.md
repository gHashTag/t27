# NOW -- The weakest operator was measuring the control (2026-08-24)

## The weakest operator was measuring the control (Refs #2161)

- Refs #2161. A five-way fan-out audit raised 19 findings; 11 survived adversarial refutation and 8 were killed. The sharpest: `--assert` scores 2 of 34 not because 32 assertions are weak but because `self_check()` plants exactly TWO faults, and Python stops at the first failing assert -- so each plant can surface exactly one assertion. Two plants, two kills, ceiling reached
- Measured one variable at a time: plant 1 falsifies both line 632 and line 660, the program dies at 632, and neutering 660 alone is invisible; neuter 632 and the same plant dies at 660. Kill set is exactly {632, 668} -- precisely the two lines missing from the survivor list. All 34 sites DO execute on a clean run, so no survivor is unreachable. Iterating on the operator cannot move the number; only adding a spawned() case per assertion can
- MY PUBLISHED EXPLANATION WAS WRONG FOR AT LEAST 26 OF THE 32. The earlier NOW entry accounts for the six held-out-threshold assertions and offers a boundary-operator rationale for the whole column. A partial explanation presented as a complete one is the most expensive kind of wrong, because it closes the question -- three iterations then deferred the column as understood rather than unexplained
- The control second plant did not test what it says. `input [31:0] x0i` spelled out occurs exactly once in the file -- at the assertion checking for it; the emitter builds ports with an f-string. So the plant rewrote THE ASSERTION OWN EXPECTED STRING and the emitted Verilog was untouched. It now targets the emitter format string, whose first occurrence is above the control. T124 fixed the case where the needle first occurrence was the control own source; this is the next one along, where the needle only occurrence is the subject CHECK rather than the subject
- Three scanner defects fixed in cli/tri/src/gates.rs, each with a test that fails when its fix is removed. The three line-oriented site finders had no string state at all, so a flush-left line inside a control docstring cleared in_control and handed the operators the control own assert -- a site nobody can kill. Two files differing by four spaces of indentation on ONE docstring line gave 3 assert sites versus 0. And word-wrapped prose beginning a line with the word assert was scored as an assertion
- Also in assert_sites: a backslash advanced one byte instead of two, so `assert s == "a\\",b", "msg"` produced a mutant that does not PARSE -- python exits 1 and the site is scored KILLED, a false green in the one column this command exists to make trustworthy. The byte-oriented sibling had `i += 2` all along
- Three of my own published numbers are stale and are corrected here. The boundary denominator 31 came from the scanner bug: the file has 62 sites, killed count unchanged at 14, so every k/31 in the skill now carries an erratum. `tri pointers` docstring froze "193 mentions and 16 unresolved" and the tool printed 191 and 12 within a day -- running the file refuted its own documentation. And --boundary help text advertised "the worst kill rate of the five (26 of 77)", whose denominator the same fix doubled. Frozen counts are removed rather than updated: a number in help text has no way to learn it went stale
