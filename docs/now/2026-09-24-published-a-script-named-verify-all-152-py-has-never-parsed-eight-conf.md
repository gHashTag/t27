# NOW -- A script named verify_all_152.py has never parsed: eight conflict markers, present since the commit that added it (published 2026-09-24)

## A bee's work on #2873, published from `queen-2873` (Closes #2873)

- The branch changes 2 file(s): `scripts/verify_all_152.py`, `tools/conflict_markers_baseline.txt`.
- `git diff --stat origin/master...queen-2873` reads: 2 files changed, 1 insertion(+), 122 deletions(-)
- This entry is written by the publisher, not by the bee. A pull request must
  add exactly one `docs/now/` entry and a bee has no way to know that: its brief
  names a boundary file and acceptance criteria, and `docs/now/` is neither.
- What this entry does NOT establish: that the work is correct. The gates on the
  pull request judge that, and they are the same gates every other change meets.
