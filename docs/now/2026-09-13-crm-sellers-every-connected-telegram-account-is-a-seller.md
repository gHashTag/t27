# NOW -- CRM sellers: every connected Telegram account is a seller (2026-09-13)

## CRM sellers: every connected Telegram account is a seller (Closes #3598)

- `specs/automation/crm-sellers.t27` (module `automation::crm_sellers`, `KIND = "automation"`) states WHO the CRM of 999-multibots-telegraf works for: `SELLER_IS = "connected-account"` -- a row in `tg_sessions` for the caller's verified id (`SESSION_KEY = "telegram_id"`, never an argument). The platform owner is always admitted (`OWNER_ALWAYS_ADMITTED = true`; their session is the row or `TELEGRAM_SESSION_STRING`). `fn admits(s) = s.has_session || s.is_owner`.
- Measured 2026-09-13 on the render's Postgres (Railway Postgres-NFrq): `SELLERS_MEASURED = 2` -- 144022504 (@t27_dev, owner, connected 2026-09-07) and 435572800 (@playom, 2026-09-09), `SESSION_LEN_MEASURED = 369` characters each. The data tables were already scoped by the caller (`DATA_SCOPED_BY_CALLER = true`); only the gate `requireOwner`, the playbook and the sweep still named one person.
- The gate `GATE = "requireSeller"` reads the session table (`GATE_READS_SESSION = true`) and its refusal names the fix (`REFUSAL_NAMES_FIX = true`: connect the account in the app). `PLAYBOOK_FOR = "every-seller"`; `SELF_OFFER_CHECK = "caller"` -- an offer to your own id is refused, the owner's id is not special to a second seller.
- `LIST_TOOL = "crm_sellers"`, callable by the owner only (`LIST_TOOL_CALLER = "owner"`), optional `probe` asks checkAuthorization per session; `LIST_TOOL_RETURNS_SESSION = false` -- never the string, never a prefix.
- The proactive sweep: `SWEEP_PER_SELLER = true`, `SWEEP_CONCURRENCY = 1` (one MTProto connection at a time on the render); the list comes from `CRM_PROACTIVE_OWNERS`, else `crm_sellers`, else the one id `CRM_PROACTIVE_OWNER` as before; `PLAN_PER_SELLER = true`.
- Checked: `public/t27/t27_compiler.wasm` through trinity `agents-from-specs.mjs` reports `typecheckOk: true, errors: 0, discarded: 0, hirOk: true`; ASCII only. Host PR gHashTag/999-multibots-telegraf#2359: render `vitest run` 163 files / 1568 passed / 1 skipped; bot services 43 files / 327 passed; `tsc --noEmit` clean -- green locally (GitHub Actions in that repo are billing-blocked).
- Not claimed: `SESSIONS_PROBED = false` -- existence and length of the two sessions were measured, liveness was not; `CARRIER_BOT_STARTED_BY_EVERY_SELLER = false` -- a seller who never opened the carrier bot gets no card or plan.
- `tools/published_figures.py`: the test-blocks pin follows the corpus, 12724 -> 12729 (five in this spec); no `pub const OP_*` added; `--check` and `--self-check` pass on this checkout.
