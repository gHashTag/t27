# NOW -- Master was red, and this loop made it so (2026-09-20)

## Two of the three red checks were mine (Closes #4303)

- **The census moved six times and nobody said so.** `tools/census/quiet.txt` pinned `workflow files read 51`; the tree holds 57. The six are spec-parse-ratchet (#4277), auto-merge-ready-prs (#4279), queen-feed-empty-bodies (#4281), pusher (#4283), dupe-ratchet (#4291) and queen-doctor (#4300) -- every one of them merged by this loop, and `cli-tri` has been red since 2026-09-17 as a result. Re-blessed here: `quiet` 51 -> 57 workflow files and 123 -> 135 "named a path but not quiet"; `shell` 51 -> 57 files, 72 -> 78 jobs, 249 -> 267 run-steps, 228 -> 246 steps the runner runs with `bash -eo pipefail`. Nothing about the gates changed; the population grew.
- **The path gate failed on a path quoted inside the warning against it.** `tools/queen/feed_defects.py` carries a comment explaining that an absolute `/Users/<name>/t27/target/release/t27c` once went into 137 open issues and made them unrunnable -- and spells the path out to say so. So does its coordination entry. Both now say `.../t27/target/release/t27c`, which carries the same meaning and is not a developer's home.
- **The same gate failed in every worktree, for a path no commit contains.** It excludes the `.git` DIRECTORY; a worktree's `.git` is a one-line FILE holding `gitdir: /Users/...`. `--exclude=.git` is added beside `--exclude-dir=.git`. This matters beyond convenience: the reviewer measures in a detached worktree, and every bee works in one.
- **The third red check is not from this loop, and is not fixed here.** `coverage` reports 544 stale seals. The last 60 runs of that workflow on `master` hold zero successes, reaching back past 2026-09-18. Re-sealing 544 specs records what the compiler now produces; that is a decision about the seal ledger, not a repair, and it needs its own change.
- What this does NOT establish: that the newly blessed numbers are RIGHT. `tri census pin --gate` says three readings are unchanged since the pin. `tri census audit` is the command that asks whether they are correct, and it was not run here.
