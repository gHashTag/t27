# NOW -- boundary drafts for the code issues too, and none for the citations (2026-09-23)

## 167 of the 553 unreachable issues now carry a draft (Closes #4638)

- `--specs-only` covered the 103 issues naming a `.t27`. 64 more name code but no spec and are just as draftable; the run had no way to include them without also including the third group.
- That third group -- 71 issues -- names **only** a path under `docs/`, and those drafts are the ones most likely to be wrong: a document is quoted far more often than it is edited. #4122 and #4115 both name `docs/audit/inngest-improvement-plan-2026-09-13.md`, the thing they cite rather than the thing they change.
- `--skip-citation-only` drops an issue whose *every* path is under `docs/`. One naming a document **and** a code path is still drafted, because there the document is context and the code is the work.
- Run over the board: **167 issues now carry a boundary draft**, and a re-run posts nothing. The remaining 315 name no path at all -- there is nothing to draft from, and a guess there would be the wrong boundary the tool exists to avoid.
