# NOW -- Two of four required contexts have no master reading, and one cannot have any; the README CI badge points at a workflow that does not exist on master (published 2026-09-24)

## A bee's work on #2961, published from `queen-2961` (Closes #2961)

- The branch changes 1 file(s): `tools/check_now_entry_shape.py`.
- `git diff --stat origin/master...queen-2961` reads: 1 file changed, 1 insertion(+), 1 deletion(-)
- This entry is written by the publisher, not by the bee. A pull request must
  add exactly one `docs/now/` entry and a bee has no way to know that: its brief
  names a boundary file and acceptance criteria, and `docs/now/` is neither.
- What this entry does NOT establish: that the work is correct. The gates on the
  pull request judge that, and they are the same gates every other change meets.
