# NOW -- Leela agent link: linking a connected seller to her editorial agent (2026-09-13)

## Leela agent link: linking a connected seller to her editorial agent (Closes #3600)

- `specs/automation/leela-agent-link.t27` (module `automation::leela_agent_link`, `KIND = "automation"`) states HOW the Leela bot (`BOT = "leela_chakra_ai_bot"`, repo gHashTag/leela) grants its content-administrator role to a connected seller: `CLAIMANT = "435572800"` (@playom) sends `CLAIM_CMD = "/agent_claim"` from her own session; `APPROVER = "144022504"` (the trusted owner) sends `LIST_CMD = "/agent_claims"` and `APPROVE_CMD = "/agent_approve"` + the `CLAIM_ID_HEX = 32` id; she sends `STATUS_CMD = "/agent"`; `STEPS = 4`. A claim lives `CLAIM_TTL_HOURS = 24`, the role `GRANT_DAYS = 30`. `fn approves(c)` mirrors `editorial-store.ts approve()`: pending, not expired, approver trusted, claimant not an owner, claimant != approver.
- Measured 2026-09-13 on Railway (project Leela, service leela, production, 17 variables): none of `LEELA_AGENT_OWNERS`, `LEELA_STARS_OPERATORS`, `LEELA_AGENT_USERNAME`, `LEELA_999_AGENT_KEY` was set -- `OWNERS_BEFORE = 0`. With an empty owner list `approve()` returns false for every claim, so no account could hold the role and the editorial agent answered nobody: the reported "agent does not answer in the bot" was an empty owner list, not a model fault. `OWNERS_ENV = "LEELA_AGENT_OWNERS"` was set to 144022504 through the Railway UI the same day (`OWNERS_SET_ON = "2026-09-13"`, owner-confirmed) and the service redeployed.
- The hand on the render: `TOOL = "crm_agent_link"` in 999-multibots-telegraf (`apps/vibee-editor/render/src/agent/crm-agent-link-tool.ts`), `TOOL_CALLER = "owner"` (requireOwner: it sends from a second person's session), `BOTH_MUST_BE_SELLERS = true` (rows in `tg_sessions`, per crm-sellers.t27), `CLAIMANT_IS_ARGUMENT = true`, `REPLY_WAIT_MS = 4000`, `REPLIES_READ = 3`; the reply read is the incoming message with an id greater than the sent command's, not "the last message". `RETURNS_TRANSCRIPT = true`, `RETURNS_SESSION = false`, `CONFIRMED_BEFORE_RUN = true`.
- Free text on the agent side: `FREE_TEXT_FOR_ADMIN = true`, `FREE_TEXT_WHEN_SEATED = false` (at a game table words stay game reports), `FREE_TEXT_HISTORY_TURNS = 12` -- the gHashTag/leela change that lets the approved administrator talk to the editorial agent without slash commands.
- Checked: `public/t27/t27_compiler.wasm` through trinity `agents-from-specs.mjs` reports `typecheckOk: true, errors: 0, discarded: 0, hirOk: true`; ASCII only. Host render: `vitest run crm-agent-link.test.ts` 8 passed (four-step transcript through two mocked sessions, owner-only gate, unconnected claimant refused before Telegram is touched, self-claim refused, no-claim-id stop with hint); `tsc` shows only the pre-existing `vibee-atoms` react-types error, present on `main` before this change.
- Not claimed: `HANDSHAKE_RAN_LIVE = false` -- the tool's returned transcript is the evidence of a live link, not this spec; `BRIDGE_999_CONFIGURED = false` -- `LEELA_999_AGENT_KEY` is absent on the Leela service, so the 999 bridge inside the Leela bot is still off.
- `tools/published_figures.py`: the test-blocks pin follows the corpus, 12729 -> 12734 (five in this spec); `--check` and `--self-check` pass on this checkout.
