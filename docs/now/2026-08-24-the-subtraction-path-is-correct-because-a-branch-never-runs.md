# NOW -- The subtraction path is correct because a branch never runs (2026-08-24)

## The subtraction path is correct because a branch never runs (Refs #2161)

- Refs #2161. `_magsub` resolves a rounding tie THREE ways, and only two arms are live. Deleting `if sticky: mant += 1` changes 0 of 525_918 outputs -- a branch-level mutation tool calls that equivalent and moves on
- Mutating the PRODUCER instead: forcing the sticky detector to always fire changes 9851 outputs. So the arm is wired -- waking the SIGNAL wakes the arm. What never happens is the value: an exhaustive search over every (hm, lm, d) finds no input where `rem == half` and `sticky == 1` hold together. Live code behind a signal that never asserts, which looks identical to dead code from the branch
- A dead branch and a dead signal are different defects, and THE MUTATION THAT DISTINGUISHES THEM IS UPSTREAM OF THE BRANCH. Branch-level mutation asks only "does anything notice if this stops running?" -- both answer no. Only the producer mutation asks "does anything notice if this STARTS running?"
- The stakes: the arm is the ADDITION rule in the SUBTRACTION path. Discarded bits of the subtrahend make the true difference SMALLER, so a tie with lost bits sits strictly below half and must round DOWN. Measured with exact rationals -- the code diff is 13303794 against a true 13303793.734375. The sign flips with the operation and the copy did not. Reported as #2652, not changed: this mirrors board/bpseq.v and I have not measured the RTL
- `if d >= 26` -> `if d > 26` is an EQUIVALENT mutant, proven: at d == 26 the else branch computes la = ls >> 26 == 0 (ls is at most 1023 << 14, below 2**26) and sticky = 1, exactly what the taken branch assigns. 0 differences. An equivalent mutant PROVEN equivalent is closed work; one ASSUMED equivalent is an excuse, and three ticks re-examined survivors an earlier tick had already looked at because looking left no trace
- Three assertions, and the third caught none of the five mutants the other two were written against -- until it was tested against a sixth, DELETING the primary rounding branch, which the boundary operator cannot express. Redundancy is a claim about operator CLASSES, not about assertions
