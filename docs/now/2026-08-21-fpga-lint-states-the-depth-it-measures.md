# NOW -- fpga-lint states the depth it measures (2026-08-21)

## fpga-lint states the depth it measures (Closes #2326)

- Re-measured #2326 before touching anything, on the artifact it cites and on the newest one. `fpga-verilog` of run 32464326319: `read_verilog -sv -DSIMULATION` + `hierarchy -top` gives 32 pass / 0 fail, `iverilog -g2012 -DSIMULATION` gives 4 pass / 28 fail. Same split on run 32511534193, same four survivors (HwTypes, SPI_Master, ZeroDSP_TopLevel, ZeroDSP_UART). The gap is live, not historical.
- The axis is reachability, not depth. Yosys enters a function body only at a call site; counted over those 32 files, 485 functions are defined and 79 have one — 406 have none. Injecting a single call site into fifo.v (`assign __probe_aw = addr_width(32'd0);`) turns the exact CI command from silent rc=0 into `fifo_probe.v:174: ERROR: Non-constant expression in constant function`, on the same line 174 Icarus reports as `Unable to bind wire/reg/memory 'cfg_depth' in 'Fifo.addr_width.addr_width_body'`.
- Deepening yosys was measured, not assumed: `synth -top` over the same 32 is 31 pass / 1 fail, and the one failure is `zerodsp_top` for a missing submodule (one-file-at-a-time compilation), not a codegen defect. It would flag 1 of the 28 for the wrong reason and certify the other 27.
- So what the job RUNS is left exactly as it was. What it CLAIMS is corrected: display name `fpga-lint (read_verilog + hierarchy)` (job id unchanged — `fpga-report` depends on it), a summary that names its depth and states that function bodies are not elaborated, a `**Result:**` line qualified to parse+hierarchy, and the `fpga-report` row, which said `Lint (all 31 modules)` — wrong depth and wrong count, the artifact holds 32.
- Held by `scripts/ci/check_fpga_lint_claim_matches_depth.py`, wired into gate-topology alongside its own `--self-test`. It reads the yosys passes out of the step's `-p` script and requires the label, the summary and the report row to name them — deepen the command to `synth` and the claims must move with it. Only lines actually redirected to `$GITHUB_STEP_SUMMARY` count; a shell comment satisfies nothing.
- Not done, deliberately: no check was added that catches the 28. That turns the job red until #2325 lands and is a gate-policy decision for the repository owner, not a wording fix. Nothing was marked `continue-on-error`, no assertion relaxed; the step's failure path still exits 1, verified against a deliberately broken module. Refs #2325, #2241, #2242, #2239.
