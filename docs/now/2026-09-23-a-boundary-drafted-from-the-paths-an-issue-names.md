# NOW -- a boundary drafted from the paths an issue already names (2026-09-23)

## 87% of the backlog is unreachable for want of a section heading (Closes #4636)

- The tick read **648 candidates and chose none** today -- `refusal="nothing to choose"` -- while twelve lanes stood free. An issue with no `## Boundary` gives the Queen nothing to reserve, so no bee can own its files and it is skipped every round, forever. `needs_boundary.py` labelled the set: **553 open issues**, about 87% of the board. No amount of worker capacity moves that ceiling.
- Surveying all 553: 103 name a `.t27` spec, 64 name code but no spec, 71 name only a `docs/` citation, 315 name nothing. So **238 (43%) already say which files they are about** -- for those the boundary is not missing information, it is unwritten information.
- `propose_boundary.py` reads those paths and prints a draft. A `.t27` ranks first because the specs are what this project is; a path in the TITLE outranks one buried in the body; a path under `docs/` comes last and is marked, because a document is quoted far more often than it is edited -- #4122 and #4115 both name `docs/audit/inngest-improvement-plan-2026-09-13.md`, which is what they cite, not what they change.
- **It never edits an issue.** A wrong boundary is worse than a missing one: it reserves files the work does not own and blocks whatever really owns them until the claim expires. `--as-comment` posts the draft as a comment, and the Queen reads the body -- so a draft cannot make an issue dispatchable by accident. A person moves it across.
