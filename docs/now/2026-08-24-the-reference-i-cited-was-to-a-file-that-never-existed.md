# NOW -- The reference I cited was to a file that never existed (2026-08-24)

## The reference I cited was to a file that never existed (Refs #2161)

- Refs #2161. Closing the sticky finding I wrote, in a merged PR, that the arm was "reported, not changed -- this mirrors board/bpseq.v". The whole-history log for that path is EMPTY: it has never existed in this repository. I repeated a pointer out of the file own docstring without checking it resolved, then used it as the reason NOT to fix a rule I had just shown to be wrong. A wrong pointer is worse than a missing one -- a missing one sends a reader looking, a wrong one makes them stop
- The real counterpart is specs/ternary/gft_sadd.t27. It carries the rule LINE FOR LINE, 30 specs under specs/ternary/ carry a copy, the arm is dead there too (exhaustive over every (hm, lm, d)), and the spec barrel-shift normalisation agrees with the Python 12-step loop bit-for-bit over 2_193_075 points. So the generated Verilog, C and Rust all carry the same dormant wrong rule
- And the bit-exactness suite cannot see it BY CONSTRUCTION: verify_multitarget and verify_emit_bitexact prove the COMPILER faithful to the spec, never the spec correct. A defect written in the spec propagates to every target and the suite stays green -- that is the suite working, not failing. "ALL TARGETS BIT-EXACT" is a true sentence a reader takes for "the arithmetic is verified"
- One narrowness WAS fixable: the suite operands come from uniform(-4, 4), which is off 35..41 -- six of the format 81 exponents. Widened to 16 of 81 across the full range, APPENDING pairs rather than reseeding so the existing 600 stay byte-identical: 1744 pairs, still green, and the negative control confirms it still reddens (12 mismatches on a mutated model)
- I had guessed the suite was blind to rounding TIES. Measured: 20 of 256 pairs land on rem == half. The guess was wrong and the measurement was cheap -- the blind spot was one dimension over from where I expected it
- New command `tri pointers`. The obvious version -- every path-shaped string -- measured 873 mentions with 409 unresolved, dominated by paths a program CREATES and fixture names in unit tests; a report that is 95 percent noise gets switched off and the 5 percent goes with it. Narrowed to a prose pointer (see X, cf. X, documented in X): 193 mentions, 16 unresolved. The narrowing was chosen by measuring both, not by taste
- Its first run reported its OWN docstring, which quoted the dead path after the word "see" while explaining the bug -- the second time in two days that a checker read its own documentation as data. No special case for the tool own file: that would hide the same thing in the next document about pointers. Both times the tell was a COUNT one higher than it should have been, not the list
