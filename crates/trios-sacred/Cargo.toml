[package]
name = "trios-sacred"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Rust bindings for zig-sacred-geometry (φ-attention, Beal conjecture search)"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
