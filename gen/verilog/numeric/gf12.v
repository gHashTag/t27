// ============================================================================
// Generated from t27 spec: GF12
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module GF12 (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [7:0] BITS = 12;

    // -------------------------------------------------------
    // Registers (from struct declarations)
    // -------------------------------------------------------
    // struct GF12
    reg [15:0] gf12_raw; // GF12.raw

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: encode
    function [31:0] encode; // -> GF12
        input [31:0] value;
        begin
            if ((value == 0.0)) begin
                encode = 0 /* GF12 {...} */;
            end
            reg [31:0] exp_unbiased = floor_log2(abs_val);
            as;
            i8;
            reg [31:0] exp_clamped = clamp(exp_biased, 0, ((1 << EXP_BITS) - 1));
            reg [31:0] mant = extract_mantissa(abs_val, exp_unbiased, MANT_BITS);
        end
    endfunction
endmodule

`default_nettype wire
