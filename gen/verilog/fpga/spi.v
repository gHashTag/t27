// ============================================================================
// Generated from t27 spec: SPI_Master
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module SPI_Master (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [31:0] CLK_FREQ = 50_000_000;
    localparam [31:0] spi = 0;

    // -------------------------------------------------------
    // Registers (from struct declarations)
    // -------------------------------------------------------
    // struct SPI_Master_Unit
    reg [7:0] spi_master_unit_state; // SPI_Master_Unit.state
    reg [7:0] spi_master_unit_tx_state; // SPI_Master_Unit.tx_state
    reg spi_master_unit_cs_asserted; // SPI_Master_Unit.cs_asserted
    reg spi_master_unit_busy; // SPI_Master_Unit.busy
    reg [7:0] spi_master_unit_prescaler; // SPI_Master_Unit.prescaler
    reg [7:0] spi_master_unit_data_width; // SPI_Master_Unit.data_width
    reg [7:0] spi_master_unit_cs_mode; // SPI_Master_Unit.cs_mode
    reg [31:0] spi_master_unit_tx_data; // SPI_Master_Unit.tx_data
    reg [31:0] spi_master_unit_rx_data; // SPI_Master_Unit.rx_data
    reg [7:0] spi_master_unit_bit_count; // SPI_Master_Unit.bit_count
    reg [31:0] spi_master_unit_bit_counter; // SPI_Master_Unit.bit_counter
    reg [31:0] spi_master_unit_cs_assert_cnt; // SPI_Master_Unit.cs_assert_cnt
    reg [31:0] spi_master_unit_cs_deassert_cnt; // SPI_Master_Unit.cs_deassert_cnt

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: spi_set_prescaler
    function spi_set_prescaler; // -> bool
        input [7:0] psc;
        begin
            if ((psc > PRESCALER_256)) begin
                spi_set_prescaler = 1'b0;
            end
            spi_prescaler = psc;
            spi_set_prescaler = 1'b1;
        end
    endfunction

    // function: spi_get_prescaler_div
    function [31:0] spi_get_prescaler_div; // -> u32
        begin
            match;
            spi_prescaler;
        end
    endfunction

    // function: spi_get_sck_freq
    function [31:0] spi_get_sck_freq; // -> u32
        begin
            spi_get_sck_freq = (CLK_FREQ / spi_get_prescaler_div());
        end
    endfunction

    // function: spi_set_data_width
    function spi_set_data_width; // -> bool
        input [7:0] width;
        begin
            if (((width == 0) || (width > MAX_DATA_WIDTH))) begin
                spi_set_data_width = 1'b0;
            end
            spi_data_width = width;
            spi_set_data_width = 1'b1;
        end
    endfunction

    // function: spi_is_busy
    function spi_is_busy; // -> bool
        begin
            spi_is_busy = spi_busy;
        end
    endfunction

    // function: spi_transfer
    function spi_transfer; // -> bool
        input [31:0] data;
        begin
            if (spi_busy) begin
                spi_transfer = 1'b0;
            end
            spi_tx_data = data;
            spi_rx_data = 0;
            spi_bit_count = 0;
            spi_bit_counter = 0;
            spi_state = SPI_CS_ASSERT;
            spi_busy = 1'b1;
            spi_transfer = 1'b1;
        end
    endfunction

    // function: spi_read_rx
    function [31:0] spi_read_rx; // -> u32
        begin
            spi_read_rx = (spi_rx_data && ((1 << spi_data_width) - 1));
        end
    endfunction

    // function: spi_get_cs
    function spi_get_cs; // -> bool
        begin
            spi_get_cs = spi_cs_asserted;
        end
    endfunction

    // function: spi_get_sck
    function spi_get_sck; // -> bool
        begin
            match;
            spi_tx_state;
        end
    endfunction

    // function: spi_get_mosi
    function spi_get_mosi; // -> bool
        begin
            if ((!spi_busy || (spi_state != SPI_TRANSFER))) begin
                spi_get_mosi = 1'b0;
            end
            spi_get_mosi = ((spi_tx_data >> ((spi_data_width - spi_bit_count) - 1)) && (1 == 1));
        end
    endfunction

    // function: spi_tick
    task spi_tick;
        begin
            match;
            spi_state;
        end
    endtask

    // function: spi_transfer_bit
    task spi_transfer_bit;
        begin
            reg [31:0] prescaler_div = spi_get_prescaler_div();
            spi_bit_counter = (spi_bit_counter + 1);
            match;
            spi_tx_state;
        end
    endtask
    // -------------------------------------------------------
    // Test assertions (from test blocks)
    // -------------------------------------------------------
    // synthesis translate_off
    // test: spi_mode_0_configuration
    // test: spi_prescaler_16_default
    // test: spi_set_prescaler_valid
    // test: spi_set_prescaler_invalid
    // test: spi_prescaler_div_16
    // test: spi_sck_freq_at_50MHz
    // test: spi_set_data_width_8
    // test: spi_set_data_width_32
    // test: spi_set_data_width_invalid
    // test: spi_initially_not_busy
    // test: spi_transfer_when_ready
    // test: spi_transfer_when_busy
    // test: spi_cs_idle_high
    // test: spi_sck_idle_low
    // test: spi_max_data_width_32
    // test: spi_prescaler_range
    // test: spi_cs_delays_defined
    // synthesis translate_on

    // -------------------------------------------------------
    // Invariant checks (compile-time assertions)
    // -------------------------------------------------------
    // invariant: spi_mode_0_constant
    // invariant: spi_states_valid
    // invariant: spi_tx_states_valid
    // invariant: spi_prescaler_divides_clock
    // invariant: spi_data_width_bounds
    // invariant: spi_busy_implies_cs_asserted
    // invariant: spi_busy_only_in_transfer
    // invariant: spi_sck_alternates
    // invariant: spi_cs_deasserted_after_transfer
    // invariant: spi_rx_data_masked
    // invariant: spi_bit_count_reset_after_transfer
    // invariant: spi_cs_delay_counters_reset

    // -------------------------------------------------------
    // Benchmark placeholders
    // -------------------------------------------------------
    // bench: spi_transfer_latency
    // bench: spi_sck_max_frequency
    // bench: spi_cs_assertion_time
    // bench: spi_prescaler_change_latency

endmodule

`default_nettype wire
