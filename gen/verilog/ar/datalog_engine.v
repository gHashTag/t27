// ============================================================================
// Generated from t27 spec: DatalogEngine
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module DatalogEngine (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    parameter [31:0] MAX_CLAUSES = 256;
    parameter [31:0] MAX_ARGS = 8;

    // -------------------------------------------------------
    // Registers (from struct declarations)
    // -------------------------------------------------------
    // struct HornClause
    reg [15:0] hornclause_name; // HornClause.name
    reg [31:0] hornclause_args; // HornClause.args
    reg [7:0] hornclause_arg_count; // HornClause.arg_count
    // struct DatalogEngine
    reg [31:0] datalogengine_facts; // DatalogEngine.facts
    reg [31:0] datalogengine_fact_count; // DatalogEngine.fact_count
    reg [31:0] datalogengine_rules; // DatalogEngine.rules
    reg [31:0] datalogengine_rule_count; // DatalogEngine.rule_count
    reg [31:0] datalogengine_derived_facts; // DatalogEngine.derived_facts
    reg datalogengine_solved; // DatalogEngine.solved

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: datalog_init
    function [31:0] datalog_init; // -> DatalogEngine
        begin
            datalog_init = 0 /* DatalogEngine {...} */;
        end
    endfunction

    // function: add_fact
    function add_fact; // -> bool
        input [31:0] engine;
        input [31:0] fact;
        begin
            reg [31:0] i = 0;
            while ((i < engine_fact_count)) begin
                if (horn_clause_eq(fact, engine_facts[i])) begin
                    add_fact = 1'b0;
                end
                i = (i + 1);
            end
            if ((engine_fact_count >= MAX_CLAUSES)) begin
                add_fact = 1'b0;
            end
            engine_facts[engine_fact_count] = fact;
            engine_derived_facts[engine_fact_count] = 1'b0;
            engine_fact_count = (engine_fact_count + 1);
            add_fact = 1'b1;
        end
    endfunction

    // function: has_fact
    function has_fact; // -> bool
        input [31:0] engine;
        input [31:0] fact;
        begin
            reg [31:0] i = 0;
            while ((i < engine_fact_count)) begin
                if (horn_clause_eq(fact, engine_facts[i])) begin
                    has_fact = 1'b1;
                end
                i = (i + 1);
            end
            has_fact = 1'b0;
        end
    endfunction

    // function: add_rule
    function add_rule; // -> bool
        input [31:0] engine;
        input [31:0] rule;
        begin
            reg [31:0] i = 0;
            while ((i < engine_rule_count)) begin
                if (rule_eq(rule, engine_rules[i])) begin
                    add_rule = 1'b0;
                end
                i = (i + 1);
            end
            if ((engine_rule_count >= MAX_CLAUSES)) begin
                add_rule = 1'b0;
            end
            engine_rules[engine_rule_count] = rule;
            engine_rule_count = (engine_rule_count + 1);
            add_rule = 1'b1;
        end
    endfunction

    // function: datalog_solve
    function [31:0] datalog_solve; // -> ProofTrace
        input [31:0] engine;
        begin
            reg [31:0] trace = proof_trace::create_trace();
            reg [31:0] new_facts = 0;
            reg changed = 1'b1;
            reg [31:0] iterations = 0;
            reg [31:0] MAX_ITERATIONS = MAX_CLAUSES;
            reg [7:0] step_num = 1;
            while ((changed && (iterations < MAX_ITERATIONS))) begin
                changed = 1'b0;
                iterations = (iterations + 1);
                reg [31:0] i = 0;
                while ((i < engine_rule_count)) begin
                    reg [31:0] rule = engine_rules[i];
                    reg [31:0] j = 0;
                    while ((j < engine_fact_count)) begin
                        reg [31:0] fact_trit = args[0];
                        reg [31:0] derived = forward_chain(rule, fact_trit);
                        if ((derived == K_TRUE)) begin
                        end
                        if (add_fact(engine, new_fact)) begin
                            new_facts = (new_facts + 1);
                            changed = 1'b1;
                            engine_derived_facts[(engine_fact_count - 1)] = 1'b1;
                            reg [31:0] step = 0 /* proof_trace::DerivationStep {...} */;
                            _ = proof_trace::append_step(&&race, step);
                            step_num = step_num + 1;
                        end
                    end
                    j = (j + 1);
                end
                i = (i + 1);
            end
        end
    endfunction
endmodule

`default_nettype wire
