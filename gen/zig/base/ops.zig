// Generated from t27 spec: tritype-ops (module name)
// DO NOT EDIT — generated by t27c
// phi^2 + 1/phi^2 = 3 | TRINITY

const std = @import("std");

pub const NEGONE: i8 = -1;
pub const ZERO: i8 = 0;
pub const ONE: i8 = 1;
pub const Trit = enum(i8) {
    neg = -1,
    zero = 0,
    pos = 1,
};
pub const mult_table: [9]i8 = [1,0,-1,0,0,0,-1,0,1];
pub const add_table: [9]i8 = [-1,-1,0,-1,0,1,0,1,1];
pub const carry_table: [9]i8 = [1,0,0,0,0,0,0,0,-1];
pub fn trit_multiply_table(a: Trit, b: Trit) Trit {
    const idx = ((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1);
    return @as(Trit, @enumFromInt(mult_table[idx]));
}
pub fn trit_add_table(a: Trit, b: Trit) Trit {
    const idx = ((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1);
    return @as(Trit, @enumFromInt(add_table[idx]));
}
pub fn trit_carry_table(a: Trit, b: Trit) Trit {
    const idx = ((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1);
    return @as(Trit, @enumFromInt(carry_table[idx]));
}
pub const AddResult = struct {
    result: Trit,
    carry_out: Trit,
};
pub fn trit_add_with_carry(a: Trit, b: Trit, carry_in: Trit) AddResult {
    var sum: i8 = @intFromEnum(a) + @intFromEnum(b);
    var carry: Trit = .zero;
    var result: Trit = .zero;
    if (sum > 1) {
        result = .neg;
        carry = .pos;
    } else if (sum < -1) {
        result = .pos;
        carry = .neg;
    } else {
        result = @as(Trit, @enumFromInt(sum));
    }
    sum = @intFromEnum(result) + @intFromEnum(carry_in);
    if (sum > 1) {
        result = .neg;
        carry = .pos;
    } else if (sum < -1) {
        result = .pos;
        carry = .neg;
    } else {
        result = @as(Trit, @enumFromInt(sum));
        carry = .zero;
    }
    return AddResult{ .result = result, .carry_out = carry };
}
pub fn trit_compare(a: Trit, b: Trit) i8 {
    if (a == b) {
        return 0;
    } else if ((a == .neg) or ((a == .zero) and (b == .pos))) {
        return -1;
    } else {
        return 1;
    }
}
pub fn trit_negate(a: Trit) Trit {
    return switch (a) {
        .neg => .pos,
        .zero => .zero,
        .pos => .neg,
    };
}
pub fn trit_abs(a: Trit) Trit {
    return if (a == .neg) .pos else a;
}
pub fn trit_min(a: Trit, b: Trit) Trit {
    return if ((a == .neg) or ((a == .zero) and (b == .pos))) a else b;
}
pub fn trit_max(a: Trit, b: Trit) Trit {
    return if ((a == .pos) or ((a == .zero) and (b == .neg))) a else b;
}
pub fn trit_subtract(a: Trit, b: Trit) Trit {
    return trit_add_table(a, trit_negate(b));
}
pub fn trit_sign(a: Trit) i8 {
    return @intFromEnum(a);
}
pub fn trit_clamp(a: Trit, min_val: Trit, max_val: Trit) Trit {
    if (trit_compare(a, min_val) < 0) {
        return min_val;
    }
    if (trit_compare(a, max_val) > 0) {
        return max_val;
    }
    return a;
}
pub fn trit_is_negative(a: Trit) bool {
    return a == .neg;
}
pub fn trit_is_zero(a: Trit) bool {
    return a == .zero;
}
pub fn trit_is_positive(a: Trit) bool {
    return a == .pos;
}
pub fn trit_equal(a: Trit, b: Trit) bool {
    return a == b;
}
pub fn trit_not_equal(a: Trit, b: Trit) bool {
    return a != b;
}
pub fn trit_lt(a: Trit, b: Trit) bool {
    return trit_compare(a, b) < 0;
}
pub fn trit_le(a: Trit, b: Trit) bool {
    return trit_compare(a, b) <= 0;
}
pub fn trit_gt(a: Trit, b: Trit) bool {
    return trit_compare(a, b) > 0;
}
pub fn trit_ge(a: Trit, b: Trit) bool {
    return trit_compare(a, b) >= 0;
}
pub fn trit_multiply_with_carry(a: Trit, b: Trit, carry_in: Trit) AddResult {
    const product = trit_multiply_table(a, b);
    return trit_add_with_carry(product, carry_in, .zero);
}
pub fn trit_reverse(a: Trit) Trit {
    return if (a == .zero) .zero else a;
}
pub fn trit_multiply_by_power_of_two(a: Trit, power: u8) Trit {
    var result = a;
    var i: u8 = 1;
    while (i < power) {
        const carry = trit_carry_table(result, a);
        if (carry != .zero) {
            result = if (carry == .pos) .pos else .neg;
        }
        result = trit_add_table(result, a);
        i += 1;
    }
    return result;
}
pub fn trit_power(a: Trit, n: u8) Trit {
    if (n == 0) {
        return .pos;
    }
    if (n == 1) {
        return a;
    }
    if (a == .zero) {
        return .zero;
    }
    if (a == .pos) {
        return .pos;
    }
    return if ((n % 2) == 1) .neg else .pos;
}
pub fn trit_from_bool(b: bool) Trit {
    return if (b) .pos else .zero;
}
pub fn trit_to_bool(a: Trit) bool {
    return a == .pos;
}
pub fn trit_abs_diff(a: Trit, b: Trit) Trit {
    return if (a == b) .zero else .pos;
}
pub fn trit_cond_swap(cond: Trit, a: Trit, b: Trit) Trit {
    return if (cond == .pos) b else a;
}
pub fn trit_is_unit(a: Trit) bool {
    return a == .pos;
}
pub fn trit_is_identity(a: Trit) bool {
    return a == .zero;
}
pub fn trit_is_negated(a: Trit, b: Trit) bool {
    return b == trit_negate(a);
}
test "test_trit_multiply_table_all_combinations" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_multiply_table(.neg, .neg));
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_table(.neg, .zero));
    try std.testing.expectEqual(@as(Trit, .neg), trit_multiply_table(.neg, .pos));
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_table(.zero, .neg));
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_table(.zero, .zero));
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_table(.zero, .pos));
    try std.testing.expectEqual(@as(Trit, .neg), trit_multiply_table(.pos, .neg));
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_table(.pos, .zero));
    try std.testing.expectEqual(@as(Trit, .pos), trit_multiply_table(.pos, .pos));
}
test "test_trit_multiply_table_commutative" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_multiply_table(a, b), trit_multiply_table(b, a));
        }
    }
}
test "test_trit_add_table_neg_plus_neg" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_add_table(.neg, .neg));
}
test "test_trit_add_table_neg_plus_zero" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_add_table(.neg, .zero));
}
test "test_trit_add_table_neg_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_add_table(.neg, .pos));
}
test "test_trit_add_table_zero_plus_zero" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_add_table(.zero, .zero));
}
test "test_trit_add_table_zero_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_add_table(.zero, .pos));
}
test "test_trit_add_table_pos_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_add_table(.pos, .pos));
}
test "test_trit_add_table_commutative" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_add_table(a, b), trit_add_table(b, a));
        }
    }
}
test "test_trit_add_table_identity_zero" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_add_table(.neg, .zero));
    try std.testing.expectEqual(@as(Trit, .zero), trit_add_table(.zero, .zero));
    try std.testing.expectEqual(@as(Trit, .pos), trit_add_table(.pos, .zero));
}
test "test_trit_carry_table_neg_plus_neg" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_carry_table(.neg, .neg));
}
test "test_trit_carry_table_neg_plus_zero" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_carry_table(.neg, .zero));
}
test "test_trit_carry_table_neg_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_carry_table(.neg, .pos));
}
test "test_trit_carry_table_zero_plus_zero" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_carry_table(.zero, .zero));
}
test "test_trit_carry_table_zero_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_carry_table(.zero, .pos));
}
test "test_trit_carry_table_pos_plus_pos" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_carry_table(.pos, .pos));
}
test "test_trit_carry_table_commutative" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_carry_table(a, b), trit_carry_table(b, a));
        }
    }
}
test "test_trit_add_with_carry_no_carry" {
    const result = trit_add_with_carry(.pos, .neg, .zero);
    try std.testing.expectEqual(@as(Trit, .zero), result.result);
    try std.testing.expectEqual(@as(Trit, .zero), result.carry_out);
}
test "test_trit_add_with_carry_positive_overflow" {
    const result = trit_add_with_carry(.pos, .pos, .zero);
    try std.testing.expectEqual(@as(Trit, .neg), result.result);
    try std.testing.expectEqual(@as(Trit, .pos), result.carry_out);
}
test "test_trit_add_with_carry_negative_overflow" {
    const result = trit_add_with_carry(.neg, .neg, .zero);
    try std.testing.expectEqual(@as(Trit, .pos), result.result);
    try std.testing.expectEqual(@as(Trit, .neg), result.carry_out);
}
test "test_trit_add_with_carry_propagation" {
    const result = trit_add_with_carry(.pos, .pos, .pos);
    try std.testing.expectEqual(@as(Trit, .zero), result.result);
    try std.testing.expectEqual(@as(Trit, .pos), result.carry_out);
}
test "test_trit_add_with_carry_result_in_range" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            for (trits) {
                const result = trit_add_with_carry(a, b, c);
                try std.testing.expect((@intFromEnum(result.result) >= -1) and (@intFromEnum(result.result) <= 1));
            }
        }
    }
}
test "test_trit_add_with_carry_carry_in_range" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            for (trits) {
                const result = trit_add_with_carry(a, b, c);
                try std.testing.expect((@intFromEnum(result.carry_out) >= -1) and (@intFromEnum(result.carry_out) <= 1));
            }
        }
    }
}
test "test_trit_compare_less_than" {
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.neg, .zero));
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.neg, .pos));
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.zero, .pos));
}
test "test_trit_compare_equal" {
    try std.testing.expectEqual(@as(i8, 0), trit_compare(.neg, .neg));
    try std.testing.expectEqual(@as(i8, 0), trit_compare(.zero, .zero));
    try std.testing.expectEqual(@as(i8, 0), trit_compare(.pos, .pos));
}
test "test_trit_compare_greater_than" {
    try std.testing.expectEqual(@as(i8, 1), trit_compare(.pos, .zero));
    try std.testing.expectEqual(@as(i8, 1), trit_compare(.pos, .neg));
    try std.testing.expectEqual(@as(i8, 1), trit_compare(.zero, .neg));
}
test "test_trit_compare_total_ordering" {
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.neg, .zero));
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.zero, .pos));
    try std.testing.expectEqual(@as(i8, -1), trit_compare(.neg, .pos));
}
test "test_trit_negate_involutive" {
    try std.testing.expectEqual(.neg, trit_negate(trit_negate(.neg)));
    try std.testing.expectEqual(.zero, trit_negate(trit_negate(.zero)));
    try std.testing.expectEqual(.pos, trit_negate(trit_negate(.pos)));
}
test "test_trit_abs_non_negative" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_abs(.neg));
    try std.testing.expectEqual(@as(Trit, .zero), trit_abs(.zero));
    try std.testing.expectEqual(@as(Trit, .pos), trit_abs(.pos));
}
test "test_trit_min_returns_minimum" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_min(.neg, .pos));
    try std.testing.expectEqual(@as(Trit, .neg), trit_min(.neg, .zero));
    try std.testing.expectEqual(@as(Trit, .zero), trit_min(.zero, .pos));
    try std.testing.expectEqual(@as(Trit, .neg), trit_min(.pos, .neg));
}
test "test_trit_max_returns_maximum" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_max(.neg, .pos));
    try std.testing.expectEqual(@as(Trit, .zero), trit_max(.neg, .zero));
    try std.testing.expectEqual(@as(Trit, .pos), trit_max(.zero, .pos));
    try std.testing.expectEqual(@as(Trit, .pos), trit_max(.pos, .neg));
}
test "test_trit_min_idempotent" {
    try std.testing.expectEqual(.neg, trit_min(.neg, .neg));
    try std.testing.expectEqual(.zero, trit_min(.zero, .zero));
    try std.testing.expectEqual(.pos, trit_min(.pos, .pos));
}
test "test_trit_max_idempotent" {
    try std.testing.expectEqual(.neg, trit_max(.neg, .neg));
    try std.testing.expectEqual(.zero, trit_max(.zero, .zero));
    try std.testing.expectEqual(.pos, trit_max(.pos, .pos));
}
test "test_trit_min_max_antisymmetric" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_min(a, b), trit_max(b, a));
        }
    }
}
test "test_trit_subtract_neg_from_pos" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_subtract(.pos, .neg));
}
test "test_trit_subtract_pos_from_pos" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_subtract(.pos, .pos));
}
test "test_trit_subtract_neg_from_neg" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_subtract(.neg, .neg));
}
test "test_trit_subtract_zero_from_pos" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_subtract(.pos, .zero));
}
test "test_trit_subtract_zero_from_neg" {
    try std.testing.expectEqual(@as(Trit, .neg), trit_subtract(.neg, .zero));
}
test "test_trit_subtract_all_combinations" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_subtract(a, b), trit_add_table(a, trit_negate(b)));
        }
    }
}
test "test_trit_sign_negative" {
    try std.testing.expectEqual(@as(i8, -1), trit_sign(.neg));
}
test "test_trit_sign_zero" {
    try std.testing.expectEqual(@as(i8, 0), trit_sign(.zero));
}
test "test_trit_sign_positive" {
    try std.testing.expectEqual(@as(i8, 1), trit_sign(.pos));
}
test "test_trit_sign_matches_enum_value" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        try std.testing.expectEqual(@intFromEnum(a), trit_sign(a));
    }
}
test "test_trit_clamp_below_min" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_clamp(.neg, .zero, .pos));
}
test "test_trit_clamp_above_max" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_clamp(.pos, .neg, .zero));
}
test "test_trit_clamp_in_range" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        try std.testing.expectEqual(a, trit_clamp(a, .neg, .pos));
    }
}
test "test_trit_clamp_at_bounds" {
    try std.testing.expectEqual(.zero, trit_clamp(.zero, .zero, .pos));
    try std.testing.expectEqual(.pos, trit_clamp(.pos, .zero, .pos));
}
test "test_trit_is_negative_true_for_neg" {
    try std.testing.expect(trit_is_negative(.neg));
}
test "test_trit_is_negative_false_for_others" {
    try std.testing.expect(!trit_is_negative(.zero));
    try std.testing.expect(!trit_is_negative(.pos));
}
test "test_trit_is_zero_true_for_zero" {
    try std.testing.expect(trit_is_zero(.zero));
}
test "test_trit_is_zero_false_for_others" {
    try std.testing.expect(!trit_is_zero(.neg));
    try std.testing.expect(!trit_is_zero(.pos));
}
test "test_trit_is_positive_true_for_pos" {
    try std.testing.expect(trit_is_positive(.pos));
}
test "test_trit_is_positive_false_for_others" {
    try std.testing.expect(!trit_is_positive(.neg));
    try std.testing.expect(!trit_is_positive(.zero));
}
test "test_trit_equal_same_values" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        try std.testing.expect(trit_equal(a, a));
    }
}
test "test_trit_equal_different_values_false" {
    try std.testing.expect(!trit_equal(.neg, .zero));
    try std.testing.expect(!trit_equal(.zero, .pos));
    try std.testing.expect(!trit_equal(.pos, .neg));
}
test "test_trit_not_equal_same_values_false" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        try std.testing.expect(!trit_not_equal(a, a));
    }
}
test "test_trit_not_equal_different_values_true" {
    try std.testing.expect(trit_not_equal(.neg, .zero));
    try std.testing.expect(trit_not_equal(.zero, .pos));
    try std.testing.expect(trit_not_equal(.pos, .neg));
}
test "test_trit_lt_less_than" {
    try std.testing.expect(trit_lt(.neg, .zero));
    try std.testing.expect(trit_lt(.neg, .pos));
    try std.testing.expect(trit_lt(.zero, .pos));
}
test "test_trit_lt_false_for_equal_or_greater" {
    try std.testing.expect(!trit_lt(.neg, .neg));
    try std.testing.expect(!trit_lt(.zero, .neg));
    try std.testing.expect(!trit_lt(.pos, .pos));
}
test "test_trit_le_less_than_or_equal" {
    try std.testing.expect(trit_le(.neg, .zero));
    try std.testing.expect(trit_le(.neg, .pos));
    try std.testing.expect(trit_le(.zero, .pos));
    try std.testing.expect(trit_le(.neg, .neg));
    try std.testing.expect(trit_le(.zero, .zero));
    try std.testing.expect(trit_le(.pos, .pos));
}
test "test_trit_gt_greater_than" {
    try std.testing.expect(trit_gt(.pos, .zero));
    try std.testing.expect(trit_gt(.pos, .neg));
    try std.testing.expect(trit_gt(.zero, .neg));
}
test "test_trit_ge_greater_than_or_equal" {
    try std.testing.expect(trit_ge(.pos, .zero));
    try std.testing.expect(trit_ge(.pos, .neg));
    try std.testing.expect(trit_ge(.zero, .neg));
    try std.testing.expect(trit_ge(.neg, .neg));
    try std.testing.expect(trit_ge(.zero, .zero));
    try std.testing.expect(trit_ge(.pos, .pos));
}
test "test_trit_comparison_consistency" {
    const a: Trit = .neg;
    const b: Trit = .pos;
    try std.testing.expect(trit_lt(a, b));
    try std.testing.expect(trit_le(a, b));
    try std.testing.expect(!trit_gt(a, b));
    try std.testing.expect(!trit_ge(a, b));
}
test "test_trit_multiply_with_carry_basic" {
    const result = trit_multiply_with_carry(.pos, .pos, .zero);
    try std.testing.expectEqual(@as(Trit, .neg), result.result);
    try std.testing.expectEqual(@as(Trit, .pos), result.carry_out);
}
test "test_trit_multiply_with_carry_with_carry" {
    const result = trit_multiply_with_carry(.pos, .pos, .pos);
    try std.testing.expectEqual(@as(Trit, .zero), result.result);
    try std.testing.expectEqual(@as(Trit, .pos), result.carry_out);
}
test "test_trit_multiply_with_carry_zero_annihilates" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        const result = trit_multiply_with_carry(.zero, t, .zero);
        try std.testing.expectEqual(@as(Trit, .zero), result.result);
        try std.testing.expectEqual(@as(Trit, .zero), result.carry_out);
    }
}
test "test_trit_reverse_non_zero" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_reverse(.pos));
    try std.testing.expectEqual(@as(Trit, .neg), trit_reverse(.neg));
}
test "test_trit_reverse_zero" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_reverse(.zero));
}
test "test_trit_multiply_by_power_of_two_zero" {
    try std.testing.expectEqual(@as(Trit, .zero), trit_multiply_by_power_of_two(.zero, 3));
}
test "test_trit_multiply_by_power_of_two_one" {
    try std.testing.expectEqual(@as(Trit, .pos), trit_multiply_by_power_of_two(.pos, 0));
}
test "test_trit_multiply_by_power_of_two_power_one" {
    const result = trit_multiply_by_power_of_two(.pos, 1);
    try std.testing.expect((result == .neg) or (result == .pos));
}
test "test_trit_multiply_by_power_of_two_identity" {
    try std.testing.expectEqual(.neg, trit_multiply_by_power_of_two(.neg, 0));
    try std.testing.expectEqual(.zero, trit_multiply_by_power_of_two(.zero, 0));
    try std.testing.expectEqual(.pos, trit_multiply_by_power_of_two(.pos, 0));
}
test "test_trit_power_zero_exponent" {
    try std.testing.expectEqual(.pos, trit_power(.neg, 0));
    try std.testing.expectEqual(.pos, trit_power(.pos, 0));
}
test "test_trit_power_zero_base" {
    try std.testing.expectEqual(.zero, trit_power(.zero, 1));
    try std.testing.expectEqual(.zero, trit_power(.zero, 2));
    try std.testing.expectEqual(.zero, trit_power(.zero, 3));
}
test "test_trit_power_one" {
    try std.testing.expectEqual(.neg, trit_power(.neg, 1));
    try std.testing.expectEqual(.zero, trit_power(.zero, 1));
    try std.testing.expectEqual(.pos, trit_power(.pos, 1));
}
test "test_trit_power_square" {
    try std.testing.expectEqual(.pos, trit_power(.neg, 2));
    try std.testing.expectEqual(.zero, trit_power(.zero, 2));
    try std.testing.expectEqual(.pos, trit_power(.pos, 2));
}
test "test_trit_power_cube" {
    try std.testing.expectEqual(.neg, trit_power(.neg, 3));
    try std.testing.expectEqual(.zero, trit_power(.zero, 3));
    try std.testing.expectEqual(.pos, trit_power(.pos, 3));
}
test "test_trit_from_bool_true" {
    try std.testing.expectEqual(.pos, trit_from_bool(true));
}
test "test_trit_from_bool_false" {
    try std.testing.expectEqual(.zero, trit_from_bool(false));
}
test "test_trit_to_bool_positive" {
    try std.testing.expect(trit_to_bool(.pos));
}
test "test_trit_to_bool_non_positive" {
    try std.testing.expect(!trit_to_bool(.zero));
    try std.testing.expect(!trit_to_bool(.neg));
}
test "test_trit_to_bool_from_bool_roundtrip" {
    try std.testing.expectEqual(trit_to_bool(trit_from_bool(true)), true);
    try std.testing.expectEqual(trit_to_bool(trit_from_bool(false)), false);
}
test "test_trit_abs_diff_equal" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        try std.testing.expectEqual(.zero, trit_abs_diff(a, a));
    }
}
test "test_trit_abs_diff_different" {
    try std.testing.expectEqual(.pos, trit_abs_diff(.neg, .zero));
    try std.testing.expectEqual(.pos, trit_abs_diff(.neg, .pos));
    try std.testing.expectEqual(.pos, trit_abs_diff(.zero, .pos));
}
test "test_trit_abs_diff_commutative" {
    const trits = [_]Trit{.neg, .zero, .pos};
    for (trits) {
        for (trits) {
            try std.testing.expectEqual(trit_abs_diff(a, b), trit_abs_diff(b, a));
        }
    }
}
test "test_trit_cond_swap_condition_true" {
    try std.testing.expectEqual(.neg, trit_cond_swap(.pos, .neg, .zero));
    try std.testing.expectEqual(.pos, trit_cond_swap(.pos, .zero, .pos));
}
test "test_trit_cond_swap_condition_false" {
    try std.testing.expectEqual(.neg, trit_cond_swap(.zero, .neg, .pos));
    try std.testing.expectEqual(.pos, trit_cond_swap(.neg, .pos, .neg));
}
test "test_trit_is_unit_true_for_pos" {
    try std.testing.expect(trit_is_unit(.pos));
}
test "test_trit_is_unit_false_for_others" {
    try std.testing.expect(!trit_is_unit(.zero));
    try std.testing.expect(!trit_is_unit(.neg));
}
test "test_trit_is_identity_true_for_zero" {
    try std.testing.expect(trit_is_identity(.zero));
}
test "test_trit_is_identity_false_for_others" {
    try std.testing.expect(!trit_is_identity(.pos));
    try std.testing.expect(!trit_is_identity(.neg));
}
test "test_trit_is_negated_true_pair" {
    try std.testing.expect(trit_is_negated(.neg, .pos));
    try std.testing.expect(trit_is_negated(.pos, .neg));
    try std.testing.expect(trit_is_negated(.zero, .zero));
}
test "test_trit_is_negated_false_non_pair" {
    try std.testing.expect(!trit_is_negated(.neg, .neg));
    try std.testing.expect(!trit_is_negated(.pos, .pos));
    try std.testing.expect(!trit_is_negated(.zero, .pos));
}
comptime {
    // invariant: trit_subtract_add_negation_identity
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_sign_matches_enum_value
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_clamp_preserves_order
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_clamp_idempotent
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_negative_matches_sign
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_zero_matches_sign
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_positive_matches_sign
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_equal_reflexive
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_equal_symmetric
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_not_equal_complement
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_lt_implies_le
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_gt_implies_ge
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_lt_and_gt_mutually_exclusive
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_le_and_ge_mutually_inclusive
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_lt_gt_antisymmetric
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_le_ge_antisymmetric
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_multiply_with_carry_no_overflow_for_zero
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_reverse_self_inverse_nonzero
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_reverse_zero_no_inverse
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_multiply_by_power_of_two_identity
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_multiply_by_power_of_two_zero_annihilates
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_multiply_table_commutative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_add_table_commutative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_add_table_identity_zero
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_carry_table_commutative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_carry_table_neg_plus_neg_gives_positive
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_carry_table_pos_plus_pos_gives_negative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_add_with_carry_result_in_range
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_add_with_carry_carry_in_range
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_compare_total_ordering
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_negate_double_identity
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_abs_result_non_negative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_min_idempotent
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_max_idempotent
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_min_max_antisymmetric
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_power_zero_exponent_returns_unit
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_power_zero_base_returns_zero
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_power_pos_always_pos
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_from_bool_to_bool_roundtrip
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_abs_diff_non_negative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_abs_diff_zero_iff_equal
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_abs_diff_commutative
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_cond_swap_condition_true_swaps
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_cond_swap_condition_false_keeps_first
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_unit_only_for_pos
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_identity_only_for_zero
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_negated_symmetric
    if (!(true)) @compileError("assertion failed");
}
comptime {
    // invariant: trit_is_negated_zero_self
    if (!(true)) @compileError("assertion failed");
}
fn bench_trit_multiply_table_latency() void {
    // bench: bench_trit_multiply_table_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_multiply_table(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_add_table_latency() void {
    // bench: bench_trit_add_table_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_add_table(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_carry_table_latency() void {
    // bench: bench_trit_carry_table_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_carry_table(.pos, .pos);
    }
    _ = result;
}
fn bench_trit_add_with_carry_latency() void {
    // bench: bench_trit_add_with_carry_latency
    @setEvalBranchQuota(10000);
    var result = trit_add_with_carry(.pos, .pos, .zero);
    for (0 .. 1000) {
        result = trit_add_with_carry(.pos, .pos, .zero);
    }
    _ = result;
}
fn bench_trit_compare_latency() void {
    // bench: bench_trit_compare_latency
    @setEvalBranchQuota(10000);
    var result: i8 = 0;
    for (0 .. 1000) {
        result = trit_compare(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_min_max_latency() void {
    // bench: bench_trit_min_max_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_min(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_lt_latency() void {
    // bench: bench_trit_lt_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_lt(.neg, .pos);
    }
    _ = result;
}
fn bench_trit_le_latency() void {
    // bench: bench_trit_le_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_le(.neg, .pos);
    }
    _ = result;
}
fn bench_trit_gt_latency() void {
    // bench: bench_trit_gt_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_gt(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_ge_latency() void {
    // bench: bench_trit_ge_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_ge(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_multiply_with_carry_latency() void {
    // bench: bench_trit_multiply_with_carry_latency
    @setEvalBranchQuota(10000);
    var result = trit_multiply_with_carry(.pos, .pos, .zero);
    for (0 .. 1000) {
        result = trit_multiply_with_carry(.pos, .pos, .zero);
    }
    _ = result;
}
fn bench_trit_reverse_latency() void {
    // bench: bench_trit_reverse_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_reverse(.pos);
    }
    _ = result;
}
fn bench_trit_multiply_by_power_of_two_latency() void {
    // bench: bench_trit_multiply_by_power_of_two_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_multiply_by_power_of_two(.pos, 1);
    }
    _ = result;
}
fn bench_trit_power_latency() void {
    // bench: bench_trit_power_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_power(.neg, 2);
    }
    _ = result;
}
fn bench_trit_from_bool_latency() void {
    // bench: bench_trit_from_bool_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_from_bool(true);
    }
    _ = result;
}
fn bench_trit_to_bool_latency() void {
    // bench: bench_trit_to_bool_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_to_bool(.pos);
    }
    _ = result;
}
fn bench_trit_abs_diff_latency() void {
    // bench: bench_trit_abs_diff_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_abs_diff(.pos, .neg);
    }
    _ = result;
}
fn bench_trit_cond_swap_latency() void {
    // bench: bench_trit_cond_swap_latency
    @setEvalBranchQuota(10000);
    var result: Trit = .zero;
    for (0 .. 1000) {
        result = trit_cond_swap(.pos, .neg, .zero);
    }
    _ = result;
}
fn bench_trit_is_unit_latency() void {
    // bench: bench_trit_is_unit_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_is_unit(.pos);
    }
    _ = result;
}
fn bench_trit_is_identity_latency() void {
    // bench: bench_trit_is_identity_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_is_identity(.zero);
    }
    _ = result;
}
fn bench_trit_is_negated_latency() void {
    // bench: bench_trit_is_negated_latency
    @setEvalBranchQuota(10000);
    var result: bool = false;
    for (0 .. 1000) {
        result = trit_is_negated(.neg, .pos);
    }
    _ = result;
}
