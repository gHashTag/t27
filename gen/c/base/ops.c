/* ============================================================================
   Generated from t27 spec: tritype-ops
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>

#ifndef TRITYPE_OPS_H
#define TRITYPE_OPS_H

/* -------------------------------------------------------
   Constants
   ------------------------------------------------------- */

#define NEGONE -1
#define ZERO 0
#define ONE 1
typedef  mult_table[1,0,-1,0,0,0,-1,0,1];
typedef  add_table[-1,-1,0,-1,0,1,0,1,1];
typedef  carry_table[1,0,0,0,0,0,0,0,-1];

/* -------------------------------------------------------
   Enums
   ------------------------------------------------------- */

typedef enum {
    TRIT_NEG = -1,
    TRIT_ZERO = 0,
    TRIT_POS = 1
} Trit;


/* -------------------------------------------------------
   Structs
   ------------------------------------------------------- */

typedef struct {
    Trit result;
    Trit carry_out;
} AddResult;


/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

Trit trit_multiply_table(Trit a, Trit b);
Trit trit_add_table(Trit a, Trit b);
Trit trit_carry_table(Trit a, Trit b);
AddResult trit_add_with_carry(Trit a, Trit b, Trit carry_in);
int8_t trit_compare(Trit a, Trit b);
Trit trit_negate(Trit a);
Trit trit_abs(Trit a);
Trit trit_min(Trit a, Trit b);
Trit trit_max(Trit a, Trit b);
Trit trit_subtract(Trit a, Trit b);
int8_t trit_sign(Trit a);
Trit trit_clamp(Trit a, Trit min_val, Trit max_val);
bool trit_is_negative(Trit a);
bool trit_is_zero(Trit a);
bool trit_is_positive(Trit a);
bool trit_equal(Trit a, Trit b);
bool trit_not_equal(Trit a, Trit b);
bool trit_lt(Trit a, Trit b);
bool trit_le(Trit a, Trit b);
bool trit_gt(Trit a, Trit b);
bool trit_ge(Trit a, Trit b);
AddResult trit_multiply_with_carry(Trit a, Trit b, Trit carry_in);
Trit trit_reverse(Trit a);
Trit trit_multiply_by_power_of_two(Trit a, uint8_t power);
Trit trit_power(Trit a, uint8_t n);
Trit trit_from_bool(bool b);
bool trit_to_bool(Trit a);
Trit trit_abs_diff(Trit a, Trit b);
Trit trit_cond_swap(Trit cond, Trit a, Trit b);
bool trit_is_unit(Trit a);
bool trit_is_identity(Trit a);
bool trit_is_negated(Trit a, Trit b);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

Trit trit_multiply_table(Trit a, Trit b) {
    int idx = (((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1));
    return (Trit)(@enumFromInt(mult_table[idx]));
}

Trit trit_add_table(Trit a, Trit b) {
    int idx = (((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1));
    return (Trit)(@enumFromInt(add_table[idx]));
}

Trit trit_carry_table(Trit a, Trit b) {
    int idx = (((@intFromEnum(a) + 1) * 3) + (@intFromEnum(b) + 1));
    return (Trit)(@enumFromInt(carry_table[idx]));
}

AddResult trit_add_with_carry(Trit a, Trit b, Trit carry_in) {
    int8_t sum = (@intFromEnum(a) + @intFromEnum(b));
    Trit carry = ZERO;
    Trit result = ZERO;
    if ((sum > 1)) {
        result = NEG;
        carry = POS;
    } else if ((sum < -1)) {
        result = POS;
        carry = NEG;
    } else {
        result = (Trit)(@enumFromInt(sum));
    }
    sum = (@intFromEnum(result) + @intFromEnum(carry_in));
    if ((sum > 1)) {
        result = NEG;
        carry = POS;
    } else if ((sum < -1)) {
        result = POS;
        carry = NEG;
    } else {
        result = (Trit)(@enumFromInt(sum));
        carry = ZERO;
    }
    return (AddResult){ .result = result, .carry_out = carry };
}

int8_t trit_compare(Trit a, Trit b) {
    if ((a == b)) {
        return 0;
    } else if (((a == NEG) || ((a == ZERO) && (b == POS)))) {
        return -1;
    } else {
        return 1;
    }
}

Trit trit_negate(Trit a) {
    return (a == NEG) ? (POS) : (a == ZERO) ? (ZERO) : (a == POS) ? (NEG) : 0;
}

Trit trit_abs(Trit a) {
    return ((a == NEG)) ? (POS) : (a);
}

Trit trit_min(Trit a, Trit b) {
    return (((a == NEG) || ((a == ZERO) && (b == POS)))) ? (a) : (b);
}

Trit trit_max(Trit a, Trit b) {
    return (((a == POS) || ((a == ZERO) && (b == NEG)))) ? (a) : (b);
}

Trit trit_subtract(Trit a, Trit b) {
    return trit_add_table(a, trit_negate(b));
}

int8_t trit_sign(Trit a) {
    return @intFromEnum(a);
}

Trit trit_clamp(Trit a, Trit min_val, Trit max_val) {
    if ((trit_compare(a, min_val) < 0)) {
        return min_val;
    }
    if ((trit_compare(a, max_val) > 0)) {
        return max_val;
    }
    return a;
}

bool trit_is_negative(Trit a) {
    return (a == NEG);
}

bool trit_is_zero(Trit a) {
    return (a == ZERO);
}

bool trit_is_positive(Trit a) {
    return (a == POS);
}

bool trit_equal(Trit a, Trit b) {
    return (a == b);
}

bool trit_not_equal(Trit a, Trit b) {
    return (a != b);
}

bool trit_lt(Trit a, Trit b) {
    return (trit_compare(a, b) < 0);
}

bool trit_le(Trit a, Trit b) {
    return (trit_compare(a, b) <= 0);
}

bool trit_gt(Trit a, Trit b) {
    return (trit_compare(a, b) > 0);
}

bool trit_ge(Trit a, Trit b) {
    return (trit_compare(a, b) >= 0);
}

AddResult trit_multiply_with_carry(Trit a, Trit b, Trit carry_in) {
    int product = trit_multiply_table(a, b);
    return trit_add_with_carry(product, carry_in, ZERO);
}

Trit trit_reverse(Trit a) {
    return ((a == ZERO)) ? (ZERO) : (a);
}

Trit trit_multiply_by_power_of_two(Trit a, uint8_t power) {
    int result = a;
    uint8_t i = 1;
    while ((i < power)) {
        int carry = trit_carry_table(result, a);
        if ((carry != ZERO)) {
            result = ((carry == POS)) ? (POS) : (NEG);
        }
        result = trit_add_table(result, a);
        i += 1;
    }
    return result;
}

Trit trit_power(Trit a, uint8_t n) {
    if ((n == 0)) {
        return POS;
    }
    if ((n == 1)) {
        return a;
    }
    if ((a == ZERO)) {
        return ZERO;
    }
    if ((a == POS)) {
        return POS;
    }
    return (((n % 2) == 1)) ? (NEG) : (POS);
}

Trit trit_from_bool(bool b) {
    return (b) ? (POS) : (ZERO);
}

bool trit_to_bool(Trit a) {
    return (a == POS);
}

Trit trit_abs_diff(Trit a, Trit b) {
    return ((a == b)) ? (ZERO) : (POS);
}

Trit trit_cond_swap(Trit cond, Trit a, Trit b) {
    return ((cond == POS)) ? (b) : (a);
}

bool trit_is_unit(Trit a) {
    return (a == POS);
}

bool trit_is_identity(Trit a) {
    return (a == ZERO);
}

bool trit_is_negated(Trit a, Trit b) {
    return (b == trit_negate(a));
}

/* -------------------------------------------------------
   Invariants (compile-time assertions)
   ------------------------------------------------------- */

/* invariant: trit_subtract_add_negation_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_subtract_add_negation_identity");

/* invariant: trit_sign_matches_enum_value */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_sign_matches_enum_value");

/* invariant: trit_clamp_preserves_order */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_clamp_preserves_order");

/* invariant: trit_clamp_idempotent */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_clamp_idempotent");

/* invariant: trit_is_negative_matches_sign */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_negative_matches_sign");

/* invariant: trit_is_zero_matches_sign */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_zero_matches_sign");

/* invariant: trit_is_positive_matches_sign */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_positive_matches_sign");

/* invariant: trit_equal_reflexive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_equal_reflexive");

/* invariant: trit_equal_symmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_equal_symmetric");

/* invariant: trit_not_equal_complement */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_not_equal_complement");

/* invariant: trit_lt_implies_le */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_lt_implies_le");

/* invariant: trit_gt_implies_ge */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_gt_implies_ge");

/* invariant: trit_lt_and_gt_mutually_exclusive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_lt_and_gt_mutually_exclusive");

/* invariant: trit_le_and_ge_mutually_inclusive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_le_and_ge_mutually_inclusive");

/* invariant: trit_lt_gt_antisymmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_lt_gt_antisymmetric");

/* invariant: trit_le_ge_antisymmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_le_ge_antisymmetric");

/* invariant: trit_multiply_with_carry_no_overflow_for_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_multiply_with_carry_no_overflow_for_zero");

/* invariant: trit_reverse_self_inverse_nonzero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_reverse_self_inverse_nonzero");

/* invariant: trit_reverse_zero_no_inverse */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_reverse_zero_no_inverse");

/* invariant: trit_multiply_by_power_of_two_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_multiply_by_power_of_two_identity");

/* invariant: trit_multiply_by_power_of_two_zero_annihilates */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_multiply_by_power_of_two_zero_annihilates");

/* invariant: trit_multiply_table_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_multiply_table_commutative");

/* invariant: trit_add_table_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_table_commutative");

/* invariant: trit_add_table_identity_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_table_identity_zero");

/* invariant: trit_carry_table_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_carry_table_commutative");

/* invariant: trit_carry_table_neg_plus_neg_gives_positive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_carry_table_neg_plus_neg_gives_positive");

/* invariant: trit_carry_table_pos_plus_pos_gives_negative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_carry_table_pos_plus_pos_gives_negative");

/* invariant: trit_add_with_carry_result_in_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_with_carry_result_in_range");

/* invariant: trit_add_with_carry_carry_in_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_with_carry_carry_in_range");

/* invariant: trit_compare_total_ordering */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_compare_total_ordering");

/* invariant: trit_negate_double_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_negate_double_identity");

/* invariant: trit_abs_result_non_negative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_abs_result_non_negative");

/* invariant: trit_min_idempotent */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_min_idempotent");

/* invariant: trit_max_idempotent */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_max_idempotent");

/* invariant: trit_min_max_antisymmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_min_max_antisymmetric");

/* invariant: trit_power_zero_exponent_returns_unit */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_power_zero_exponent_returns_unit");

/* invariant: trit_power_zero_base_returns_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_power_zero_base_returns_zero");

/* invariant: trit_power_pos_always_pos */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_power_pos_always_pos");

/* invariant: trit_from_bool_to_bool_roundtrip */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_from_bool_to_bool_roundtrip");

/* invariant: trit_abs_diff_non_negative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_abs_diff_non_negative");

/* invariant: trit_abs_diff_zero_iff_equal */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_abs_diff_zero_iff_equal");

/* invariant: trit_abs_diff_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_abs_diff_commutative");

/* invariant: trit_cond_swap_condition_true_swaps */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_cond_swap_condition_true_swaps");

/* invariant: trit_cond_swap_condition_false_keeps_first */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_cond_swap_condition_false_keeps_first");

/* invariant: trit_is_unit_only_for_pos */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_unit_only_for_pos");

/* invariant: trit_is_identity_only_for_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_identity_only_for_zero");

/* invariant: trit_is_negated_symmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_negated_symmetric");

/* invariant: trit_is_negated_zero_self */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_is_negated_zero_self");


/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_test_trit_multiply_table_all_combinations(void) {
    assert((Trit)(POS) == trit_multiply_table(NEG, NEG));
    assert((Trit)(ZERO) == trit_multiply_table(NEG, ZERO));
    assert((Trit)(NEG) == trit_multiply_table(NEG, POS));
    assert((Trit)(ZERO) == trit_multiply_table(ZERO, NEG));
    assert((Trit)(ZERO) == trit_multiply_table(ZERO, ZERO));
    assert((Trit)(ZERO) == trit_multiply_table(ZERO, POS));
    assert((Trit)(NEG) == trit_multiply_table(POS, NEG));
    assert((Trit)(ZERO) == trit_multiply_table(POS, ZERO));
    assert((Trit)(POS) == trit_multiply_table(POS, POS));
}

void test_test_trit_multiply_table_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_multiply_table(a, b) == trit_multiply_table(b, a));
        }
    }
}

void test_test_trit_add_table_neg_plus_neg(void) {
    assert((Trit)(NEG) == trit_add_table(NEG, NEG));
}

void test_test_trit_add_table_neg_plus_zero(void) {
    assert((Trit)(NEG) == trit_add_table(NEG, ZERO));
}

void test_test_trit_add_table_neg_plus_pos(void) {
    assert((Trit)(ZERO) == trit_add_table(NEG, POS));
}

void test_test_trit_add_table_zero_plus_zero(void) {
    assert((Trit)(ZERO) == trit_add_table(ZERO, ZERO));
}

void test_test_trit_add_table_zero_plus_pos(void) {
    assert((Trit)(POS) == trit_add_table(ZERO, POS));
}

void test_test_trit_add_table_pos_plus_pos(void) {
    assert((Trit)(POS) == trit_add_table(POS, POS));
}

void test_test_trit_add_table_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_add_table(a, b) == trit_add_table(b, a));
        }
    }
}

void test_test_trit_add_table_identity_zero(void) {
    assert((Trit)(NEG) == trit_add_table(NEG, ZERO));
    assert((Trit)(ZERO) == trit_add_table(ZERO, ZERO));
    assert((Trit)(POS) == trit_add_table(POS, ZERO));
}

void test_test_trit_carry_table_neg_plus_neg(void) {
    assert((Trit)(POS) == trit_carry_table(NEG, NEG));
}

void test_test_trit_carry_table_neg_plus_zero(void) {
    assert((Trit)(ZERO) == trit_carry_table(NEG, ZERO));
}

void test_test_trit_carry_table_neg_plus_pos(void) {
    assert((Trit)(ZERO) == trit_carry_table(NEG, POS));
}

void test_test_trit_carry_table_zero_plus_zero(void) {
    assert((Trit)(ZERO) == trit_carry_table(ZERO, ZERO));
}

void test_test_trit_carry_table_zero_plus_pos(void) {
    assert((Trit)(ZERO) == trit_carry_table(ZERO, POS));
}

void test_test_trit_carry_table_pos_plus_pos(void) {
    assert((Trit)(NEG) == trit_carry_table(POS, POS));
}

void test_test_trit_carry_table_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_carry_table(a, b) == trit_carry_table(b, a));
        }
    }
}

void test_test_trit_add_with_carry_no_carry(void) {
    int result = trit_add_with_carry(POS, NEG, ZERO);
    assert((Trit)(ZERO) == result.result);
    assert((Trit)(ZERO) == result.carry_out);
}

void test_test_trit_add_with_carry_positive_overflow(void) {
    int result = trit_add_with_carry(POS, POS, ZERO);
    assert((Trit)(NEG) == result.result);
    assert((Trit)(POS) == result.carry_out);
}

void test_test_trit_add_with_carry_negative_overflow(void) {
    int result = trit_add_with_carry(NEG, NEG, ZERO);
    assert((Trit)(POS) == result.result);
    assert((Trit)(NEG) == result.carry_out);
}

void test_test_trit_add_with_carry_propagation(void) {
    int result = trit_add_with_carry(POS, POS, POS);
    assert((Trit)(ZERO) == result.result);
    assert((Trit)(POS) == result.carry_out);
}

void test_test_trit_add_with_carry_result_in_range(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            /* for-each loop (see t27 source) */
            {
                int result = trit_add_with_carry(a, b, c);
                assert(((@intFromEnum(result.result) >= -1) && (@intFromEnum(result.result) <= 1)));
            }
        }
    }
}

void test_test_trit_add_with_carry_carry_in_range(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            /* for-each loop (see t27 source) */
            {
                int result = trit_add_with_carry(a, b, c);
                assert(((@intFromEnum(result.carry_out) >= -1) && (@intFromEnum(result.carry_out) <= 1)));
            }
        }
    }
}

void test_test_trit_compare_less_than(void) {
    assert((i8)(-1) == trit_compare(NEG, ZERO));
    assert((i8)(-1) == trit_compare(NEG, POS));
    assert((i8)(-1) == trit_compare(ZERO, POS));
}

void test_test_trit_compare_equal(void) {
    assert((i8)(0) == trit_compare(NEG, NEG));
    assert((i8)(0) == trit_compare(ZERO, ZERO));
    assert((i8)(0) == trit_compare(POS, POS));
}

void test_test_trit_compare_greater_than(void) {
    assert((i8)(1) == trit_compare(POS, ZERO));
    assert((i8)(1) == trit_compare(POS, NEG));
    assert((i8)(1) == trit_compare(ZERO, NEG));
}

void test_test_trit_compare_total_ordering(void) {
    assert((i8)(-1) == trit_compare(NEG, ZERO));
    assert((i8)(-1) == trit_compare(ZERO, POS));
    assert((i8)(-1) == trit_compare(NEG, POS));
}

void test_test_trit_negate_involutive(void) {
    assert(NEG == trit_negate(trit_negate(NEG)));
    assert(ZERO == trit_negate(trit_negate(ZERO)));
    assert(POS == trit_negate(trit_negate(POS)));
}

void test_test_trit_abs_non_negative(void) {
    assert((Trit)(POS) == trit_abs(NEG));
    assert((Trit)(ZERO) == trit_abs(ZERO));
    assert((Trit)(POS) == trit_abs(POS));
}

void test_test_trit_min_returns_minimum(void) {
    assert((Trit)(NEG) == trit_min(NEG, POS));
    assert((Trit)(NEG) == trit_min(NEG, ZERO));
    assert((Trit)(ZERO) == trit_min(ZERO, POS));
    assert((Trit)(NEG) == trit_min(POS, NEG));
}

void test_test_trit_max_returns_maximum(void) {
    assert((Trit)(POS) == trit_max(NEG, POS));
    assert((Trit)(ZERO) == trit_max(NEG, ZERO));
    assert((Trit)(POS) == trit_max(ZERO, POS));
    assert((Trit)(POS) == trit_max(POS, NEG));
}

void test_test_trit_min_idempotent(void) {
    assert(NEG == trit_min(NEG, NEG));
    assert(ZERO == trit_min(ZERO, ZERO));
    assert(POS == trit_min(POS, POS));
}

void test_test_trit_max_idempotent(void) {
    assert(NEG == trit_max(NEG, NEG));
    assert(ZERO == trit_max(ZERO, ZERO));
    assert(POS == trit_max(POS, POS));
}

void test_test_trit_min_max_antisymmetric(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_min(a, b) == trit_max(b, a));
        }
    }
}

void test_test_trit_subtract_neg_from_pos(void) {
    assert((Trit)(POS) == trit_subtract(POS, NEG));
}

void test_test_trit_subtract_pos_from_pos(void) {
    assert((Trit)(ZERO) == trit_subtract(POS, POS));
}

void test_test_trit_subtract_neg_from_neg(void) {
    assert((Trit)(ZERO) == trit_subtract(NEG, NEG));
}

void test_test_trit_subtract_zero_from_pos(void) {
    assert((Trit)(POS) == trit_subtract(POS, ZERO));
}

void test_test_trit_subtract_zero_from_neg(void) {
    assert((Trit)(NEG) == trit_subtract(NEG, ZERO));
}

void test_test_trit_subtract_all_combinations(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_subtract(a, b) == trit_add_table(a, trit_negate(b)));
        }
    }
}

void test_test_trit_sign_negative(void) {
    assert((i8)(-1) == trit_sign(NEG));
}

void test_test_trit_sign_zero(void) {
    assert((i8)(0) == trit_sign(ZERO));
}

void test_test_trit_sign_positive(void) {
    assert((i8)(1) == trit_sign(POS));
}

void test_test_trit_sign_matches_enum_value(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(@intFromEnum(a) == trit_sign(a));
    }
}

void test_test_trit_clamp_below_min(void) {
    assert((Trit)(ZERO) == trit_clamp(NEG, ZERO, POS));
}

void test_test_trit_clamp_above_max(void) {
    assert((Trit)(ZERO) == trit_clamp(POS, NEG, ZERO));
}

void test_test_trit_clamp_in_range(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(a == trit_clamp(a, NEG, POS));
    }
}

void test_test_trit_clamp_at_bounds(void) {
    assert(ZERO == trit_clamp(ZERO, ZERO, POS));
    assert(POS == trit_clamp(POS, ZERO, POS));
}

void test_test_trit_is_negative_true_for_neg(void) {
    assert(trit_is_negative(NEG));
}

void test_test_trit_is_negative_false_for_others(void) {
    assert(!trit_is_negative(ZERO));
    assert(!trit_is_negative(POS));
}

void test_test_trit_is_zero_true_for_zero(void) {
    assert(trit_is_zero(ZERO));
}

void test_test_trit_is_zero_false_for_others(void) {
    assert(!trit_is_zero(NEG));
    assert(!trit_is_zero(POS));
}

void test_test_trit_is_positive_true_for_pos(void) {
    assert(trit_is_positive(POS));
}

void test_test_trit_is_positive_false_for_others(void) {
    assert(!trit_is_positive(NEG));
    assert(!trit_is_positive(ZERO));
}

void test_test_trit_equal_same_values(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(trit_equal(a, a));
    }
}

void test_test_trit_equal_different_values_false(void) {
    assert(!trit_equal(NEG, ZERO));
    assert(!trit_equal(ZERO, POS));
    assert(!trit_equal(POS, NEG));
}

void test_test_trit_not_equal_same_values_false(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(!trit_not_equal(a, a));
    }
}

void test_test_trit_not_equal_different_values_true(void) {
    assert(trit_not_equal(NEG, ZERO));
    assert(trit_not_equal(ZERO, POS));
    assert(trit_not_equal(POS, NEG));
}

void test_test_trit_lt_less_than(void) {
    assert(trit_lt(NEG, ZERO));
    assert(trit_lt(NEG, POS));
    assert(trit_lt(ZERO, POS));
}

void test_test_trit_lt_false_for_equal_or_greater(void) {
    assert(!trit_lt(NEG, NEG));
    assert(!trit_lt(ZERO, NEG));
    assert(!trit_lt(POS, POS));
}

void test_test_trit_le_less_than_or_equal(void) {
    assert(trit_le(NEG, ZERO));
    assert(trit_le(NEG, POS));
    assert(trit_le(ZERO, POS));
    assert(trit_le(NEG, NEG));
    assert(trit_le(ZERO, ZERO));
    assert(trit_le(POS, POS));
}

void test_test_trit_gt_greater_than(void) {
    assert(trit_gt(POS, ZERO));
    assert(trit_gt(POS, NEG));
    assert(trit_gt(ZERO, NEG));
}

void test_test_trit_ge_greater_than_or_equal(void) {
    assert(trit_ge(POS, ZERO));
    assert(trit_ge(POS, NEG));
    assert(trit_ge(ZERO, NEG));
    assert(trit_ge(NEG, NEG));
    assert(trit_ge(ZERO, ZERO));
    assert(trit_ge(POS, POS));
}

void test_test_trit_comparison_consistency(void) {
    Trit a = NEG;
    Trit b = POS;
    assert(trit_lt(a, b));
    assert(trit_le(a, b));
    assert(!trit_gt(a, b));
    assert(!trit_ge(a, b));
}

void test_test_trit_multiply_with_carry_basic(void) {
    int result = trit_multiply_with_carry(POS, POS, ZERO);
    assert((Trit)(NEG) == result.result);
    assert((Trit)(POS) == result.carry_out);
}

void test_test_trit_multiply_with_carry_with_carry(void) {
    int result = trit_multiply_with_carry(POS, POS, POS);
    assert((Trit)(ZERO) == result.result);
    assert((Trit)(POS) == result.carry_out);
}

void test_test_trit_multiply_with_carry_zero_annihilates(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        int result = trit_multiply_with_carry(ZERO, t, ZERO);
        assert((Trit)(ZERO) == result.result);
        assert((Trit)(ZERO) == result.carry_out);
    }
}

void test_test_trit_reverse_non_zero(void) {
    assert((Trit)(POS) == trit_reverse(POS));
    assert((Trit)(NEG) == trit_reverse(NEG));
}

void test_test_trit_reverse_zero(void) {
    assert((Trit)(ZERO) == trit_reverse(ZERO));
}

void test_test_trit_multiply_by_power_of_two_zero(void) {
    assert((Trit)(ZERO) == trit_multiply_by_power_of_two(ZERO, 3));
}

void test_test_trit_multiply_by_power_of_two_one(void) {
    assert((Trit)(POS) == trit_multiply_by_power_of_two(POS, 0));
}

void test_test_trit_multiply_by_power_of_two_power_one(void) {
    int result = trit_multiply_by_power_of_two(POS, 1);
    assert(((result == NEG) || (result == POS)));
}

void test_test_trit_multiply_by_power_of_two_identity(void) {
    assert(NEG == trit_multiply_by_power_of_two(NEG, 0));
    assert(ZERO == trit_multiply_by_power_of_two(ZERO, 0));
    assert(POS == trit_multiply_by_power_of_two(POS, 0));
}

void test_test_trit_power_zero_exponent(void) {
    assert(POS == trit_power(NEG, 0));
    assert(POS == trit_power(POS, 0));
}

void test_test_trit_power_zero_base(void) {
    assert(ZERO == trit_power(ZERO, 1));
    assert(ZERO == trit_power(ZERO, 2));
    assert(ZERO == trit_power(ZERO, 3));
}

void test_test_trit_power_one(void) {
    assert(NEG == trit_power(NEG, 1));
    assert(ZERO == trit_power(ZERO, 1));
    assert(POS == trit_power(POS, 1));
}

void test_test_trit_power_square(void) {
    assert(POS == trit_power(NEG, 2));
    assert(ZERO == trit_power(ZERO, 2));
    assert(POS == trit_power(POS, 2));
}

void test_test_trit_power_cube(void) {
    assert(NEG == trit_power(NEG, 3));
    assert(ZERO == trit_power(ZERO, 3));
    assert(POS == trit_power(POS, 3));
}

void test_test_trit_from_bool_true(void) {
    assert(POS == trit_from_bool(true));
}

void test_test_trit_from_bool_false(void) {
    assert(ZERO == trit_from_bool(false));
}

void test_test_trit_to_bool_positive(void) {
    assert(trit_to_bool(POS));
}

void test_test_trit_to_bool_non_positive(void) {
    assert(!trit_to_bool(ZERO));
    assert(!trit_to_bool(NEG));
}

void test_test_trit_to_bool_from_bool_roundtrip(void) {
    assert(trit_to_bool(trit_from_bool(true)) == true);
    assert(trit_to_bool(trit_from_bool(false)) == false);
}

void test_test_trit_abs_diff_equal(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(ZERO == trit_abs_diff(a, a));
    }
}

void test_test_trit_abs_diff_different(void) {
    assert(POS == trit_abs_diff(NEG, ZERO));
    assert(POS == trit_abs_diff(NEG, POS));
    assert(POS == trit_abs_diff(ZERO, POS));
}

void test_test_trit_abs_diff_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_abs_diff(a, b) == trit_abs_diff(b, a));
        }
    }
}

void test_test_trit_cond_swap_condition_true(void) {
    assert(NEG == trit_cond_swap(POS, NEG, ZERO));
    assert(POS == trit_cond_swap(POS, ZERO, POS));
}

void test_test_trit_cond_swap_condition_false(void) {
    assert(NEG == trit_cond_swap(ZERO, NEG, POS));
    assert(POS == trit_cond_swap(NEG, POS, NEG));
}

void test_test_trit_is_unit_true_for_pos(void) {
    assert(trit_is_unit(POS));
}

void test_test_trit_is_unit_false_for_others(void) {
    assert(!trit_is_unit(ZERO));
    assert(!trit_is_unit(NEG));
}

void test_test_trit_is_identity_true_for_zero(void) {
    assert(trit_is_identity(ZERO));
}

void test_test_trit_is_identity_false_for_others(void) {
    assert(!trit_is_identity(POS));
    assert(!trit_is_identity(NEG));
}

void test_test_trit_is_negated_true_pair(void) {
    assert(trit_is_negated(NEG, POS));
    assert(trit_is_negated(POS, NEG));
    assert(trit_is_negated(ZERO, ZERO));
}

void test_test_trit_is_negated_false_non_pair(void) {
    assert(!trit_is_negated(NEG, NEG));
    assert(!trit_is_negated(POS, POS));
    assert(!trit_is_negated(ZERO, POS));
}


/* -------------------------------------------------------
   Benchmarks
   ------------------------------------------------------- */

void bench_trit_multiply_table_latency(void) {
    /* bench: bench_trit_multiply_table_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_multiply_table(POS, NEG);
    }
    (void)result;
}

void bench_trit_add_table_latency(void) {
    /* bench: bench_trit_add_table_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_add_table(POS, NEG);
    }
    (void)result;
}

void bench_trit_carry_table_latency(void) {
    /* bench: bench_trit_carry_table_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_carry_table(POS, POS);
    }
    (void)result;
}

void bench_trit_add_with_carry_latency(void) {
    /* bench: bench_trit_add_with_carry_latency */
    /* @setEvalBranchQuota(10000) */;
    int result = trit_add_with_carry(POS, POS, ZERO);
    /* for-each loop (see t27 source) */
    {
        result = trit_add_with_carry(POS, POS, ZERO);
    }
    (void)result;
}

void bench_trit_compare_latency(void) {
    /* bench: bench_trit_compare_latency */
    /* @setEvalBranchQuota(10000) */;
    int8_t result = 0;
    /* for-each loop (see t27 source) */
    {
        result = trit_compare(POS, NEG);
    }
    (void)result;
}

void bench_trit_min_max_latency(void) {
    /* bench: bench_trit_min_max_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_min(POS, NEG);
    }
    (void)result;
}

void bench_trit_lt_latency(void) {
    /* bench: bench_trit_lt_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_lt(NEG, POS);
    }
    (void)result;
}

void bench_trit_le_latency(void) {
    /* bench: bench_trit_le_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_le(NEG, POS);
    }
    (void)result;
}

void bench_trit_gt_latency(void) {
    /* bench: bench_trit_gt_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_gt(POS, NEG);
    }
    (void)result;
}

void bench_trit_ge_latency(void) {
    /* bench: bench_trit_ge_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_ge(POS, NEG);
    }
    (void)result;
}

void bench_trit_multiply_with_carry_latency(void) {
    /* bench: bench_trit_multiply_with_carry_latency */
    /* @setEvalBranchQuota(10000) */;
    int result = trit_multiply_with_carry(POS, POS, ZERO);
    /* for-each loop (see t27 source) */
    {
        result = trit_multiply_with_carry(POS, POS, ZERO);
    }
    (void)result;
}

void bench_trit_reverse_latency(void) {
    /* bench: bench_trit_reverse_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_reverse(POS);
    }
    (void)result;
}

void bench_trit_multiply_by_power_of_two_latency(void) {
    /* bench: bench_trit_multiply_by_power_of_two_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_multiply_by_power_of_two(POS, 1);
    }
    (void)result;
}

void bench_trit_power_latency(void) {
    /* bench: bench_trit_power_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_power(NEG, 2);
    }
    (void)result;
}

void bench_trit_from_bool_latency(void) {
    /* bench: bench_trit_from_bool_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_from_bool(true);
    }
    (void)result;
}

void bench_trit_to_bool_latency(void) {
    /* bench: bench_trit_to_bool_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_to_bool(POS);
    }
    (void)result;
}

void bench_trit_abs_diff_latency(void) {
    /* bench: bench_trit_abs_diff_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_abs_diff(POS, NEG);
    }
    (void)result;
}

void bench_trit_cond_swap_latency(void) {
    /* bench: bench_trit_cond_swap_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_cond_swap(POS, NEG, ZERO);
    }
    (void)result;
}

void bench_trit_is_unit_latency(void) {
    /* bench: bench_trit_is_unit_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_is_unit(POS);
    }
    (void)result;
}

void bench_trit_is_identity_latency(void) {
    /* bench: bench_trit_is_identity_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_is_identity(ZERO);
    }
    (void)result;
}

void bench_trit_is_negated_latency(void) {
    /* bench: bench_trit_is_negated_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    /* for-each loop (see t27 source) */
    {
        result = trit_is_negated(NEG, POS);
    }
    (void)result;
}


#endif /* TRITYPE_OPS_H */
