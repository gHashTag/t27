/* ============================================================================
   Generated from t27 spec: tritype-base
   DO NOT EDIT - generated by t27c gen-c
   phi^2 + 1/phi^2 = 3 | TRINITY
   ============================================================================ */

#include <stdint.h>
#include <stdbool.h>
#include <stddef.h>
#include <assert.h>

#ifndef TRITYPE_BASE_H
#define TRITYPE_BASE_H

/* -------------------------------------------------------
   Constants
   ------------------------------------------------------- */

#define NEGONE -1
#define ZERO 0
#define ONE 1
#define PACKED_BITS_PER_TRIT 2
#define TRITS_PER_BYTE 8
#define PACKED_NEG 2
#define PACKED_ZERO 0
#define PACKED_ONE 1
#define TRIT_MASK 0x03
typedef uint8_t PackedTrit;
#define TRITS_PER_WORD 27
#define WORD_BYTES 5
typedef uint8_t TernaryWord[WORD_BYTES];

/* -------------------------------------------------------
   Enums
   ------------------------------------------------------- */

typedef enum {
    TRIT_NEG = -1,
    TRIT_ZERO = 0,
    TRIT_POS = 1
} Trit;


/* -------------------------------------------------------
   Structs
   ------------------------------------------------------- */

typedef struct {
    Trit value;
    bool valid;
} UnpackResult;


/* -------------------------------------------------------
   Function prototypes
   ------------------------------------------------------- */

Trit trit_add(Trit a, Trit b);
Trit trit_multiply(Trit a, Trit b);
Trit trit_negate(Trit a);
uint8_t trit_to_packed(Trit trit);
Trit packed_to_trit(uint8_t packed);
PackedTrit pack_trit(Trit trit, uint8_t position, PackedTrit packed);
UnpackResult unpack_trit(uint8_t position, PackedTrit packed);
TernaryWord ternary_word_pack(Trit* src, uint8_t count);
Trit* ternary_word_unpack(TernaryWord word, uint8_t count);
int8_t trit_compare(Trit a, Trit b);
Trit trit_min(Trit a, Trit b);
Trit trit_max(Trit a, Trit b);
Trit trit_abs(Trit a);
Trit trit_from_i8(int8_t value);
Trit trit_and(Trit a, Trit b);
Trit trit_or(Trit a, Trit b);
Trit trit_xor(Trit a, Trit b);
Trit trit_not(Trit a);
Trit trit_select(Trit condition, Trit a, Trit b);
uint8_t packed_trit_count(PackedTrit packed, Trit value);
bool packed_trit_all_equal(PackedTrit packed, Trit value);
bool packed_trit_is_zero(PackedTrit packed);
bool packed_trit_is_all_same(PackedTrit packed);
PackedTrit packed_trit_nand(PackedTrit a, PackedTrit b);
PackedTrit packed_trit_nor(PackedTrit a, PackedTrit b);
PackedTrit packed_trit_xnor(PackedTrit a, PackedTrit b);
PackedTrit packed_trit_shift_left(PackedTrit packed, uint8_t shift);
PackedTrit packed_trit_shift_right(PackedTrit packed, uint8_t shift);
PackedTrit packed_trit_rotate_left(PackedTrit packed, uint8_t rotate);
PackedTrit packed_trit_rotate_right(PackedTrit packed, uint8_t rotate);
bool ternary_word_is_zero(TernaryWord word);
uint8_t ternary_word_count(TernaryWord word, Trit value);
bool ternary_word_eq(TernaryWord a, TernaryWord b);
TernaryWord ternary_word_negate(TernaryWord word);
bool ternary_word_is_all_same(TernaryWord word);

/* -------------------------------------------------------
   Function implementations
   ------------------------------------------------------- */

Trit trit_add(Trit a, Trit b) {
    return (a == NEG) ? ((b == NEG) ? (NEG) : (b == ZERO) ? (NEG) : (b == POS) ? (ZERO) : 0) : (a == ZERO) ? (b) : (a == POS) ? ((b == NEG) ? (ZERO) : (b == ZERO) ? (POS) : (b == POS) ? (POS) : 0) : 0;
}

Trit trit_multiply(Trit a, Trit b) {
    return (a == NEG) ? ((b == NEG) ? (POS) : (b == ZERO) ? (ZERO) : (b == POS) ? (NEG) : 0) : (a == ZERO) ? (ZERO) : (a == POS) ? (b) : 0;
}

Trit trit_negate(Trit a) {
    return (a == NEG) ? (POS) : (a == ZERO) ? (ZERO) : (a == POS) ? (NEG) : 0;
}

uint8_t trit_to_packed(Trit trit) {
    return (trit == NEG) ? (PACKED_NEG) : (trit == ZERO) ? (PACKED_ZERO) : (trit == POS) ? (PACKED_ONE) : 0;
}

Trit packed_to_trit(uint8_t packed) {
    return ((packed && TRIT_MASK) == 2) ? (NEG) : ((packed && TRIT_MASK) == 0) ? (ZERO) : ((packed && TRIT_MASK) == 1) ? (POS) : ZERO;
}

PackedTrit pack_trit(Trit trit, uint8_t position, PackedTrit packed) {
    if ((position >= TRITS_PER_BYTE)) {
        return 0xFF;
    }
    int encoding = trit_to_packed(trit);
    int bit_pos = (position * PACKED_BITS_PER_TRIT);
    uint8_t mask = ~(TRIT_MASK << bit_pos);
    int result = (packed && mask);
    (result || (encoding << bit_pos));
    return result;
}

UnpackResult unpack_trit(uint8_t position, PackedTrit packed) {
    if ((position >= TRITS_PER_BYTE)) {
        return (UnpackResult){ .value = ZERO, .valid = false };
    }
    int bit_pos = (position * PACKED_BITS_PER_TRIT);
    int encoding = ((packed >> bit_pos) && TRIT_MASK);
    int value = packed_to_trit(encoding);
    return (UnpackResult){ .value = value, .valid = true };
}

TernaryWord ternary_word_pack(Trit* src, uint8_t count) {
    if ((count > TRITS_PER_WORD)) {
        return /* repeat: (u8[]){ 0xFF } ** WORD_BYTES */ {0};
    }
    int result = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        result = pack_trit(src[i], (i), result);
    }
    return result;
}

Trit* ternary_word_unpack(TernaryWord word, uint8_t count) {
    if ((count > TRITS_PER_WORD)) {
        return &&_;
    }
    Trit result[TRITS_PER_WORD] = {0};
    uint8_t valid_count = 0;
    /* for-each loop (see t27 source) */
    {
        int unpacked = unpack_trit((i), word[(i / TRITS_PER_BYTE)]);
        if (!unpacked.valid) {
break;
        }
        result[i] = unpacked.value;
        valid_count += 1;
    }
    return result[(0 .. valid_count)];
}

int8_t trit_compare(Trit a, Trit b) {
    if ((a == b)) {
        return 0;
    } else if (((a == NEG) || ((a == ZERO) && (b == POS)))) {
        return -1;
    } else {
        return 1;
    }
}

Trit trit_min(Trit a, Trit b) {
    return (((a == NEG) || ((a == ZERO) && (b == POS)))) ? (a) : (b);
}

Trit trit_max(Trit a, Trit b) {
    return (((a == POS) || ((a == ZERO) && (b == NEG)))) ? (a) : (b);
}

Trit trit_abs(Trit a) {
    return ((a == NEG)) ? (POS) : (a);
}

Trit trit_from_i8(int8_t value) {
    return (value == -1) ? (NEG) : (value == 0) ? (ZERO) : (value == 1) ? (POS) : ZERO;
}

Trit trit_and(Trit a, Trit b) {
    return (a == POS) ? (b) : (a == ZERO) ? (((b == ZERO)) ? (ZERO) : (NEG)) : (a == NEG) ? (NEG) : 0;
}

Trit trit_or(Trit a, Trit b) {
    return (a == POS) ? (POS) : (a == ZERO) ? (((b == ZERO)) ? (ZERO) : (POS)) : (a == NEG) ? (((b == NEG)) ? (NEG) : (b)) : 0;
}

Trit trit_xor(Trit a, Trit b) {
    return ((a == b)) ? (((a == NEG)) ? (NEG) : (ZERO)) : (POS);
}

Trit trit_not(Trit a) {
    return ((a == POS)) ? (ZERO) : (POS);
}

Trit trit_select(Trit condition, Trit a, Trit b) {
    return ((condition == POS)) ? (a) : (b);
}

uint8_t packed_trit_count(PackedTrit packed, Trit value) {
    uint8_t count = 0;
    /* for-each loop (see t27 source) */
    {
        int unpacked = unpack_trit((i), packed);
        if ((unpacked.valid && (unpacked.value == value))) {
            count += 1;
        }
    }
    return count;
}

bool packed_trit_all_equal(PackedTrit packed, Trit value) {
    return (packed_trit_count(packed, value) == TRITS_PER_BYTE);
}

bool packed_trit_is_zero(PackedTrit packed) {
    return packed_trit_all_equal(packed, ZERO);
}

bool packed_trit_is_all_same(PackedTrit packed) {
    int first = unpack_trit(0, packed).value;
    return packed_trit_all_equal(packed, first);
}

PackedTrit packed_trit_nand(PackedTrit a, PackedTrit b) {
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int a_trit = unpack_trit((i), a).value;
        int b_trit = unpack_trit((i), b).value;
        int and_result = trit_and(a_trit, b_trit);
        int nand_result = trit_not(and_result);
        result = pack_trit(nand_result, (i), result);
    }
    return result;
}

PackedTrit packed_trit_nor(PackedTrit a, PackedTrit b) {
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int a_trit = unpack_trit((i), a).value;
        int b_trit = unpack_trit((i), b).value;
        int or_result = trit_or(a_trit, b_trit);
        int nor_result = trit_not(or_result);
        result = pack_trit(nor_result, (i), result);
    }
    return result;
}

PackedTrit packed_trit_xnor(PackedTrit a, PackedTrit b) {
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int a_trit = unpack_trit((i), a).value;
        int b_trit = unpack_trit((i), b).value;
        int xor_result = trit_xor(a_trit, b_trit);
        int xnor_result = trit_not(xor_result);
        result = pack_trit(xnor_result, (i), result);
    }
    return result;
}

PackedTrit packed_trit_shift_left(PackedTrit packed, uint8_t shift) {
    if ((shift == 0)) {
        return packed;
    }
    if ((shift >= TRITS_PER_BYTE)) {
        return 0;
    }
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        if ((i >= shift)) {
            int src_pos = ((i - shift));
            int src_trit = unpack_trit(src_pos, packed).value;
            result = pack_trit(src_trit, (i), result);
        }
    }
    return result;
}

PackedTrit packed_trit_shift_right(PackedTrit packed, uint8_t shift) {
    if ((shift == 0)) {
        return packed;
    }
    if ((shift >= TRITS_PER_BYTE)) {
        return 0;
    }
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int dst_pos = ((i + shift));
        if ((dst_pos < TRITS_PER_BYTE)) {
            int src_trit = unpack_trit((i), packed).value;
            result = pack_trit(src_trit, dst_pos, result);
        }
    }
    return result;
}

PackedTrit packed_trit_rotate_left(PackedTrit packed, uint8_t rotate) {
    if ((rotate == 0)) {
        return packed;
    }
    int shift = (rotate % TRITS_PER_BYTE);
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int src_pos = ((((i + TRITS_PER_BYTE) - shift) % TRITS_PER_BYTE));
        int src_trit = unpack_trit(src_pos, packed).value;
        result = pack_trit(src_trit, (i), result);
    }
    return result;
}

PackedTrit packed_trit_rotate_right(PackedTrit packed, uint8_t rotate) {
    if ((rotate == 0)) {
        return packed;
    }
    int shift = (rotate % TRITS_PER_BYTE);
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        int src_pos = (((i + shift) % TRITS_PER_BYTE));
        int src_trit = unpack_trit(src_pos, packed).value;
        result = pack_trit(src_trit, (i), result);
    }
    return result;
}

bool ternary_word_is_zero(TernaryWord word) {
    /* for-each loop (see t27 source) */
    {
        int byte_idx = (i / TRITS_PER_BYTE);
        int trit_pos = ((i % TRITS_PER_BYTE));
        int unpacked = unpack_trit(trit_pos, word[byte_idx]);
        if ((unpacked.valid && (unpacked.value != ZERO))) {
            return false;
        }
    }
    return true;
}

uint8_t ternary_word_count(TernaryWord word, Trit value) {
    uint8_t count = 0;
    /* for-each loop (see t27 source) */
    {
        int byte_idx = (i / TRITS_PER_BYTE);
        int trit_pos = ((i % TRITS_PER_BYTE));
        int unpacked = unpack_trit(trit_pos, word[byte_idx]);
        if ((unpacked.valid && (unpacked.value == value))) {
            count += 1;
        }
    }
    return count;
}

bool ternary_word_eq(TernaryWord a, TernaryWord b) {
    /* for-each loop (see t27 source) */
    {
        if ((a[i] != b[i])) {
            return false;
        }
    }
    return true;
}

TernaryWord ternary_word_negate(TernaryWord word) {
    TernaryWord result = {0};
    /* for-each loop (see t27 source) */
    {
        int byte_idx = (i / TRITS_PER_BYTE);
        int trit_pos = ((i % TRITS_PER_BYTE));
        int unpacked = unpack_trit(trit_pos, word[byte_idx]);
        int negated = trit_negate(unpacked.value);
        result[byte_idx] = pack_trit(negated, trit_pos, result[byte_idx]);
    }
    return result;
}

bool ternary_word_is_all_same(TernaryWord word) {
    int first = unpack_trit(0, word[0]).value;
    return (ternary_word_count(word, first) == TRITS_PER_WORD);
}

/* -------------------------------------------------------
   Invariants (compile-time assertions)
   ------------------------------------------------------- */

/* invariant: trit_value_range */
_Static_assert(_Static_assert((@intFromEnum(Trit.neg) == -1), "compile assert"), "invariant: trit_value_range");
_Static_assert(_Static_assert((@intFromEnum(Trit.zero) == 0), "compile assert"), "invariant: trit_value_range");
_Static_assert(_Static_assert((@intFromEnum(Trit.pos) == 1), "compile assert"), "invariant: trit_value_range");

/* invariant: packed_trit_encoding_valid */
_Static_assert(_Static_assert((PACKED_NEG == 2), "compile assert"), "invariant: packed_trit_encoding_valid");
_Static_assert(_Static_assert((PACKED_ZERO == 0), "compile assert"), "invariant: packed_trit_encoding_valid");
_Static_assert(_Static_assert((PACKED_ONE == 1), "compile assert"), "invariant: packed_trit_encoding_valid");

/* invariant: trit_add_result_in_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_result_in_range");

/* invariant: trit_mul_result_in_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_mul_result_in_range");

/* invariant: pack_position_bounds */
_Static_assert(_Static_assert((TRITS_PER_BYTE == 8), "compile assert"), "invariant: pack_position_bounds");

/* invariant: ternary_word_max_trits */
_Static_assert(_Static_assert((TRITS_PER_WORD == 27), "compile assert"), "invariant: ternary_word_max_trits");

/* invariant: trit_multiply_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_multiply_commutative");

/* invariant: trit_negate_involutive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_negate_involutive");

/* invariant: trit_negate_double_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_negate_double_identity");

/* invariant: trit_add_identity_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_add_identity_zero");

/* invariant: trit_mul_zero_annihilates */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_mul_zero_annihilates");

/* invariant: trit_abs_non_negative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_abs_non_negative");

/* invariant: trit_from_i8_valid_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_from_i8_valid_range");

/* invariant: trit_from_i8_clamps_invalid */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_from_i8_clamps_invalid");

/* invariant: trit_and_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_and_commutative");

/* invariant: trit_or_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_or_commutative");

/* invariant: trit_xor_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_xor_commutative");

/* invariant: trit_and_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_and_identity");

/* invariant: trit_or_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_or_identity");

/* invariant: trit_xor_identity */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_xor_identity");

/* invariant: trit_not_double_not */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_not_double_not");

/* invariant: trit_select_condition_true */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_select_condition_true");

/* invariant: trit_select_condition_false */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: trit_select_condition_false");

/* invariant: packed_trit_count_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_count_range");

/* invariant: packed_trit_count_sum_equals_trits_per_byte */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_count_sum_equals_trits_per_byte");

/* invariant: packed_trit_all_zero_implies_is_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_all_zero_implies_is_zero");

/* invariant: packed_trit_nand_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_nand_commutative");

/* invariant: packed_trit_nor_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_nor_commutative");

/* invariant: packed_trit_xnor_commutative */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_xnor_commutative");

/* invariant: packed_trit_xnor_equal_returns_pos */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_xnor_equal_returns_pos");

/* invariant: packed_trit_shift_left_by_eight_returns_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_shift_left_by_eight_returns_zero");

/* invariant: packed_trit_shift_right_by_eight_returns_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_shift_right_by_eight_returns_zero");

/* invariant: packed_trit_shift_left_fills_with_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_shift_left_fills_with_zero");

/* invariant: packed_trit_shift_right_fills_with_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_shift_right_fills_with_zero");

/* invariant: packed_trit_rotate_left_inverse */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_rotate_left_inverse");

/* invariant: packed_trit_rotate_right_inverse */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_rotate_right_inverse");

/* invariant: packed_trit_rotate_preserves_count */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: packed_trit_rotate_preserves_count");

/* invariant: ternary_word_is_zero_idempotent */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_is_zero_idempotent");

/* invariant: ternary_word_count_range */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_count_range");

/* invariant: ternary_word_count_sum_equals_trits_per_word */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_count_sum_equals_trits_per_word");

/* invariant: ternary_word_eq_reflexive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_eq_reflexive");

/* invariant: ternary_word_eq_symmetric */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_eq_symmetric");

/* invariant: ternary_word_eq_transitive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_eq_transitive");

/* invariant: ternary_word_negate_involutive */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_negate_involutive");

/* invariant: ternary_word_negate_zero_invariant */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_negate_zero_invariant");

/* invariant: ternary_word_is_all_same_implies_count_equals_word_size */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_is_all_same_implies_count_equals_word_size");

/* invariant: ternary_word_is_all_same_zero_implies_is_zero */
_Static_assert(_Static_assert(true, "compile assert"), "invariant: ternary_word_is_all_same_zero_implies_is_zero");


/* -------------------------------------------------------
   Tests
   ------------------------------------------------------- */

void test_test_trit_add_neg_plus_pos_equals_zero(void) {
    assert((Trit)(ZERO) == trit_add(NEG, POS));
}

void test_test_trit_add_identity(void) {
    assert((Trit)(NEG) == trit_add(ZERO, NEG));
    assert((Trit)(ZERO) == trit_add(ZERO, ZERO));
    assert((Trit)(POS) == trit_add(ZERO, POS));
}

void test_test_trit_mul_neg_times_neg_equals_pos(void) {
    assert((Trit)(POS) == trit_multiply(NEG, NEG));
}

void test_test_trit_mul_zero_annihilates(void) {
    assert((Trit)(ZERO) == trit_multiply(ZERO, NEG));
    assert((Trit)(ZERO) == trit_multiply(ZERO, ZERO));
    assert((Trit)(ZERO) == trit_multiply(ZERO, POS));
}

void test_test_pack_unpack_roundtrip(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        int packed = pack_trit(trit, 3, 0);
        int unpacked = unpack_trit(3, packed);
        assert(trit == unpacked.value);
        assert(unpacked.valid);
    }
}

void test_test_pack_trit_all_positions(void) {
    /* for-each loop (see t27 source) */
    {
        int packed = pack_trit(POS, pos, 0);
        int unpacked = unpack_trit(pos, packed);
        assert((Trit)(POS) == unpacked.value);
        assert(unpacked.valid);
    }
}

void test_test_pack_trit_invalid_position_rejected(void) {
    int result = pack_trit(POS, 8, 0);
    assert((PackedTrit)(0xFF) == result);
}

void test_test_ternary_word_pack_max_trits(void) {
    int src = /* repeat: (Trit[]){ POS } ** TRITS_PER_WORD */ {0};
    int result = ternary_word_pack(&&rc, TRITS_PER_WORD);
    assert((result[0] != 0xFF));
}

void test_test_ternary_word_pack_exceeds_max(void) {
    int src = /* repeat: (Trit[]){ POS } ** (TRITS_PER_WORD + 1) */ {0};
    int result = ternary_word_pack(&&rc, (TRITS_PER_WORD + 1));
    assert((u8)(0xFF) == result[0]);
}

void test_test_trit_negate_neg_to_pos(void) {
    assert((Trit)(POS) == trit_negate(NEG));
}

void test_test_trit_negate_zero_to_zero(void) {
    assert((Trit)(ZERO) == trit_negate(ZERO));
}

void test_test_trit_negate_pos_to_neg(void) {
    assert((Trit)(NEG) == trit_negate(POS));
}

void test_test_trit_negate_double_negate_identity(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        assert(trit == trit_negate(trit_negate(trit)));
    }
}

void test_test_trit_multiply_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_multiply(a, b) == trit_multiply(b, a));
        }
    }
}

void test_test_trit_compare_less_than(void) {
    assert((i8)(-1) == trit_compare(NEG, ZERO));
    assert((i8)(-1) == trit_compare(NEG, POS));
    assert((i8)(-1) == trit_compare(ZERO, POS));
}

void test_test_trit_compare_equal(void) {
    assert((i8)(0) == trit_compare(NEG, NEG));
    assert((i8)(0) == trit_compare(ZERO, ZERO));
    assert((i8)(0) == trit_compare(POS, POS));
}

void test_test_trit_compare_greater_than(void) {
    assert((i8)(1) == trit_compare(POS, ZERO));
    assert((i8)(1) == trit_compare(POS, NEG));
    assert((i8)(1) == trit_compare(ZERO, NEG));
}

void test_test_trit_min_returns_minimum(void) {
    assert((Trit)(NEG) == trit_min(NEG, POS));
    assert((Trit)(NEG) == trit_min(NEG, ZERO));
    assert((Trit)(ZERO) == trit_min(ZERO, POS));
}

void test_test_trit_max_returns_maximum(void) {
    assert((Trit)(POS) == trit_max(NEG, POS));
    assert((Trit)(POS) == trit_max(ZERO, POS));
    assert((Trit)(ZERO) == trit_max(NEG, ZERO));
}

void test_test_trit_abs_non_negative(void) {
    assert((Trit)(POS) == trit_abs(NEG));
    assert((Trit)(ZERO) == trit_abs(ZERO));
    assert((Trit)(POS) == trit_abs(POS));
}

void test_test_trit_to_packed_conversion(void) {
    assert((u8)(PACKED_NEG) == trit_to_packed(NEG));
    assert((u8)(PACKED_ZERO) == trit_to_packed(ZERO));
    assert((u8)(PACKED_ONE) == trit_to_packed(POS));
}

void test_test_packed_to_trit_conversion(void) {
    assert((Trit)(NEG) == packed_to_trit(PACKED_NEG));
    assert((Trit)(ZERO) == packed_to_trit(PACKED_ZERO));
    assert((Trit)(POS) == packed_to_trit(PACKED_ONE));
}

void test_test_ternary_word_pack_unpack_roundtrip(void) {
    int src = (Trit[]){ NEG, ZERO, POS, NEG, ZERO, POS };
    int packed = ternary_word_pack(&&rc, 6);
    int unpacked = ternary_word_unpack(packed, 6);
    assert(src.len == unpacked.len);
    /* for-each loop (see t27 source) */
    {
        assert(s == u);
    }
}

void test_test_trit_from_i8_valid_values(void) {
    assert((Trit)(NEG) == trit_from_i8(-1));
    assert((Trit)(ZERO) == trit_from_i8(0));
    assert((Trit)(POS) == trit_from_i8(1));
}

void test_test_trit_from_i8_invalid_values_clamp_to_zero(void) {
    assert((Trit)(ZERO) == trit_from_i8(-2));
    assert((Trit)(ZERO) == trit_from_i8(2));
    assert((Trit)(ZERO) == trit_from_i8(100));
    assert((Trit)(ZERO) == trit_from_i8(-100));
}

void test_test_trit_and_truth_table(void) {
    assert((Trit)(NEG) == trit_and(NEG, NEG));
    assert((Trit)(NEG) == trit_and(NEG, ZERO));
    assert((Trit)(NEG) == trit_and(NEG, POS));
    assert((Trit)(NEG) == trit_and(ZERO, NEG));
    assert((Trit)(ZERO) == trit_and(ZERO, ZERO));
    assert((Trit)(ZERO) == trit_and(ZERO, POS));
    assert((Trit)(NEG) == trit_and(POS, NEG));
    assert((Trit)(ZERO) == trit_and(POS, ZERO));
    assert((Trit)(POS) == trit_and(POS, POS));
}

void test_test_trit_or_truth_table(void) {
    assert((Trit)(NEG) == trit_or(NEG, NEG));
    assert((Trit)(ZERO) == trit_or(NEG, ZERO));
    assert((Trit)(POS) == trit_or(NEG, POS));
    assert((Trit)(ZERO) == trit_or(ZERO, NEG));
    assert((Trit)(ZERO) == trit_or(ZERO, ZERO));
    assert((Trit)(POS) == trit_or(ZERO, POS));
    assert((Trit)(POS) == trit_or(POS, NEG));
    assert((Trit)(POS) == trit_or(POS, ZERO));
    assert((Trit)(POS) == trit_or(POS, POS));
}

void test_test_trit_xor_truth_table(void) {
    assert((Trit)(NEG) == trit_xor(NEG, NEG));
    assert((Trit)(ZERO) == trit_xor(NEG, ZERO));
    assert((Trit)(POS) == trit_xor(NEG, POS));
    assert((Trit)(ZERO) == trit_xor(ZERO, NEG));
    assert((Trit)(ZERO) == trit_xor(ZERO, ZERO));
    assert((Trit)(POS) == trit_xor(ZERO, POS));
    assert((Trit)(POS) == trit_xor(POS, NEG));
    assert((Trit)(POS) == trit_xor(POS, ZERO));
    assert((Trit)(ZERO) == trit_xor(POS, POS));
}

void test_test_trit_not_truth_table(void) {
    assert((Trit)(POS) == trit_not(NEG));
    assert((Trit)(POS) == trit_not(ZERO));
    assert((Trit)(ZERO) == trit_not(POS));
}

void test_test_trit_select_condition_true(void) {
    assert((Trit)(NEG) == trit_select(POS, NEG, POS));
    assert((Trit)(ZERO) == trit_select(POS, ZERO, NEG));
    assert((Trit)(POS) == trit_select(POS, POS, ZERO));
}

void test_test_trit_select_condition_false(void) {
    assert((Trit)(POS) == trit_select(NEG, NEG, POS));
    assert((Trit)(NEG) == trit_select(NEG, ZERO, NEG));
    assert((Trit)(ZERO) == trit_select(NEG, POS, ZERO));
    assert((Trit)(POS) == trit_select(ZERO, NEG, POS));
    assert((Trit)(NEG) == trit_select(ZERO, ZERO, NEG));
    assert((Trit)(ZERO) == trit_select(ZERO, POS, ZERO));
}

void test_test_trit_and_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_and(a, b) == trit_and(b, a));
        }
    }
}

void test_test_trit_or_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_or(a, b) == trit_or(b, a));
        }
    }
}

void test_test_trit_xor_commutative(void) {
    int trits = (Trit[]){ NEG, ZERO, POS };
    /* for-each loop (see t27 source) */
    {
        /* for-each loop (see t27 source) */
        {
            assert(trit_xor(a, b) == trit_xor(b, a));
        }
    }
}

void test_test_packed_trit_count_zeros(void) {
    PackedTrit packed = 0;
    packed = pack_trit(ZERO, 0, packed);
    packed = pack_trit(ZERO, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    packed = pack_trit(POS, 3, packed);
    assert((u8)(3) == packed_trit_count(packed, ZERO));
}

void test_test_packed_trit_count_pos(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(POS, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    packed = pack_trit(NEG, 3, packed);
    assert((u8)(2) == packed_trit_count(packed, POS));
}

void test_test_packed_trit_count_neg(void) {
    PackedTrit packed = 0;
    packed = pack_trit(NEG, 0, packed);
    packed = pack_trit(NEG, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    packed = pack_trit(POS, 3, packed);
    assert((u8)(2) == packed_trit_count(packed, NEG));
}

void test_test_packed_trit_all_equal_true(void) {
    PackedTrit packed = 0;
    /* for-each loop (see t27 source) */
    {
        packed = pack_trit(POS, (i), packed);
    }
    assert(packed_trit_all_equal(packed, POS));
}

void test_test_packed_trit_all_equal_false(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(POS, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    assert(!packed_trit_all_equal(packed, POS));
}

void test_test_packed_trit_is_zero_true(void) {
    PackedTrit packed = 0;
    /* for-each loop (see t27 source) */
    {
        packed = pack_trit(ZERO, (i), packed);
    }
    assert(packed_trit_is_zero(packed));
}

void test_test_packed_trit_is_zero_false(void) {
    PackedTrit packed = 0;
    packed = pack_trit(ZERO, 0, packed);
    packed = pack_trit(POS, 1, packed);
    assert(!packed_trit_is_zero(packed));
}

void test_test_packed_trit_is_all_same_true(void) {
    PackedTrit packed = 0;
    /* for-each loop (see t27 source) */
    {
        packed = pack_trit(NEG, (i), packed);
    }
    assert(packed_trit_is_all_same(packed));
}

void test_test_packed_trit_is_all_same_false(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(ZERO, 1, packed);
    assert(!packed_trit_is_all_same(packed));
}

void test_test_packed_trit_nand_basic(void) {
    PackedTrit a = 0;
    PackedTrit b = 0;
    a = pack_trit(POS, 0, a);
    a = pack_trit(POS, 1, a);
    b = pack_trit(POS, 0, b);
    b = pack_trit(ZERO, 1, b);
    int result = packed_trit_nand(a, b);
    assert((Trit)(ZERO) == unpack_trit(0, result).value);
    assert((Trit)(POS) == unpack_trit(1, result).value);
}

void test_test_packed_trit_nor_basic(void) {
    PackedTrit a = 0;
    PackedTrit b = 0;
    a = pack_trit(POS, 0, a);
    a = pack_trit(ZERO, 1, a);
    b = pack_trit(ZERO, 0, b);
    b = pack_trit(ZERO, 1, b);
    int result = packed_trit_nor(a, b);
    assert((Trit)(ZERO) == unpack_trit(0, result).value);
    assert((Trit)(POS) == unpack_trit(1, result).value);
}

void test_test_packed_trit_xnor_equal_returns_pos(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(NEG, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    int result = packed_trit_xnor(packed, packed);
    assert((Trit)(POS) == unpack_trit(0, result).value);
    assert((Trit)(POS) == unpack_trit(1, result).value);
    assert((Trit)(POS) == unpack_trit(2, result).value);
}

void test_test_packed_trit_xnor_different_returns_neg(void) {
    PackedTrit a = 0;
    PackedTrit b = 0;
    a = pack_trit(POS, 0, a);
    b = pack_trit(NEG, 0, b);
    int result = packed_trit_xnor(a, b);
    assert((Trit)(NEG) == unpack_trit(0, result).value);
}

void test_test_packed_trit_shift_left_basic(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(NEG, 1, packed);
    packed = pack_trit(ZERO, 2, packed);
    int result = packed_trit_shift_left(packed, 1);
    assert((Trit)(NEG) == unpack_trit(1, result).value);
    assert((Trit)(ZERO) == unpack_trit(2, result).value);
    assert((Trit)(ZERO) == unpack_trit(0, result).value);
}

void test_test_packed_trit_shift_left_by_eight_returns_zero(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(NEG, 1, packed);
    int result = packed_trit_shift_left(packed, 8);
    assert((PackedTrit)(0) == result);
}

void test_test_packed_trit_shift_right_basic(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 1, packed);
    packed = pack_trit(NEG, 2, packed);
    packed = pack_trit(ZERO, 3, packed);
    int result = packed_trit_shift_right(packed, 1);
    assert((Trit)(POS) == unpack_trit(0, result).value);
    assert((Trit)(NEG) == unpack_trit(1, result).value);
    assert((Trit)(ZERO) == unpack_trit(2, result).value);
}

void test_test_packed_trit_shift_right_by_eight_returns_zero(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 7, packed);
    int result = packed_trit_shift_right(packed, 8);
    assert((PackedTrit)(0) == result);
}

void test_test_packed_trit_rotate_left_basic(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 7, packed);
    packed = pack_trit(NEG, 0, packed);
    int result = packed_trit_rotate_left(packed, 1);
    assert((Trit)(POS) == unpack_trit(0, result).value);
    assert((Trit)(NEG) == unpack_trit(1, result).value);
}

void test_test_packed_trit_rotate_left_by_three(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 5, packed);
    int result = packed_trit_rotate_left(packed, 3);
    assert((Trit)(POS) == unpack_trit(2, result).value);
}

void test_test_packed_trit_rotate_right_basic(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 0, packed);
    packed = pack_trit(NEG, 7, packed);
    int result = packed_trit_rotate_right(packed, 1);
    assert((Trit)(POS) == unpack_trit(7, result).value);
    assert((Trit)(ZERO) == unpack_trit(0, result).value);
}

void test_test_packed_trit_rotate_right_by_four(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 2, packed);
    int result = packed_trit_rotate_right(packed, 4);
    assert((Trit)(POS) == unpack_trit(6, result).value);
}

void test_test_packed_trit_shift_rotate_are_different(void) {
    PackedTrit packed = 0;
    packed = pack_trit(POS, 7, packed);
    int shift_result = packed_trit_shift_left(packed, 1);
    int rotate_result = packed_trit_rotate_left(packed, 1);
    assert((Trit)(ZERO) == unpack_trit(0, shift_result).value);
    assert((Trit)(POS) == unpack_trit(0, rotate_result).value);
}

void test_test_ternary_word_is_zero_true(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    assert(ternary_word_is_zero(word));
}

void test_test_ternary_word_is_zero_false(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(POS, 0, word[0]);
    assert(!ternary_word_is_zero(word));
}

void test_test_ternary_word_count_zeros(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(ZERO, 0, word[0]);
    word[0] = pack_trit(ZERO, 1, word[0]);
    word[0] = pack_trit(POS, 2, word[0]);
    assert((u8)(2) == ternary_word_count(word, ZERO));
    assert((u8)(1) == ternary_word_count(word, POS));
}

void test_test_ternary_word_count_all_same(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        int byte_idx = (i / TRITS_PER_BYTE);
        int trit_pos = ((i % TRITS_PER_BYTE));
        word[byte_idx] = pack_trit(NEG, trit_pos, word[byte_idx]);
    }
    assert(TRITS_PER_WORD == ternary_word_count(word, NEG));
}

void test_test_ternary_word_equal_same(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(POS, 0, word[0]);
    word[1] = pack_trit(NEG, 0, word[1]);
    assert(ternary_word_eq(word, word));
}

void test_test_ternary_word_equal_different(void) {
    TernaryWord word_a = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    TernaryWord word_b = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word_a[0] = pack_trit(POS, 0, word_a[0]);
    word_b[0] = pack_trit(NEG, 0, word_b[0]);
    assert(!ternary_word_eq(word_a, word_b));
}

void test_test_ternary_word_negate(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(POS, 0, word[0]);
    word[0] = pack_trit(NEG, 1, word[0]);
    word[0] = pack_trit(ZERO, 2, word[0]);
    int negated = ternary_word_negate(word);
    assert((Trit)(NEG) == unpack_trit(0, negated[0]).value);
    assert((Trit)(POS) == unpack_trit(1, negated[0]).value);
    assert((Trit)(ZERO) == unpack_trit(2, negated[0]).value);
}

void test_test_ternary_word_negate_double_identity(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(POS, 0, word[0]);
    word[1] = pack_trit(NEG, 0, word[1]);
    int double_negated = ternary_word_negate(ternary_word_negate(word));
    assert(ternary_word_eq(word, double_negated));
}

void test_test_ternary_word_is_all_same_true(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        int byte_idx = (i / TRITS_PER_BYTE);
        int trit_pos = ((i % TRITS_PER_BYTE));
        word[byte_idx] = pack_trit(POS, trit_pos, word[byte_idx]);
    }
    assert(ternary_word_is_all_same(word));
}

void test_test_ternary_word_is_all_same_false(void) {
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    word[0] = pack_trit(POS, 0, word[0]);
    word[0] = pack_trit(NEG, 1, word[0]);
    assert(!ternary_word_is_all_same(word));
}


/* -------------------------------------------------------
   Benchmarks
   ------------------------------------------------------- */

void bench_trit_add_latency(void) {
    /* bench: bench_trit_add_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_add(POS, NEG);
    }
    (void)result;
}

void bench_trit_multiply_latency(void) {
    /* bench: bench_trit_multiply_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_multiply(POS, NEG);
    }
    (void)result;
}

void bench_trit_negate_latency(void) {
    /* bench: bench_trit_negate_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_negate(POS);
    }
    (void)result;
}

void bench_pack_trit_latency(void) {
    /* bench: bench_pack_trit_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    /* for-each loop (see t27 source) */
    {
        result = pack_trit(POS, 3, result);
    }
    (void)result;
}

void bench_unpack_trit_latency(void) {
    /* bench: bench_unpack_trit_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        int unpacked = unpack_trit(3, 0);
        result = unpacked.value;
    }
    (void)result;
}

void bench_trit_and_latency(void) {
    /* bench: bench_trit_and_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_and(POS, NEG);
    }
    (void)result;
}

void bench_trit_or_latency(void) {
    /* bench: bench_trit_or_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_or(POS, NEG);
    }
    (void)result;
}

void bench_trit_xor_latency(void) {
    /* bench: bench_trit_xor_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_xor(POS, NEG);
    }
    (void)result;
}

void bench_trit_not_latency(void) {
    /* bench: bench_trit_not_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_not(POS);
    }
    (void)result;
}

void bench_trit_select_latency(void) {
    /* bench: bench_trit_select_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_select(POS, NEG, ZERO);
    }
    (void)result;
}

void bench_trit_from_i8_latency(void) {
    /* bench: bench_trit_from_i8_latency */
    /* @setEvalBranchQuota(10000) */;
    Trit result = ZERO;
    /* for-each loop (see t27 source) */
    {
        result = trit_from_i8(1);
    }
    (void)result;
}

void bench_packed_trit_count_latency(void) {
    /* bench: bench_packed_trit_count_latency */
    /* @setEvalBranchQuota(10000) */;
    uint8_t result = 0;
    PackedTrit packed = 0xAA;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_count(packed, POS);
    }
    (void)result;
}

void bench_packed_trit_all_equal_latency(void) {
    /* bench: bench_packed_trit_all_equal_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    PackedTrit packed = 0x55;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_all_equal(packed, ZERO);
    }
    (void)result;
}

void bench_packed_trit_is_zero_latency(void) {
    /* bench: bench_packed_trit_is_zero_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    PackedTrit packed = 0x00;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_is_zero(packed);
    }
    (void)result;
}

void bench_packed_trit_nand_latency(void) {
    /* bench: bench_packed_trit_nand_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit a = 0xFF;
    PackedTrit b = 0xFF;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_nand(a, b);
    }
    (void)result;
}

void bench_packed_trit_nor_latency(void) {
    /* bench: bench_packed_trit_nor_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit a = 0x00;
    PackedTrit b = 0x00;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_nor(a, b);
    }
    (void)result;
}

void bench_packed_trit_xnor_latency(void) {
    /* bench: bench_packed_trit_xnor_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit a = 0xAA;
    PackedTrit b = 0x55;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_xnor(a, b);
    }
    (void)result;
}

void bench_packed_trit_shift_left_latency(void) {
    /* bench: bench_packed_trit_shift_left_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit packed = 0xFF;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_shift_left(packed, 1);
    }
    (void)result;
}

void bench_packed_trit_shift_right_latency(void) {
    /* bench: bench_packed_trit_shift_right_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit packed = 0xFF;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_shift_right(packed, 1);
    }
    (void)result;
}

void bench_packed_trit_rotate_left_latency(void) {
    /* bench: bench_packed_trit_rotate_left_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit packed = 0xFF;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_rotate_left(packed, 1);
    }
    (void)result;
}

void bench_packed_trit_rotate_right_latency(void) {
    /* bench: bench_packed_trit_rotate_right_latency */
    /* @setEvalBranchQuota(10000) */;
    PackedTrit result = 0;
    PackedTrit packed = 0xFF;
    /* for-each loop (see t27 source) */
    {
        result = packed_trit_rotate_right(packed, 1);
    }
    (void)result;
}

void bench_ternary_word_is_zero_latency(void) {
    /* bench: bench_ternary_word_is_zero_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    TernaryWord word = /* repeat: (u8[]){ 0 } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        result = ternary_word_is_zero(word);
    }
    (void)result;
}

void bench_ternary_word_count_latency(void) {
    /* bench: bench_ternary_word_count_latency */
    /* @setEvalBranchQuota(10000) */;
    uint8_t result = 0;
    TernaryWord word = (u8[]){ 0xAA, 0x55, 0xAA, 0x55, 0xAA };
    /* for-each loop (see t27 source) */
    {
        result = ternary_word_count(word, POS);
    }
    (void)result;
}

void bench_ternary_word_eq_latency(void) {
    /* bench: bench_ternary_word_eq_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    TernaryWord word_a = /* repeat: (u8[]){ 0xAA } ** WORD_BYTES */ {0};
    TernaryWord word_b = /* repeat: (u8[]){ 0xAA } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        result = ternary_word_eq(word_a, word_b);
    }
    (void)result;
}

void bench_ternary_word_negate_latency(void) {
    /* bench: bench_ternary_word_negate_latency */
    /* @setEvalBranchQuota(10000) */;
    TernaryWord result = {0};
    TernaryWord word = (u8[]){ 0x55, 0xAA, 0x55, 0xAA, 0x55 };
    /* for-each loop (see t27 source) */
    {
        result = ternary_word_negate(word);
    }
    (void)result;
}

void bench_ternary_word_is_all_same_latency(void) {
    /* bench: bench_ternary_word_is_all_same_latency */
    /* @setEvalBranchQuota(10000) */;
    bool result = false;
    TernaryWord word = /* repeat: (u8[]){ 0x00 } ** WORD_BYTES */ {0};
    /* for-each loop (see t27 source) */
    {
        result = ternary_word_is_all_same(word);
    }
    (void)result;
}


#endif /* TRITYPE_BASE_H */
