# JSON files that do not parse today. Each line is a debt, not a rule.
# Remove the line when the file is fixed; the gate then holds it fixed.
#
# EMPTY, and that is the measurement. This ledger held six entries naming files
# that are in neither git nor the working tree -- five under
# .trinity/icarus-baselines/specs/scratch/ and research/toda_derivation.json.
# The scan finds ZERO unparseable and ZERO empty tracked JSON files today, so
# every line here was debt about something that had already left. The gate only
# ever SUBTRACTED this file from the current bad set, so nothing looked at an
# entry that had outlived its file. `tri ledgers audit` planted a line naming a
# file it had just parsed and the gate stayed green.
#
# With the ledger empty the gate holds the line at zero: any unparseable JSON
# fails, with no slack to hide in.
