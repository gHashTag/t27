# OWNERS — tests/

## Primary

**Q-QA** — integration runners (`run_all.sh`, conformance validators).

## Dependencies

- `./scripts/tri` (CLI shim), `bootstrap/target/release/t27c`, `specs/`, `conformance/`, `gen/`.

## Outputs

Pass/fail signal for PHI Loop CI and local sweeps.
