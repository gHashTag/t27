// ============================================================================
// Generated from t27 spec: Linker_Testbench
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module Linker_Testbench (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [31:0] CLK_PERIOD = 20;

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: tick
    function [31:0] tick; // -> auto
        begin
            clk = 1'b0;
            clk = 1'b1;
        end
    endfunction

    // function: reset
    function [31:0] reset; // -> auto
        begin
            rst_n = 1'b0;
            tick();
            tick();
            rst_n = 1'b1;
            tick();
        end
    endfunction

    // function: align_addr
    function [31:0] align_addr; // -> u32
        input [31:0] addr;
        input [31:0] align;
        begin
            align_addr = ((addr + mask) && !mask);
        end
    endfunction

    // function: section_size
    function [31:0] section_size; // -> u32
        input [31:0] num_instrs;
        input [31:0] instr_width;
        begin
            section_size = (num_instrs * (instr_width / 8));
        end
    endfunction
    // -------------------------------------------------------
    // Test assertions (from test blocks)
    // -------------------------------------------------------
    // synthesis translate_off
    // test: test_reset_state
    initial begin : test_reset_state_test
        $display("[TEST] test_reset_state : starting");
        // reset();
        $display("[TEST] test_reset_state : PASSED");
    end
    // test: test_align_addr_aligned
    initial begin : test_align_addr_aligned_test
        $display("[TEST] test_align_addr_aligned : starting");
        //         reg [31:0] a = align_addr(8, 4);
        $display("[TEST] test_align_addr_aligned : PASSED");
    end
    // test: test_align_addr_unaligned
    initial begin : test_align_addr_unaligned_test
        $display("[TEST] test_align_addr_unaligned : starting");
        //         reg [31:0] a = align_addr(5, 4);
        $display("[TEST] test_align_addr_unaligned : PASSED");
    end
    // test: test_align_addr_zero
    initial begin : test_align_addr_zero_test
        $display("[TEST] test_align_addr_zero : starting");
        //         reg [31:0] a = align_addr(0, 4);
        $display("[TEST] test_align_addr_zero : PASSED");
    end
    // test: test_section_size
    initial begin : test_section_size_test
        $display("[TEST] test_section_size : starting");
        //         reg [31:0] s = section_size(100, 32);
        $display("[TEST] test_section_size : PASSED");
    end
    // test: test_max_sections
    initial begin : test_max_sections_test
        $display("[TEST] test_max_sections : starting");
        $display("[TEST] test_max_sections : PASSED");
    end
    // synthesis translate_on

    // -------------------------------------------------------
    // Invariant checks (compile-time assertions)
    // -------------------------------------------------------
    // invariant: section_align_power_of_2

    // -------------------------------------------------------
    // Benchmark blocks (simulation only)
    // -------------------------------------------------------
    initial begin : bench_linker_bench // synthesis translate_off
        $display("[BENCH] bench_linker : starting");
        integer _bench_cycles = 0;
        // reset();
        _bench_cycles = _bench_cycles + 1;
        //         reg [31:0] i = 0;
        _bench_cycles = _bench_cycles + 1;
        $display("[BENCH] bench_linker : %%0d cycles", _bench_cycles);
        $display("[BENCH] bench_linker : DONE");
    end // synthesis translate_on

endmodule

`default_nettype wire
