// ============================================================================
// Generated from t27 spec: BootROM_Testbench
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module BootROM_Testbench (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [31:0] CLK_PERIOD = 20;

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: tick
    function [31:0] tick; // -> auto
        begin
            clk = 1'b0;
            clk = 1'b1;
        end
    endfunction

    // function: reset
    function [31:0] reset; // -> auto
        begin
            rst_n = 1'b0;
            tick();
            tick();
            rst_n = 1'b1;
            tick();
            pc = RESET_VECTOR;
        end
    endfunction

    // function: read_rom
    function [31:0] read_rom; // -> u32
        input [31:0] addr;
        begin
            rom_addr = addr;
            tick();
            read_rom = rom_data;
        end
    endfunction

    // function: boot_sequence
    function boot_sequence; // -> bool
        begin
            pc = RESET_VECTOR;
            reg [31:0] magic = read_rom(pc);
            boot_valid = 1'b1;
            reg [31:0] i = 0;
            boot_sequence = 1'b1;
        end
    endfunction
    // -------------------------------------------------------
    // Test assertions (from test blocks)
    // -------------------------------------------------------
    // synthesis translate_off
    // test: test_reset_state
    initial begin : test_reset_state_test
        $display("[TEST] test_reset_state : starting");
        // reset();
        $display("[TEST] test_reset_state : PASSED");
    end
    // test: test_reset_vector
    initial begin : test_reset_vector_test
        $display("[TEST] test_reset_vector : starting");
        $display("[TEST] test_reset_vector : PASSED");
    end
    // test: test_rom_size
    initial begin : test_rom_size_test
        $display("[TEST] test_rom_size : starting");
        $display("[TEST] test_rom_size : PASSED");
    end
    // test: test_boot_magic
    initial begin : test_boot_magic_test
        $display("[TEST] test_boot_magic : starting");
        $display("[TEST] test_boot_magic : PASSED");
    end
    // synthesis translate_on

    // -------------------------------------------------------
    // Invariant checks (compile-time assertions)
    // -------------------------------------------------------
    // invariant: rom_size_positive

    // -------------------------------------------------------
    // Benchmark blocks (simulation only)
    // -------------------------------------------------------
    initial begin : bench_bootrom_bench // synthesis translate_off
        $display("[BENCH] bench_bootrom : starting");
        integer _bench_cycles = 0;
        // reset();
        _bench_cycles = _bench_cycles + 1;
        // boot_sequence();
        _bench_cycles = _bench_cycles + 1;
        $display("[BENCH] bench_bootrom : %%0d cycles", _bench_cycles);
        $display("[BENCH] bench_bootrom : DONE");
    end // synthesis translate_on

endmodule

`default_nettype wire
