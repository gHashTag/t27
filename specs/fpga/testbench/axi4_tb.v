// ============================================================================
// Generated from t27 spec: AXI4_Testbench
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module AXI4_Testbench (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [31:0] CLK_PERIOD = 20;

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: tick
    function [31:0] tick; // -> auto
        begin
            clk = 1'b0;
            clk = 1'b1;
        end
    endfunction

    // function: reset
    function [31:0] reset; // -> auto
        begin
            rst_n = 1'b0;
            tick();
            tick();
            rst_n = 1'b1;
            tick();
        end
    endfunction

    // function: axi_write_single
    function [31:0] axi_write_single; // -> u32
        input [31:0] addr;
        input [31:0] data;
        begin
            aw_valid = 1'b1;
            aw_addr = addr;
            aw_len = 0;
            aw_size = 2;
            aw_burst = 1;
            tick();
            w_valid = 1'b1;
            w_data = data;
            w_strb = 4'hF;
            w_last = 1'b1;
            tick();
            b_ready = 1'b1;
            b_ready = 1'b0;
            axi_write_single = resp;
        end
    endfunction

    // function: axi_read_single
    function [31:0] axi_read_single; // -> u32
        input [31:0] addr;
        begin
            ar_valid = 1'b1;
            ar_addr = addr;
            ar_len = 0;
            ar_size = 2;
            ar_burst = 1;
            tick();
            r_ready = 1'b1;
            r_ready = 1'b0;
            axi_read_single = data;
        end
    endfunction
    // -------------------------------------------------------
    // Test assertions (from test blocks)
    // -------------------------------------------------------
    // synthesis translate_off
    // test: test_reset_state
    // test: test_single_write
    // test: test_single_read
    // test: test_write_read_roundtrip
    // test: test_multiple_writes
    // test: test_aligned_address
    // test: test_burst_len_zero_means_single
    // synthesis translate_on

    // -------------------------------------------------------
    // Invariant checks (compile-time assertions)
    // -------------------------------------------------------
    // invariant: data_width_power_of_2
    // invariant: addr_width_valid

    // -------------------------------------------------------
    // Benchmark placeholders
    // -------------------------------------------------------
    // bench: bench_axi_throughput

endmodule

`default_nettype wire
