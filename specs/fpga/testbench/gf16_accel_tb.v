// ============================================================================
// Generated from t27 spec: GF16_Accel_Testbench
// DO NOT EDIT - generated by t27c gen-verilog
// phi^2 + 1/phi^2 = 3 | TRINITY
// ============================================================================

`timescale 1ns / 1ps
`default_nettype none

module GF16_Accel_Testbench (
    input  wire        clk,
    input  wire        rst_n,
    input  wire        en,
    output wire        ready
);

    // -------------------------------------------------------
    // Parameters (from const declarations)
    // -------------------------------------------------------
    localparam [31:0] CLK_PERIOD = 20;

    assign ready = 1'b1;

    // -------------------------------------------------------
    // Combinational logic (from function declarations)
    // -------------------------------------------------------

    // function: tick
    function [31:0] tick; // -> auto
        begin
            clk = 1'b0;
            clk = 1'b1;
        end
    endfunction

    // function: reset
    function [31:0] reset; // -> auto
        begin
            rst_n = 1'b0;
            tick();
            tick();
            rst_n = 1'b1;
            tick();
        end
    endfunction

    // function: gf16_add
    function [15:0] gf16_add; // -> u16
        input [15:0] a;
        input [15:0] b;
        begin
            op_a = a;
            op_b = b;
            op_code = 0;
            start = 1'b1;
            tick();
            start = 1'b0;
        end
    endfunction

    // function: gf16_mul
    function [15:0] gf16_mul; // -> u16
        input [15:0] a;
        input [15:0] b;
        begin
            op_a = a;
            op_b = b;
            op_code = 1;
            start = 1'b1;
            tick();
            start = 1'b0;
        end
    endfunction

    // function: gf16_mac
    function [15:0] gf16_mac; // -> u16
        input [15:0] a;
        input [15:0] b;
        input [15:0] acc;
        begin
            op_a = a;
            op_b = b;
            op_code = 2;
            start = 1'b1;
            tick();
            start = 1'b0;
        end
    endfunction
    // -------------------------------------------------------
    // Test assertions (from test blocks)
    // -------------------------------------------------------
    // synthesis translate_off
    // test: test_reset_state
    // test: test_add_zero
    // test: test_mul_by_one
    // test: test_mul_by_zero
    // test: test_mac_identity
    // test: test_commutative_add
    // test: test_commutative_mul
    // test: test_phi_squared
    // synthesis translate_on

    // -------------------------------------------------------
    // Invariant checks (compile-time assertions)
    // -------------------------------------------------------
    // invariant: phi_tolerance_positive

    // -------------------------------------------------------
    // Benchmark placeholders
    // -------------------------------------------------------
    // bench: bench_gf16_throughput

endmodule

`default_nettype wire
