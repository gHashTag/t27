// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// 2D average pooling formula | φ² + 1/φ² = 3 | TRINITY

module Avgpool2d;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 1. Constants
    // ═══════════════════════════════════════════════════════════

    const DEFAULT_KERNEL : []u32 = "[2, 2]";
    const DEFAULT_STRIDE : []u32 = "[2, 2]";

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const PoolConfig = struct {
        kernel_size : []u32,
        stride : []u32,
        padding : []u32,
    };

    pub const PoolShape = struct {
        batch : u32,
        channels : u32,
        height_in : u32,
        width_in : u32,
        height_out : u32,
        width_out : u32,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // calc_output_size(height_in: u32, width_in: u32, kernel_size: []u32, stride: []u32, padding: []u32) → PoolShape
    fn calc_output_size(height_in: u32, width_in: u32, kernel_size: []u32, stride: []u32, padding: []u32) -> PoolShape {
        // TODO: Implement from .tri spec
    }

    // forward(input: []f32, config: PoolConfig) → []f32
    fn forward(input: []f32, config: PoolConfig) -> []f32 {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for calc_output_size() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for forward() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // ═══════════════════════════════════════════════════════════
    // TDD: Invariants (from .tri constraints)
    // ═══════════════════════════════════════════════════════════

    // TODO: invariant -- kernel_size[i] >= 1
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- stride[i] >= 1
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- stride[i] <= kernel_size[i]
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- name: avgpool_formula
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- "Zeiler & Fergus (2014) - Visualizing and Understanding Convolutional Networks"
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- "He et al. (2016) - Deep Residual Learning for Image Recognition"
    // Not generated: the constraint is prose, not an expression.

