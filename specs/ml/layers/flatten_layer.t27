// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Vectorization operation | φ² + 1/φ² = 3 | TRINITY

module Flatten;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 1. Constants
    // ═══════════════════════════════════════════════════════════

    const MAX_DIMS : u32 = 8;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const FlattenConfig = struct {
        input_shape : []u32,
    };

    pub const InputTensor = struct {
        dims : []u32,
        data : []f32,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // calc_output_size(input_dims: []u32) → u32
    fn calc_output_size(input_dims: []u32) -> u32 {
        // TODO: Implement from .tri spec
    }

    // forward(input: []f32, config: FlattenConfig) → []f32
    fn forward(input: []f32, config: FlattenConfig) -> []f32 {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for calc_output_size() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for forward() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // ═══════════════════════════════════════════════════════════
    // TDD: Invariants (from .tri constraints)
    // ═══════════════════════════════════════════════════════════

    // TODO: invariant -- len(input_dims) >= 1
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- len(input_dims) <= MAX_DIMS
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- name: flatten_formula
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- "Goodfellow et al. (2016) - Deep Learning"
    // Not generated: the constraint is prose, not an expression.

    // TODO: invariant -- "Used in CNN → Flatten → Dense → Classification"
    // Not generated: the constraint is prose, not an expression.

