// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Reduce spatial dimensions | φ² + 1/φ² = 3 | TRINITY

module Maxpool2d;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const MaxPool2DConfig = struct {
        kernel_size : u32,
        stride : u32,
        padding : u32,
        dilation : u32,
        return_indices : bool,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // forward(input: []const f32, output: []f32, indices: []u32, config: MaxPool2DConfig) → void
    fn forward(input: []const f32, output: []f32, indices: []u32, config: MaxPool2DConfig) -> void {
        // TODO: Implement from .tri spec
    }

    // backward(grad_output: []const f32, input: []const f32, indices: []const u32, grad_input: []f32, config: MaxPool2DConfig) → void
    fn backward(grad_output: []const f32, input: []const f32, indices: []const u32, grad_input: []f32, config: MaxPool2DConfig) -> void {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for forward() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for backward() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

