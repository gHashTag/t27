// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Binary representation | φ² + 1/φ² = 3 | TRINITY

module TriBson;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const BsonValue = struct {
        enum : [Double, String, Document, Array, Binary, ObjectId, Boolean, DateTime, Null, Int32, Int64],
    };

    pub const BsonDocument = struct {
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // parse(data: []const u8, allocator: std.mem.Allocator) → !BsonDocument
    fn parse(data: []const u8, allocator: std.mem.Allocator) -> !BsonDocument {
        // TODO: Implement from .tri spec
    }

    // serialize(doc: BsonDocument, allocator: std.mem.Allocator) → ![]u8
    fn serialize(doc: BsonDocument, allocator: std.mem.Allocator) -> ![]u8 {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for parse() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for serialize() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

