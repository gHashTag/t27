// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Common markdown subset | φ² + 1/φ² = 3 | TRINITY

module TriMarkup;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const MarkdownNode = struct {
        type : []const u8,
        content : []const u8,
        children : []MarkdownNode,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // parse(markdown: []const u8, allocator: std.mem.Allocator) → ![]MarkdownNode
    fn parse(markdown: []const u8, allocator: std.mem.Allocator) -> ![]MarkdownNode {
        // TODO: Implement from .tri spec
    }

    // to_html(nodes: []MarkdownNode, allocator: std.mem.Allocator) → ![]u8
    fn to_html(nodes: []MarkdownNode, allocator: std.mem.Allocator) -> ![]u8 {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for parse() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for to_html() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

