// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Output type must be a monoid | φ² + 1/φ² = 3 | TRINITY

module TriWriter;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Writer(W, T) = struct {
        value : T,
        output : W,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // pure(value: T) → Writer(W, T)
    fn pure(value: T) -> Writer(W, T) {
        // TODO: Implement from .tri spec
    }

    // tell(output: W) → Writer(W, void)
    fn tell(output: W) -> Writer(W, void) {
        // TODO: Implement from .tri spec
    }

    // listen(writer: Writer(W, T)) → Writer(W, (T, W))
    fn listen(writer: Writer(W, T)) -> Writer(W, (T, W)) {
        // TODO: Implement from .tri spec
    }

    // censor(fn: fn(W) -> W, writer: Writer(W, T)) → Writer(W, T)
    fn censor(fn: fn(W) -> W, writer: Writer(W, T)) -> Writer(W, T) {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for pure() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for tell() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for listen() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for censor() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

