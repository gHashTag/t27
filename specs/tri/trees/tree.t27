// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Operations return new trees | φ² + 1/φ² = 3 | TRINITY

module TriTree;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Tree(T) = struct {
        is_leaf : bool,
        value : T,
        left : Tree(T),
        right : Tree(T),
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // leaf(value: T) → Tree(T)
    fn leaf(value: T) -> Tree(T) {
        // TODO: Implement from .tri spec
    }

    // branch(left: Tree(T), right: Tree(T)) → Tree(T)
    fn branch(left: Tree(T), right: Tree(T)) -> Tree(T) {
        // TODO: Implement from .tri spec
    }

    // is_leaf(tree: Tree(T)) → bool
    fn is_leaf(tree: Tree(T)) -> bool {
        // TODO: Implement from .tri spec
    }

    // height(tree: Tree(T)) → usize
    fn height(tree: Tree(T)) -> usize {
        // TODO: Implement from .tri spec
    }

    // size(tree: Tree(T)) → usize
    fn size(tree: Tree(T)) -> usize {
        // TODO: Implement from .tri spec
    }

    // inorder(tree: Tree(T)) → []T
    fn inorder(tree: Tree(T)) -> []T {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for leaf() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for branch() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for is_leaf() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for height() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for size() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for inorder() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

