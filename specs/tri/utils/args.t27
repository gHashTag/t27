// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// -- separates options from positional args | φ² + 1/φ² = 3 | TRINITY

module TriArgs;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Arg = struct {
        short : ?u8,
        long : ?[]const u8,
        required : bool,
    };

    pub const ArgValue = struct {
        value : ?[]const u8,
        present : bool,
    };

    pub const ParseResult = struct {
        positional : [][]const u8,
        named : []ArgValue,
        error : ?[]const u8,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // parse(allocator: std.mem.Allocator) → void
    fn parse(allocator: std.mem.Allocator) -> void {
        // TODO: Implement from .tri spec
    }

    // has_flag(result: ParseResult) → void
    fn has_flag(result: ParseResult) -> void {
        // TODO: Implement from .tri spec
    }

    // get_value(result: ParseResult) → void
    fn get_value(result: ParseResult) -> void {
        // TODO: Implement from .tri spec
    }

    // get_positional(result: ParseResult) → void
    fn get_positional(result: ParseResult) -> void {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

