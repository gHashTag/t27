// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Alpha channel support | φ² + 1/φ² = 3 | TRINITY

module TriColor;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Color = struct {
        r : u8,
        g : u8,
        b : u8,
        a : u8,
    };

    pub const ColorSpace = struct {
        enum : [RGB, HSV, HSL, LAB],
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // rgb(r: u8, g: u8, b: u8) → Color
    fn rgb(r: u8, g: u8, b: u8) -> Color {
        // TODO: Implement from .tri spec
    }

    // to_hex(color: Color, allocator: std.mem.Allocator) → ![]u8
    fn to_hex(color: Color, allocator: std.mem.Allocator) -> ![]u8 {
        // TODO: Implement from .tri spec
    }

    // blend(a: Color, b: Color, factor: f64) → Color
    fn blend(a: Color, b: Color, factor: f64) -> Color {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for rgb() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for to_hex() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for blend() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

