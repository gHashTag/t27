// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Check exhaustiveness at compile time when possible | φ² + 1/φ² = 3 | TRINITY

module []const u8;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Match = struct {
        matched : bool,
        captures : []MatchCapture,
    };

    pub const MatchCapture = struct {
        value : []const u8,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // match_literal(input: []const u8, pattern: []const u8) → bool
    fn match_literal(input: []const u8, pattern: []const u8) -> bool {
        // TODO: Implement from .tri spec
    }

    // match_type(type_name: []const u8, value: Any) → bool
    fn match_type(type_name: []const u8, value: Any) -> bool {
        // TODO: Implement from .tri spec
    }

    // exhaustive(cases: [][]const u8, handled: []bool) → bool
    fn exhaustive(cases: [][]const u8, handled: []bool) -> bool {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for match_literal() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for match_type() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for exhaustive() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

