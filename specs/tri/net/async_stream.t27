// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Lazy evaluation | φ² + 1/φ² = 3 | TRINITY

module TriAsyncStream;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Stream(T) = struct {
        generator : fn() ?T,
        state : StreamState,
    };

    pub const StreamState = struct {
        enum : [Ready, Pending, Done],
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // from(items: []T, allocator: std.mem.Allocator) → !Stream(T)
    fn from(items: []T, allocator: std.mem.Allocator) -> !Stream(T) {
        // TODO: Implement from .tri spec
    }

    // map(stream: Stream(T), fn: fn(T) U) → Stream(U)
    fn map(stream: Stream(T), fn: fn(T) U) -> Stream(U) {
        // TODO: Implement from .tri spec
    }

    // filter(stream: Stream(T), predicate: fn(T) bool) → Stream(T)
    fn filter(stream: Stream(T), predicate: fn(T) bool) -> Stream(T) {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for from() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for map() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for filter() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

