// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Eliminates null pointer issues | φ² + 1/φ² = 3 | TRINITY

module TriOption;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Option(T) = struct {
        is_some : bool,
        value : T,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // some(value: T) → Option(T)
    fn some(value: T) -> Option(T) {
        // TODO: Implement from .tri spec
    }

    // none() → Option(void)
    fn none() -> Option(void) {
        // TODO: Implement from .tri spec
    }

    // unwrap_or(opt: Option(T), default: T) → T
    fn unwrap_or(opt: Option(T), default: T) -> T {
        // TODO: Implement from .tri spec
    }

    // is_some(opt: Option(T)) → bool
    fn is_some(opt: Option(T)) -> bool {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for some() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for none() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for unwrap_or() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for is_some() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

