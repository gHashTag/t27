// SPDX-License-Identifier: Apache-2.0
// t27/specs/
// Uses usize words for efficiency | φ² + 1/φ² = 3 | TRINITY

module TriBitmap;
    use base::types;
    use math::constants;

    // ═══════════════════════════════════════════════════════════
    // 2. Types
    // ═══════════════════════════════════════════════════════════

    pub const Bitmap = struct {
        bits : []usize,
        capacity : usize,
    };

    // ═══════════════════════════════════════════════════════════
    // 3. Core Functions
    // ═══════════════════════════════════════════════════════════

    // init(capacity: usize, allocator: std.mem.Allocator) → !Bitmap
    fn init(capacity: usize, allocator: std.mem.Allocator) -> !Bitmap {
        // TODO: Implement from .tri spec
    }

    // get(bitmap: Bitmap, index: usize) → bool
    fn get(bitmap: Bitmap, index: usize) -> bool {
        // TODO: Implement from .tri spec
    }

    // set(bitmap: *Bitmap, index: usize) → void
    fn set(bitmap: *Bitmap, index: usize) -> void {
        // TODO: Implement from .tri spec
    }

    // clear(bitmap: *Bitmap, index: usize) → void
    fn clear(bitmap: *Bitmap, index: usize) -> void {
        // TODO: Implement from .tri spec
    }

    // flip(bitmap: *Bitmap, index: usize) → void
    fn flip(bitmap: *Bitmap, index: usize) -> void {
        // TODO: Implement from .tri spec
    }

    // set_all(bitmap: *Bitmap) → void
    fn set_all(bitmap: *Bitmap) -> void {
        // TODO: Implement from .tri spec
    }

    // clear_all(bitmap: *Bitmap) → void
    fn clear_all(bitmap: *Bitmap) -> void {
        // TODO: Implement from .tri spec
    }

    // count(bitmap: Bitmap) → usize
    fn count(bitmap: Bitmap) -> usize {
        // TODO: Implement from .tri spec
    }

    // find_first(bitmap: Bitmap) → ?usize
    fn find_first(bitmap: Bitmap) -> ?usize {
        // TODO: Implement from .tri spec
    }

    // find_last(bitmap: Bitmap) → ?usize
    fn find_last(bitmap: Bitmap) -> ?usize {
        // TODO: Implement from .tri spec
    }

    // ═══════════════════════════════════════════════════════════
    // TDD: Tests (from .tri behaviors)
    // ═══════════════════════════════════════════════════════════

    // TODO: behaviour for init() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for get() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for set() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for clear() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for flip() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for set_all() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for clear_all() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for count() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for find_first() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

    // TODO: behaviour for find_last() -- write given/when/then by hand.
    // Not generated: a signature does not imply a behaviour.

