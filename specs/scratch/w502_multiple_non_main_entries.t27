pub fn a() -> u32 { return 11; }
pub fn b() -> u32 { return 13; }
pub fn main() -> u32 { return a() + b(); }

test multiple_non_main_entries {
    assert_eq(a(), 11);
    assert_eq(b(), 13);
    assert_eq(main(), 24);
}
