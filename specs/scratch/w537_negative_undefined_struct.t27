// W537 negative witness: a function returning an undeclared struct type is NOT
// Icarus-lowerable. Issue #1508.
module w537_negative_undefined_struct;

pub fn make_pt(x: u32) -> Pt {
    return Pt{x: x};
}

test should_not_be_icarus_lowerable {
    // Intentionally empty: exercises the lowerability classifier boundary.
}

endmodule
