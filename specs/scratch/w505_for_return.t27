pub fn factorial(n : u32) -> u32 {
    var acc : u32 = 1;
    for i in 1..(n + 1) {
        acc = acc * i;
    }
    return acc;
}

test factorial_test {
    assert_eq(factorial(0), 1);
    assert_eq(factorial(1), 1);
    assert_eq(factorial(5), 120);
}
