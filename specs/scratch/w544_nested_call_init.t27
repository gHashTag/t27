// W544 Variant B witness: a module const initialized by a nested function call.
module w544_nested_call_init;

pub fn inc(x : u32) -> u32 {
    return x + 1;
}

const a : u32 = inc(inc(1));

test nested_call_init {
    assert_eq(a, 3);
}

endmodule
