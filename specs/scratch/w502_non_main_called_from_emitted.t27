pub fn helper() -> u32 { return 1; }
pub fn caller() -> u32 { return helper(); }
pub fn main() -> u32 { return caller(); }

test non_main_called_from_emitted {
    assert_eq(helper(), 1);
    assert_eq(caller(), 1);
    assert_eq(main(), 1);
}
