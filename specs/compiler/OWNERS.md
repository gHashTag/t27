# OWNERS — specs/compiler/

## Primary

**C-Compiler** — lexer, parser, types as `.t27` specifications.

## Dependencies

- `specs/base/` for shared types.

## Note

**L-Lexer** shares this tree with **C-Compiler**; compiler agent is default owner for PR routing.
