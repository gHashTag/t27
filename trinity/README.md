# `trinity/`

Reserved for Trinity layout / tooling artifacts tracked as **normal files** in this repo.

A stray nested `.git` here previously blocked `git add trinity/` (`does not have a commit checked out`). That directory was removed on 2026-04-08.
