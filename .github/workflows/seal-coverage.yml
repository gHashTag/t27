name: SEAL Coverage

on:
  pull_request:
    branches: [master]
  push:
    branches: [master]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run coverage
        run: |
          echo "Running SEAL coverage analysis..."
