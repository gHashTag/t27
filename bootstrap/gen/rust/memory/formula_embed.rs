// Generated from .t27 spec
// DO NOT EDIT — generated by t27c

pub const EMBEDDING_DIM: usize = 27;

pub const FEATURE_VALUE: usize = 0;

pub const FEATURE_COMPLEXITY: usize = 1;

pub const FEATURE_PHI_DIST: usize = 2;

pub const FEATURE_SECTOR_ID: usize = 3;

pub const BASE_FEATURE_COUNT: usize = 4;

#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize, serde::Deserialize)]
pub enum Sector {
    unknown = 0,
    qcd = 1,
    electroweak = 2,
    cosmology = 3,
    condensed = 4,
    nuclear = 5,
    particle = 6,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Formula {
    pub id: Vec<constu8>,
    pub name: Vec<constu8>,
    pub sector: Sector,
    pub value: Float,
    pub complexity: u8,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Embedding {
    pub vector: Vec<Float>,
    pub formula_id: Vec<constu8>,
    pub normalized: bool,
}

#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct Features {
    pub value: Float,
    pub complexity: Float,
    pub phi_distance: Float,
    pub sector_id: Float,
}

pub fn extract_features(formula: Formula) -> Features {
    return Features { value: formula.value, complexity: @as(Float, @floatFromInt(formula.complexity)), phi_distance: phi_distance(formula.value), sector_id: @as(Float, @floatFromInt(@intFromEnum(formula.sector))) };
}

pub fn pad_features(features: Features) -> Vec<Float> { unimplemented!() }

pub fn embed_formula(formula: Formula) -> Embedding {
    let features = extract_features(formula);
    let mut vector = pad_features(features);
    return Embedding { vector: vector, formula_id: formula.id, normalized: true };
}

pub fn embed_query(text: Vec<const u8>) -> Vec<Float> {
    return undefined;
}

pub fn phi_distance(value: Float) -> Float {
    let log_phi = (@log(value) / @log(PHI));
    let nearest = @round(log_phi);
    let phi_power = @pow(PHI, nearest);
    return (@abs((value - phi_power)) / phi_power);
}

