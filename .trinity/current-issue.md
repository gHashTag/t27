# Wave Loop 444 — Theorem-matrix fixture replay + deterministic CI artifact + real-capture fallback + gen-verilog debt (Variant B default)

**Issue:** #1418
**Branch:** `wave-loop-444`
**Milestone:** Continue the FPGA boot-evidence line from Wave Loop 443.

---

## Goal

Wave Loop 443 added explicit PVT-envelope validation to the 24-variant board-less
`tri fpga smoke-gate --theorem-matrix`, making every synthetic `ff`/`tt`/`ss`
corner check `inside_envelope` before a theorem is generated and recording a
closed-vocabulary `envelope_check` in the report. The bench remains blocked
(DLC10 cable not detected, P12 unwired, no relay gate), the `gen-verilog` fix
set on `master` (`701d79b3b`) is still not merged, and the full Trinity
`lake build` is still broken on unrelated physics proofs in
`Trinity/NeutrinoMasses.lean` and `Trinity/H4Lagrangian.lean`.

Wave Loop 444 executes **Variant B** from
`docs/reports/FPGA_LOOP_COOPERATION_W444_2026-07-01.md`:

1. Persist the 24 synthetic PVT-context and raw-ns fixtures generated by
   `--theorem-matrix` to a stable path under `build/fpga/`.
2. Add a `tri fpga smoke-gate --theorem-matrix --replay-fixtures <dir>` mode that
   reproduces the matrix report from the fixtures instead of regenerating them.
3. Extend the `theorem_matrix` report block with a `fixtures` object recording the
   paths to the PVT, raw-ns, summary, and Lean files for each variant.
4. Add Rust unit tests for fixture replay and report-to-fixture consistency.
5. Refresh `docs/reports/T27_VS_FORMAL_HDL_2026.md` with any new public Sparkle
   signals that surface after 2026-07-11.
6. Mint the W444 evidence file and cooperation variants for W445.

**Variant A remains preferred** if the bench unblocks during the wave: run a real
`cclk-sweep --xadc --to-pvt-context`, capture `XADC_LIVE_W444_OPERATING_POINT`,
and mint a quantified combined-check theorem over OSCFSEL 0..7 and all three
corners.

**Variant C is deferred** to a dedicated future wave; the `gen-verilog` fix-set
merge is still too risky to mix with boot-evidence work.

---

## Definition of done

- [ ] `cargo check -p tri` passes.
- [ ] `cargo test -p tri` passes (target: 135+/135 active, 0 ignored, 0 new regressions).
- [ ] `cargo test -p t27c --bin t27c suite::tests` passes.
- [ ] `lake build Trinity.TernaryFPGABoot` passes.
- [ ] `./scripts/tri test` passes with the documented baseline (7 pre-existing
      gen-verilog smoke failures; no new failures; FPGA smoke fails: 0).
- [ ] `./scripts/tri test --json suite-summary.json` produces a parseable
      machine-readable summary and the schema regression tests pass.
- [ ] `tri fpga smoke-gate --synthetic-operating-point --verify-lean --theorem-matrix --json ...`
      produces a 24-variant `theorem_matrix` with per-variant `envelope_check`
      records and `passed: true`.
- [ ] Fixture replay path (`--replay-fixtures`) produces an identical report
      shape and passes in under 30 seconds.
- [ ] Close-out report and next-wave cooperation variants are written.
- [ ] Issue/branch for Wave Loop 445 are created.

---

*φ² + φ⁻² = 3 | TRINITY*
