# Population sizes this repository is ratcheted against (Prop. 209/210).
#
# gate-scripts 37 -> 39 and gate-scripts-lines 5768 -> 6103 -> 6643 are GROWTH,
# locked in here because the scan only prints growth and never fails on it, so an
# unratcheted rise can be given back later with nothing going red. The rise is:
# 69e8f5884 tracked formal/replacement_scan.py (37 -> 38, +335 lines, measured in
# a clean clone at HEAD and never recorded); this change adds the --init guard to
# 12 baseline-owning gates and adds formal/index_drift_scan.py (38 -> 39).
# THESE TWO NUMBERS ASSUME THE SAME COMMIT CARRIES formal/index_drift_scan.py AND
# all 12 guarded gates. Landing a subset makes the scan report a shrink, which is
# the ratchet working, not a bug in it.
#
# specs-lines 76092 is NOT reachable from commit 69e8f5884. That value was
# measured on a working tree carrying 55 edited .t27 files that were never
# committed; a clean clone at HEAD measures 75936 and the scan exits 1 with
# "specs-lines: 76092 -> 75936 (156 missing)". The 156 lines are real (51 added
# smoke-test blocks plus parser fixes such as `module ;` -> `module string;`) and
# four further gates -- spec_parse_gate, runaway_string_scan,
# delimiter_balance_scan, capture_density_scan -- fail in that same clone for the
# same reason. The number here is correct for the corpus; it is the corpus that
# is behind, so the fix is to commit those 55 spec files, NOT to lower this to
# 75936. Lowering it would canonicalise a tree in which 22 declarations are
# silently swallowed by the parser and would still leave the other four gates red.
coq-sources	30
coq-sources-lines	3328
gate-invocations	47
gate-scripts	39
gate-scripts-lines	6643
generated-rtl	13
generated-rtl-lines	1247
specs	497
specs-lines	76092
workflows	38
workflows-lines	3848
