# Prop. 202. Each line: workflow<TAB>step. The prose below each records what
# would END the suspension -- the promise a bare `continue-on-error: true`
# makes and never keeps.
#
# coq-kernel.yml / Build trios-coq:  ** CLOSED 2026-08-13, Prop. 203 **
#   The condition was "exits 0". It does: 28 of 28 .vo, make exit 0, on Rocq
#   9.2. Six mechanical classes were cleared across Props. 201-203 -- missing
#   List import x3, bare nat numerals under Z_ and R_scope, invalid
#   `assert (H : P) := t` syntax, `&&` outside bool_scope, `lia` on an abstract
#   large literal, and a missing PeanoNat import. continue-on-error is OFF and
#   this entry is retired: it is kept as the worked example of a suspension that
#   ended, since every other entry here is one that has not.
#
# formal-yosys.yml / Workflow reachable scan:
#   ENDS WHEN formal-yosys.yml and formal-mutation.yml exist on the default
#   branch. Blocked on the branch decision (Prop. 169), open ~60 waves.
#
# The remaining three are infrastructure, not checks: an artifact upload, a
# status sync, and a matrix generator. They are ratcheted with the rest because
# this gate cannot tell a check from a side effect by parsing YAML, and guessing
# would be classification-by-name (Prop. 197).
formal-yosys.yml	Workflow reachable scan — a cited workflow must exist on the default branch (non-blocking, see Prop. 169)
fpga-build.yml	Upload bitstream + flashing guide
notebook-sync.yml	Update sync status as commit (best-effort)
rings-rust.yml	Generate matrix
